# TODOs

큰 그림은 [devlog/20260729_P01_roadmap.md](devlog/20260729_P01_roadmap.md),
DB 설계는 [devlog/20260730_P02_db-schema.md](devlog/20260730_P02_db-schema.md).
지금 상태와 인수 사항은 [HANDOFF.md](HANDOFF.md).

> **2026-08-02 현재**
> 파이프라인이 전부 DB 를 쓰고, NAS 에 슬라이드가 올라오면 1분 안에 검출까지
> 스스로 간다. 그렇게 4장이 자동으로 들어와 **슬라이드 7 · 시야 270** 이 됐다.
> **검토는 124/270 에서 멈춰 있다** — 처음 3장만 전수 검토가 끝났다.
> 뷰어는 http://172.16.116.98/DiaRUGA/ 다.

## 이름 변경(048)이 남긴 것

- [ ] **옛 자동 사본 정리 — 2026-08-06 이후에 한다** (사용자 판단, 하루 두고
      본다). 대상은 `backup/diatom_????????_??????.db` **23개 · 2.4 GB** 와
      NAS 의 3개 · 300 MB 다. 새 로테이션 glob 이 `DiaRUGA_*` 라 이것들은
      영원히 안 걷힌다.
      **하루를 기다리는 이유**: 시간별 백업이 24판을 채우면 `DiaRUGA_*` 만으로
      24시간 rolling 이 온전해진다. 그때 옛 것은 증명 가능하게 잉여다.
      지우기 전에 `DiaRUGA_*` 가 24개인지부터 센다:
      `ls /data3/DiaRUGA/backup/DiaRUGA_*.db | wc -l`
      **`manual/` 은 이 건과 무관하다** — 48개 · 1.8 GB 가 있지만 그것은
      원래부터 로테이션이 안 건드리는 자리이고(사람이 `--note` 로 뜬 것),
      이름 변경과 상관없이 늘 사람이 지우던 더미다
- [x] **NAS 공유를 쓰는 사람들에게 알리기** — `N:\diatom` → `N:\DiaRUGA`.
      2026-08-05 사용자가 공지했다
- [ ] **옛 이미지 `honestjung/diatom:*` 정리 여부** — 지금은 롤백 경로라 둔다

## 지금 가장 급한 것

- [ ] **`AM22-GC10B 25cm` 그룹핑 확인** — 새 경고 기준(여유 0.2)에 걸린다.
      그룹 안 최소 상관이 0.594 다(정상 0.90~0.99). 초점 시리즈가 잘못 묶였을 수
      있다. **교정을 붙이기 전이 가장 싸다** — 재그룹핑은 `Viewpoint` 를 재편하므로
      그 아래 검출·교정이 통째로 어긋난다
- [ ] **새 슬라이드 4장 검토 (146 시야)** — 파이프라인이 더 갈 곳이 없다.
      자동 판정 그대로면 통과 개체 수를 슬라이드끼리 비교할 수 없다
- [ ] **`export_review.py`** (P02 5단계) — 교정 2,441건이 DB 에만 있다.
      지금 안전망은 `backup_db.py` 하나뿐

## DB 이전 (P02)

- [x] **1. 모델 + 마이그레이션 + `DATABASES`** — 14개 모델, WAL
- [x] **2. `import_json.py`** — 멱등, 8초
- [x] **3. 대조 `verify_db.py`** — 검사 37개 전부 일치
- [x] **4. 뷰어를 DB 로**
- [ ] **5. `export_review.py`** — DB → `review/*.json` (git 에 남길 감사 기록)
- [x] **6. 스크립트 4개를 DB 에 쓰게** — devlog 010·011·012
  - [x] `refilter.py` — UPDATE 한 번(124 시야 0.9초). 문턱이 `ThresholdSet` 에
        남고 실행이 `Run` 에 기록된다. 판정 규칙은 `judge.py` 로 떼어냈다
  - [x] `focus_stack.py` — 메타데이터가 `Stack` 행으로. 잃어버린 품질 지표 49개를
        되찾았다
  - [x] `segment_diatoms.py` — `is_current` 이동과 재바인딩을 한 트랜잭션으로
  - [x] `group_focus_series.py` — 기존 시야가 있으면 경고한다
- [ ] **7. JSON 을 원본에서 내리기** — 파이프라인에서는 빠졌다.
      `import_json.py` 의 중복 코드와 `README` 갱신이 남았다
- [ ] **8. 재바인딩 + 고아 화면**(`/orphans/`) — `rebind.py` 는 만들었다(011).
      **화면이 없다.** 학습·엔진 교체의 전제 — 없으면 교정이 조용히 버려진다

## 계획

- [x] **NAS 연결** — 1분마다 보고 그룹핑·합성·검출까지 자동 (P03 4·5단계)
- [x] **컨테이너화** — 뷰어는 상주, 파이프라인은 일회성 (P03)
- [ ] **데이터셋 + 학습 파이프라인** — 뷰어 교정을 정답으로 YOLO11 커스텀 학습.
      `ObjectReview.geom` 에 지운 것까지 기하가 있어 **어려운 음성 표본**으로 쓴다
- [ ] **검출 엔진 교체** — 학습 완료 후 SAM + 필터를 커스텀 모델로.
      **8번(고아 화면)이 전제다** — 엔진이 바뀌면 `exact` 바인딩이 거의 0 이 된다.

      **`ObjectReview` 의 유일 제약을 함께 봐야 한다.** 지금은
      `(viewpoint, mask_key)` 인데, 이것이 성립하는 이유는 **시야마다 현재 검출
      이미지가 한 장**이기 때문이다. SAM2 는 합성본 한 장에만 달고, 합성본이 없는
      싱글턴 시야는 프레임이 한 장뿐이다. **YOLO 는 프레임마다 단다** — 프레임별
      검출이 `is_current` 가 되는 순간 그 전제가 깨지고, 서로 다른 프레임의 다른
      개체가 교정 행 하나를 나눠 갖는다. 실측으로 시야 452개 중 **203개(45%)**에서
      프레임끼리 `mask_key` 가 겹친다 (2026-08-05).

      **`(viewpoint, frame, mask_key)` 로 가면 안 된다.** `Detection.frame` 은
      `target="stack"` 일 때 **NULL** 이고(오늘 현재 검출 452 중 317이 그렇다),
      **유일 제약에서 NULL 은 서로 다른 값으로 친다** — SQLite 도 Postgres 도.
      제약이 걸려 있는 것처럼 보이면서 **합성본에 붙은 교정 5,491행(82%)에 대해
      조용히 작동을 멈춘다.** 거기에 `Detection.frame` 은 `SET_NULL` 이라 시야
      가르기로 `Frame` 이 지워지면 프레임에 매인 교정이 NULL 쪽으로 넘어간다.

      **NOT NULL 판별자 문자열로 간다.** 센티널은 이미 있다 —
      `data.STACK_KEY = "__stack__"` 이고 주석이 "프레임 이름(`Snap-…`)과 겹칠 수
      없어야 한다" 고 못 박아 두었다. `target_key`(합성본이면 `__stack__`, 아니면
      프레임 이름)를 두고 제약을 `(viewpoint, target_key, mask_key)` 로.
      FK 가 아니라 문자열이라 `Frame` 이 지워져도 값이 남는다 — `mask_key` 를
      FK 대신 문자열로 둔 것과 같은 이유다.

      **이관은 상수 채우기가 아니다.** `db_default="__stack__"` 은 옛 이미지
      INSERT 방어용이고(CLAUDE.md 규칙), 실제 백필은 **시야마다 현재 검출의
      `target` 을 봐야 한다** — 5,491행은 `__stack__`, **1,240행은 그 시야의 프레임
      이름**이다. 한 값으로 밀면 싱글턴 시야 135개의 교정이 통째로 잘못된 열쇠를
      단다. **교체 당일에 발견할 일이 아니다**
- [ ] **Identification** — 논문·handbook 이미지로 종 동정 (별개 트랙, 병행).
      `ClassDef` 표에 속을 더한다(Eucampia · Chaetoceros). **다만 아직 배포가
      필요하다 — 색이 `base.html` 에 키로 박혀 있다**(038). 아래 항목 참고
- [ ] **데스크탑 앱** (P05) — GPU 서버 없는 연구자에게 배포하기 위한 갈래.
      라이선스(AGPL-3.0)·엔진(YOLO)·스택(PySide6)은 정해졌다. **1단계는 읽기
      전용 뷰어**이고, 거기서 PyInstaller + Django 가 얼려지는지를 먼저 본다.
      근거는 `docs/20260805_desktop-app-review.md`, 실측은 049
- [ ] **사람이 개체를 직접 더한다 — 엔진이 놓친 것** (사용자 제안 2026-08-05)

      **"수동 박스 그리기" 로 적혀 있던 그것이다.** 이름을 바꿨다 — **박스가
      아니라 마스크다.** 검출기가 이미 `YOLO11m-seg` 라 엔진이 내는 것이
      폴리곤이고, 계측(장축·단축·면적·신장비·타원 IoU)도 폴리곤에서 나온다.
      사람이 더한 것만 bbox 면 **같은 표에 다른 것이 섞인다** — `judge` 가 쓰는
      지표가 안 나오고, 학습 자료로 내보낼 때도 마스크 자리가 빈다.

      지금 사람이 할 수 있는 것은 **엔진이 낸 것 중에서 고르는 일**뿐이다.
      지우고(오검출), 분류를 바꾸고, 탈락 후보를 되살린다. **엔진이 마스크를
      아예 안 낸 자리는 손댈 방법이 없다** — 재현율의 나머지 절반이 거기 있고
      ("점선조차 없는 것", 아래 재현율 실측 항목), 학습으로 보면 **가장 값진
      양성 표본**이다.

      저장 쪽은 새로 만들 것이 거의 없다. 교정은 이미 `mask_key`(bbox 문자열)로
      붙고 **모든 교정 행이 `geom` 에 기하를 스스로 들고 있다** — 탈락 후보
      되살리기가 `manual: true` 로 가는 그 길의 연장이다. 새로 필요한 것은
      **기하를 사람이 만드는 수단**이다.

      - **어떻게 그리게 할 것인가** — 다각형 찍기냐 브러시냐. 규조각은 윤곽이
        매끈해서 점 10~20개면 충분해 보인다. 화면은 이미 확대·이동이 되고
        (`_detection.html`), 폴리곤을 SVG 로 그리고 있다
      - **엔진이 거들게 할 것인가** (사용자 아이디어) — 누른 자리에서 **conf 를
        낮춰 YOLO 를 돌려 마스크를 제안**한다. 손으로 그리는 것보다 경계가
        일관되고(같은 모델이 그린 것이라) 빠르다.
        **걸리는 것**: 뷰어 컨테이너에는 torch 가 없다(`requirements-web`).
        파이프라인 컨테이너를 불러야 하는데 **GPU 는 한 번에 하나만 돈다** —
        폴러가 도는 중이면 기다린다. 요청 안에서 기다릴 것인가, 큐로 뺄 것인가.
        CPU 로 돌리면 YOLO 1280 이 시야당 1.7초다(049) — **자른 조각이면 훨씬
        싸다.** 먼저 잴 것
      - **되돌리기와 저장 규약**을 지금 것에 맞춘다. `/review` POST 는 그 시야의
        교정 **전체**를 갈아치우므로, 사람이 그린 개체도 그 payload 안에 있어야
        한다
- [ ] **여러 프레임에서 잡힌 같은 개체를 하나로 묶는다** (사용자 제안 2026-08-05)

      YOLO 는 **프레임마다** 검출을 낸다(합성본이 아니라 원본을 본다). 지금은
      `Detection.target="frame"` 으로 따로 서 있고 **시야 안에서 서로 대응이
      없다** — 초점면 5장에 같은 규조각이 다섯 번 잡혀 있는데 그것이 하나라는
      것을 아무도 모른다.

      묶는 근거는 있다: **같은 시야는 스테이지가 안 움직인다.** 초점만 다르므로
      좌표가 거의 같고, IoU·중심거리로 묶인다(문턱은 실측으로 정한다).

      쓸 곳 — 사용자가 든 것은 **3차원 형태**다(초점면마다의 마스크를 쌓으면
      두께 방향이 생긴다). 당장 쓰는 곳은 아직 정하지 않았다. 그 밖에 눈에
      보이는 것:

      - **어느 초점면에서 가장 잘 잡혔나** — 지금 합성본 검출과 프레임 검출이
        따로 노는데, 묶이면 "이 개체는 3번 프레임이 제일 선명하다" 가 나온다
      - **검출 신뢰도** — 여러 프레임에서 반복해 잡힌 것은 진짜일 확률이 높다.
        한 프레임에만 뜬 것은 의심스럽다. 문턱 하나로 거르는 지금보다 낫다
      - **초점면별 형태 변화** — 원반이 초점을 옮기며 어떻게 보이는지

      **이것은 검출 위에 얹히는 층이다.** 검출 결과를 가리키기만 하므로
      `mask_key` 규약이나 교정 쪽은 건드릴 일이 없다 — 멤버는 `Candidate` 를
      FK 로 가리키면 된다.

      그래서 **묶음은 검출 세대에 속한다.** 한 시야 · 한 묶음(batch)의 프레임
      검출들에서 계산되는 것이고, 그 검출이 지워지면 CASCADE 로 함께 지워지는
      것이 **맞다.** 다시 검출하면 다시 계산한다 — 산수지 사람의 판단이 아니다.
      화면이 묶음마다 하나씩 골라 그리는 것(`data._engine_pick`)과 같은 결이다.

      **스키마는 규칙이 정해진 뒤에 박는다** (암상 항목과 같은 이유). 정할 것:
      매칭 문턱(IoU·중심거리)을 실측으로, 그리고 **사람이 묶음을 고칠 수 있게 할
      것인가.** 고치게 하는 순간 그것은 재생성 불가 자료가 되어 교정과 같은 무게를
      갖는다 — 그때 다시 볼 문제이지 지금 미리 박을 문제는 아니다
- [ ] **UI · 편의성 개선** — 지속. 만든 것: 문턱 조정(`/thresholds/`),
      시료 속성 편집(`/d/<slug>/edit/`), 검토 화면 안의 엔진 비교(051).
      남은 것: 시야 병합, 계측 결과 CSV 내보내기.
      **"수동 박스 그리기" 는 위의 "사람이 개체를 직접 더한다" 로 옮겼다** —
      박스가 아니라 마스크로
- [ ] **분류 색을 표에서 뿜어내기** — `ClassDef.color` 칸이 있는데 아무도 안
      쓴다. 색은 `base.html` 이 키로 잡는다(여섯 자리 × 분류마다 한 줄 + 배지).
      **속을 더할 때마다 CSS 를 따라 고쳐야 한다** — 안 고치면 마스크가 투명해
      "지정은 되는데 화면에 안 보이는" 상태가 된다. `--cls` 변수를 분류마다
      뿜고 규칙은 문맥별로 하나씩 두면 30줄이 6줄 + 분류당 한 줄이 된다.
      검토 화면 렌더링을 통째로 건드리는 일이라 따로 잡는다 (038)

## 당면 과제

- [ ] **노두로 표시할 슬라이드를 사람이 고르기** — `0018_slide_sample_kind` 는
      v0.5.0 으로 **운영에 걸렸다**(052). 다만 10장 전부 `core` 라 `위치` 칸이
      아직 깊이만 낸다. `BP09-0901` 이 후보다(깊이가 비어 있다) —
      `/DiaRUGA/d/260803_bp09-0901/edit/` 의 "시료 종류" 에서 고른다
- [ ] **미리보기 정지 화면 치우기** — `/srv/paleolab/DiaRUGA-preview/` 와
      `N:\DiaRUGA\preview\`. **nginx 조각은 결국 설치하지 않았다**(sudo 가
      필요했고 정지 화면으로 충분했다) — 걷어낼 조각이 없다. 날짜별로 쌓이는
      자리라 급하지 않다. 치울 때는 디렉토리째 지운다 (049·052)
- [ ] **암상(lithology) 모델** — 코어 페이지(`/core/<지역>/<코어>/`)에 **띠만
      비워 두었다**(049). 구간 상·하한 · 암상 코드 · 색 · 조직 · 화석 함량 같은
      칸이 정해져야 표가 선다. **기재 규칙이 정해지기 전에는 스키마를 박지
      않는다** — 지금 박으면 나중에 이미 붙은 자료를 옮겨야 한다.
      깊이 축과 시료 위치는 이미 진짜 값으로 그려져 있다
- [ ] **코어 페이지에 깊이 대 조성** — 지금은 깊이 축·시료 위치·현황·속성뿐이다.
      "깊이 대 개체수", "깊이 방향 조성 변화" 가 붙어야 이 화면이 제 몫을 한다.
      **깊이 방향으로 보는 것이 분석의 목적**이라(`data.py` 가 정렬을 그렇게 두는
      이유) 어차피 필요하다 (049)
- [ ] **원형(중심목) 판정 기준 검증** — 1순위. `--round-texture-min` 을 걸어
      areolae 가 약한 원반을 떨어뜨렸다. **문턱값 자체와, 이것이 작은 중심목을
      함께 떨어뜨리지 않는지는 실물로 확인해야 한다** — `/crops/?cls=round` 에서
      방향을 세워 훑을 수 있다
- [ ] **재현율 실측** — 2순위. 전수 검토가 끝난 3장에서 **정밀도는 23.6%** 다
      (자동 2,522 중 사람이 남긴 594). 재현율은 `탈락` 레이어를 켜고
      **"점선만 있는 것"(문턱이 놓침)** 과 **"점선조차 없는 것"(분할이 놓침)** 을
      갈라 세면 나온다 — 앞은 `refilter` 로 2초, 뒤는 분할을 고쳐야 한다
- [ ] **bbox 본체 정렬** — `tighten_bbox.py` 를 만들어 dry-run 까지 했으나
      **적용하지 않았다.** `mask_key` 를 바꾸는 작업이라 **8번과 함께** 한다.
      dry-run 숫자는 슬라이드 3장 시절 것이라 다시 재야 한다.
      함께: 크기 관문을 타원 장축으로 통일
- [ ] **분할 단계 개선** — 재현율 문제의 근본 원인. 엔진 교체로 무의미해질 수
      있으므로 투자 판단이 필요하다
- [ ] **텍스처 가드 방향 수정** — `max_period_px` 만 보는데 앨리어싱은 짧은 쪽에서
      먼저 생긴다. 현재 장비(원본 해상도)에서는 실害 없음
- [ ] **116cm 그룹핑 임계값** — 단독 시야 26개가 촬영 문제인지 임계값 문제인지
      미확인
- [ ] **`Site.name`·`region` 채우기** — `RS23`·`WAP13`·`AM22` 의 정식 명칭.
      **화면은 만들었다**(`/d/<slug>/edit/`) — 사람이 채우면 된다
- [x] **실제 ZEN XML 로 스케일 파서 확인** — 촬영 XML 로 확인
- [x] **합성 품질 지표 49개 시야 결측** — 다시 합성해 되찾았다 (devlog 010)

## 운영

- [x] **백업 lane 을 cron 에** — 시간별 `backup_db.py`(:20, `--keep 24`) ·
      일별 `sync_backup_nas.py`(04:40, `--newest-only --prune`). 신선도 게이트도
      켰다(`DIARUGA_BACKUP_MAX_AGE_H=3`) (034)
- [x] **손으로 뜬 스냅샷과 시간별 스냅샷을 갈라 두기** — `backup/manual/` 로
      갈랐다. 로테이션·NAS 대상 밖이다 (034)
- [x] **NAS 의 `manual/` 정리** — 지웠다(1.3 GB). 43개가 전부 로컬
      `backup/manual/` 에 있는 것을 확인한 뒤에. 수동은 오프사이트로 안 간다 (034)
- [x] **사진도 오프사이트로** — `photos_YYYYMMDD.tar`, 일주일 rolling.
      압축하지 않는다(JPEG 이라 5%) — 묶는 목적이 파일 하나이지 용량이 아니다 (034)
- [ ] **저장소 `backup/` 정리** — 머신 이전 때 쓴 스냅샷·DB 사본 3.4 GB.
      `/` 가 80% 다 (`/data3` 는 9%)
- [ ] **배율이 다른 슬라이드를 `state="failed"` 로 세우기** — 지금은 경고만 찍고
      40x 로 계산하고 넘어간다 (devlog 015)
- [x] **`/healthz` 가 백업·무결성 상태를 보게** — `db_sentinel.py` 가 DB 옆에
      깃발을 세우고 `/healthz` 가 `degraded`(200)로 낸다 (034)
- [ ] **표준 deploy 동사 5개** (preflight/deploy/seed/smoke/rollback) — 016
  - [x] `deploy` (019) · `smoke` (034)
  - [ ] `rollback` · `preflight` · `seed`("(none)" 선언)
- [ ] **계단식 보관의 첫 실삭제를 확인** — NAS 사본이 아직 전부 오늘 것이라
      지울 것이 없었다. 로직은 가짜 이름 366개로만 봤다(→ 24개). 7일 경계를
      넘는 **8월 11일** 에 `logs/nas-sync.log` 를 볼 것 (034)
- [ ] **뷰어 인증** — 80 으로 나오면서 노출 면이 넓어졌다.
      `DiaRUGA-subpath.conf` 에 `auth_basic` 을 걸면 된다 (018)

## 외부에 걸린 것

- [ ] **촬영 쪽 ZEN 대물렌즈 설정** — 소프트웨어에서 선택된 렌즈와 실제 교환대가
      어긋나 있다. 다음 촬영 전에 맞춰 두면 배율 사고가 다시 안 생긴다 (015)
