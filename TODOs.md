# TODOs

큰 그림은 [devlog/20260729_P01_roadmap.md](devlog/20260729_P01_roadmap.md),
DB 설계는 [devlog/20260730_P02_db-schema.md](devlog/20260730_P02_db-schema.md).
지금 상태와 인수 사항은 [HANDOFF.md](HANDOFF.md).

> **2026-08-02 현재**
> 파이프라인이 전부 DB 를 쓰고, NAS 에 슬라이드가 올라오면 1분 안에 검출까지
> 스스로 간다. 그렇게 4장이 자동으로 들어와 **슬라이드 7 · 시야 270** 이 됐다.
> **검토는 124/270 에서 멈춰 있다** — 처음 3장만 전수 검토가 끝났다.
> 뷰어는 http://172.16.116.98/DiaRUGA/ 다.

## 이름 변경(048)이 남긴 것

- [ ] **옛 자동 사본 정리 — 2026-08-06 이후에 한다** (사용자 판단, 하루 두고
      본다). 대상은 `backup/diatom_????????_??????.db` **23개 · 2.4 GB** 와
      NAS 의 3개 · 300 MB 다. 새 로테이션 glob 이 `DiaRUGA_*` 라 이것들은
      영원히 안 걷힌다.
      **하루를 기다리는 이유**: 시간별 백업이 24판을 채우면 `DiaRUGA_*` 만으로
      24시간 rolling 이 온전해진다. 그때 옛 것은 증명 가능하게 잉여다.
      지우기 전에 `DiaRUGA_*` 가 24개인지부터 센다:
      `ls /data3/DiaRUGA/backup/DiaRUGA_*.db | wc -l`
      **`manual/` 은 이 건과 무관하다** — 48개 · 1.8 GB 가 있지만 그것은
      원래부터 로테이션이 안 건드리는 자리이고(사람이 `--note` 로 뜬 것),
      이름 변경과 상관없이 늘 사람이 지우던 더미다
- [ ] **NAS 공유를 쓰는 사람들에게 알리기** — `N:\diatom` → `N:\DiaRUGA`.
      남이 매핑해 둔 경로라 우리가 옮긴 것을 저쪽은 모른다
- [ ] **옛 이미지 `honestjung/diatom:*` 정리 여부** — 지금은 롤백 경로라 둔다

## 지금 가장 급한 것

- [ ] **`AM22-GC10B 25cm` 그룹핑 확인** — 새 경고 기준(여유 0.2)에 걸린다.
      그룹 안 최소 상관이 0.594 다(정상 0.90~0.99). 초점 시리즈가 잘못 묶였을 수
      있다. **교정을 붙이기 전이 가장 싸다** — 재그룹핑은 `Viewpoint` 를 재편하므로
      그 아래 검출·교정이 통째로 어긋난다
- [ ] **새 슬라이드 4장 검토 (146 시야)** — 파이프라인이 더 갈 곳이 없다.
      자동 판정 그대로면 통과 개체 수를 슬라이드끼리 비교할 수 없다
- [ ] **`export_review.py`** (P02 5단계) — 교정 2,441건이 DB 에만 있다.
      지금 안전망은 `backup_db.py` 하나뿐

## DB 이전 (P02)

- [x] **1. 모델 + 마이그레이션 + `DATABASES`** — 14개 모델, WAL
- [x] **2. `import_json.py`** — 멱등, 8초
- [x] **3. 대조 `verify_db.py`** — 검사 37개 전부 일치
- [x] **4. 뷰어를 DB 로**
- [ ] **5. `export_review.py`** — DB → `review/*.json` (git 에 남길 감사 기록)
- [x] **6. 스크립트 4개를 DB 에 쓰게** — devlog 010·011·012
  - [x] `refilter.py` — UPDATE 한 번(124 시야 0.9초). 문턱이 `ThresholdSet` 에
        남고 실행이 `Run` 에 기록된다. 판정 규칙은 `judge.py` 로 떼어냈다
  - [x] `focus_stack.py` — 메타데이터가 `Stack` 행으로. 잃어버린 품질 지표 49개를
        되찾았다
  - [x] `segment_diatoms.py` — `is_current` 이동과 재바인딩을 한 트랜잭션으로
  - [x] `group_focus_series.py` — 기존 시야가 있으면 경고한다
- [ ] **7. JSON 을 원본에서 내리기** — 파이프라인에서는 빠졌다.
      `import_json.py` 의 중복 코드와 `README` 갱신이 남았다
- [ ] **8. 재바인딩 + 고아 화면**(`/orphans/`) — `rebind.py` 는 만들었다(011).
      **화면이 없다.** 학습·엔진 교체의 전제 — 없으면 교정이 조용히 버려진다

## 계획

- [x] **NAS 연결** — 1분마다 보고 그룹핑·합성·검출까지 자동 (P03 4·5단계)
- [x] **컨테이너화** — 뷰어는 상주, 파이프라인은 일회성 (P03)
- [ ] **데이터셋 + 학습 파이프라인** — 뷰어 교정을 정답으로 YOLO11 커스텀 학습.
      `ObjectReview.geom` 에 지운 것까지 기하가 있어 **어려운 음성 표본**으로 쓴다
- [ ] **검출 엔진 교체** — 학습 완료 후 SAM + 필터를 커스텀 모델로.
      **8번(고아 화면)이 전제다** — 엔진이 바뀌면 `exact` 바인딩이 거의 0 이 된다
- [ ] **Identification** — 논문·handbook 이미지로 종 동정 (별개 트랙, 병행).
      `ClassDef` 표에 속을 더한다(Eucampia · Chaetoceros). **다만 아직 배포가
      필요하다 — 색이 `base.html` 에 키로 박혀 있다**(038). 아래 항목 참고
- [ ] **UI · 편의성 개선** — 지속. 만든 것: 문턱 조정(`/thresholds/`),
      시료 속성 편집(`/d/<slug>/edit/`). 남은 것: 수동 박스 그리기,
      시야 병합·분할, 계측 결과 CSV 내보내기
- [ ] **분류 색을 표에서 뿜어내기** — `ClassDef.color` 칸이 있는데 아무도 안
      쓴다. 색은 `base.html` 이 키로 잡는다(여섯 자리 × 분류마다 한 줄 + 배지).
      **속을 더할 때마다 CSS 를 따라 고쳐야 한다** — 안 고치면 마스크가 투명해
      "지정은 되는데 화면에 안 보이는" 상태가 된다. `--cls` 변수를 분류마다
      뿜고 규칙은 문맥별로 하나씩 두면 30줄이 6줄 + 분류당 한 줄이 된다.
      검토 화면 렌더링을 통째로 건드리는 일이라 따로 잡는다 (038)

## 당면 과제

- [ ] **배경에 로고 워터마크** — 페이지 뒤에 규조 마크를 아주 옅게 깐다.
      표시 겸 디자인. **PNG 을 쓰지 않는다** — `docs/assets/logo/` 의 PNG 은
      281 KB 이고, 이 배포에는 정적 파일을 내보내는 길이 없어(`STATIC_ROOT` 도
      nginx 의 static 자리도 없다) data URI 로 심어야 하는데 base64 면
      **~375 KB 가 모든 페이지에 붙는다.** 같은 그림의 마크가 이미
      `_logo.html` 로 1 KB 남짓 심겨 있다 — 그것을 쓴다. 밟을 것 넷:
  - **테마가 둘이라 투명도도 둘이다.** 어두운 바탕의 옅은 마크와 밝은 바탕의
        옅은 마크는 같은 알파로 같게 안 보인다. `:root` 에 변수를 두고
        테마마다 준다. 색은 `currentColor` 라 저절로 맞는다
  - **검토 화면 위에는 깔지 않는다.** 현미경 사진과 마스크를 읽는 자리라
        뒤에 무늬가 있으면 **구조로 오독될 수 있다.** `body::before` 에
        `position:fixed; z-index:-1` 로 두면 `--panel` 이 불투명이라 카드·표가
        덮고 여백에만 남는다 — 그 자체로 경계가 선다
  - **`pointer-events: none`** — 검토 화면이 캔버스 전체에 클릭·드래그를 건다
  - **워드마크는 넣지 않는다.** 원본의 글자는 Montserrat 인데 서버에 없고
        사내망이라 웹폰트도 못 받는다(`_logo.html` 머리말). 마크만 쓴다
- [ ] **원형(중심목) 판정 기준 검증** — 1순위. `--round-texture-min` 을 걸어
      areolae 가 약한 원반을 떨어뜨렸다. **문턱값 자체와, 이것이 작은 중심목을
      함께 떨어뜨리지 않는지는 실물로 확인해야 한다** — `/crops/?cls=round` 에서
      방향을 세워 훑을 수 있다
- [ ] **재현율 실측** — 2순위. 전수 검토가 끝난 3장에서 **정밀도는 23.6%** 다
      (자동 2,522 중 사람이 남긴 594). 재현율은 `탈락` 레이어를 켜고
      **"점선만 있는 것"(문턱이 놓침)** 과 **"점선조차 없는 것"(분할이 놓침)** 을
      갈라 세면 나온다 — 앞은 `refilter` 로 2초, 뒤는 분할을 고쳐야 한다
- [ ] **bbox 본체 정렬** — `tighten_bbox.py` 를 만들어 dry-run 까지 했으나
      **적용하지 않았다.** `mask_key` 를 바꾸는 작업이라 **8번과 함께** 한다.
      dry-run 숫자는 슬라이드 3장 시절 것이라 다시 재야 한다.
      함께: 크기 관문을 타원 장축으로 통일
- [ ] **분할 단계 개선** — 재현율 문제의 근본 원인. 엔진 교체로 무의미해질 수
      있으므로 투자 판단이 필요하다
- [ ] **텍스처 가드 방향 수정** — `max_period_px` 만 보는데 앨리어싱은 짧은 쪽에서
      먼저 생긴다. 현재 장비(원본 해상도)에서는 실害 없음
- [ ] **116cm 그룹핑 임계값** — 단독 시야 26개가 촬영 문제인지 임계값 문제인지
      미확인
- [ ] **`Site.name`·`region` 채우기** — `RS23`·`WAP13`·`AM22` 의 정식 명칭.
      **화면은 만들었다**(`/d/<slug>/edit/`) — 사람이 채우면 된다
- [ ] **노두 시료의 깊이 칸** — 시추코어가 아닌 노두(outcrop) 시료는 깊이가
      없다. 지금 `Slide.depth_cm` 은 `FloatField` 라 비워 두는 수밖에 없고,
      화면에는 `—`·"깊이 미상" 으로 나와 **값을 안 채운 것과 구별이 안 된다.**
      깊이 칸에 "노두 시료" 라고 적을 수 있어야 한다.
      **`depth_cm` 을 문자열로 바꾸지 말 것** — 슬라이드 정렬이 이 값으로 서고
      (`data.py` 두 곳 · `Slide.Meta.ordering`) `(core, depth_cm)` 인덱스가
      걸려 있다. 시료 종류를 따로 두고(코어/노두) 화면이 그것을 읽어 쓰는 쪽이
      맞다. 고칠 자리는 모델·`/d/<slug>/edit/`·표시 다섯 곳
      (`index.html` 표·카드, `dataset.html`, `_map_legend.html`)
- [ ] **오검출 삭제에 스페이스바** — 040 에서 분류에는 키를 줬는데 **가장 많이
      하는 조작인 삭제만 우클릭 메뉴로 남았다**(교정 6,753 중 삭제가 5,467 이다).
      고른 개체에 스페이스 = 오검출 삭제, 이미 지운 것에 다시 누르면 되살림 —
      분류 키가 도는 것과 같은 모양이다. 밟을 곳 넷:
  - **`ClassDef.hotkey` 에 못 태운다.** 오검출은 분류가 아니라 `removed` 집합이고,
        `data.hotkey_groups()` 가 `.strip()` 을 걸어 **공백은 빈 값으로 사라진다.**
        `_detection.html` 의 keydown 에 제 갈래를 따로 둔다
  - **스페이스는 브라우저가 스크롤에 쓴다.** 지금 핸들러는 `HOTKEYS` 에 걸린
        키에만 `preventDefault()` 를 부른다 — 안 막으면 지우면서 화면이 뛴다
  - **`e.key` 는 `'Space'` 가 아니라 `' '` 다** (`e.code` 가 `'Space'`)
  - **버튼에 초점이 있으면 스페이스가 그 버튼을 누른다.** 지금 거르는 것은
        `INPUT`·`TEXTAREA`·`SELECT` 뿐이라 `BUTTON` 이 새 나간다
        — 저장·초기화 버튼 위에서 눌리면 곤란하다
  - 안내 줄(`keyhint`)에도 함께 낸다. **읽기 전용(`/engine/`)에서는 키도 안내도
        내지 않는다** — 027 이 그 자리에서 났다
- [ ] **카드의 두 줄을 축으로 가르기** — `index.html` 카드 판. 지금
      `cardstats2` 의 윗변 선이 `평균 장수`와 `검출` 사이를 지나 아랫줄이
      "분류별 + 규모 둘 + 진행률" 잡탕이다. `검출`·`시야당`·`검토` 를 첫째 줄로
      올려 **첫째 = 얼마나 있고 얼마나 봤나 · 둘째 = 무엇이 있었나**로 가른다.
      그러면 둘째 줄이 정확히 `ClassDef.counted` 만 남아 **분류를 더하면 저절로
      한 칸 는다** — 038~040 과 같은 방향이다. 걸릴 것 셋:
  - **`{% if d.mean_counted is not None %}` 가 아랫줄 전체를 감싸고 있다.**
        올릴 셋을 함께 올리면서 `검출 결과 없음` 이 설 자리도 첫째 줄로 옮긴다.
        안 그러면 검출 없는 슬라이드에 **선만 있는 빈 줄**이 남는다
  - **`검토` 는 검출과 무관한 값인데 지금 그 `if` 안에 있다** — 검출이 없는
        슬라이드는 검토 수가 아예 안 보인다. 올리면서 밖으로 뺀다 (덤)
  - `.stat-row` 는 `flex-wrap` 이라 여섯 칸이 좁은 카드에서 접힌다.
        고장은 아니고 접히는 자리만 달라진다. **표(`/`)는 안 건드린다**
- [ ] **목록을 코어로 묶고 접기** — 표·카드 둘 다. 지금은 **정렬만 되어 있고
      묶여 있지 않다**: `data.py:777` 이 지역→코어→깊이 순으로 내주니 같은 코어의
      깊이들이 이웃해 있을 뿐, 한 덩어리로 보이지 않는다. 코어 이름이 머리줄이
      되고 그 아래 깊이들이 접히게 한다 — 코어가 분석의 단위이고
      (깊이에 따른 변화가 목적이다) 슬라이드가 늘수록 평평한 목록은 못 읽는다.
  - **묶는 열쇠는 지역이 아니라 `Core` 다.** 지역은 머리줄에 함께 낸다.
        지금 지역↔코어가 거의 1:1(5:5)이라 어느 쪽으로 묶어도 화면은 같지만,
        **먼저 오는 것은 한 코어에서 슬라이드가 여럿이 되는 일이다** — 이미
        RS23-GC03 이 셋이다. 한 지역에 코어가 둘 되는 날은 그 다음이고,
        그때는 층이 하나 더 생길 뿐 이 묶음이 안 흔들린다
  - **접힌 머리줄이 숫자를 들고 있어야 한다** — 사진·시야·검출·검토 합.
        `datasets_total()` 을 **묶음마다 한 번씩 부르면 그대로 나온다**
        (지금도 전체 합계를 그 함수 하나로 낸다). **평균은 머리줄에 넣지 않는다**
        — 분모가 슬라이드마다 달라 다시 더할 수 없다(그 함수 머리말의 이유)
  - **표 안에서는 `<details>` 가 안 먹는다.** 코어마다 `<tbody>` 를 두고
        머리 `<tr>` 이 그것을 토글한다. 카드는 `<details>/<summary>` 로 그대로 된다
  - **전부 접힌 채로 열면 첫 화면이 빈 목록이다.** 여닫은 상태를 기억한다 —
        보기 방식이 이미 `localStorage['diaruga.dsview']` 로 그렇게 하고 있다
  - **코어가 안 붙은 슬라이드가 있다** (`data.py` 가 빈 값을 허용한다).
        "코어 미지정" 묶음이 필요하고, 위의 노두 시료 항목과 여기서 만난다
  - **구분은 ▶ 와 들여쓰기가 낸다. 색은 거드는 것이다.** 머리줄 앞에 ▶(닫힘)
        ▼(열림), 속 줄은 슬라이드 이름 칸만 살짝 들여쓴다. **▶ 는 장식이 아니다**
        — 접히는 머리줄은 "눌리는 것" 과 "지금 열렸나" 를 같이 내야 하는데
        옅은 면만으로는 둘 다 못 낸다. 들여쓰기는 테마와 싸울 일이 없는 단서고,
        표에서는 첫 칸(`.dsname`) `padding-left` 한 줄이면 된다
    - 카드 쪽 `<details>` 는 **브라우저가 그리는 삼각형을 끄고**(`::marker`)
          같은 ▶ 를 직접 그린다 — 표와 모양이 달라지면 두 화면이 남남이 된다
    - 여닫는 것은 `aria-expanded` 로 알린다. 보기 전환 단추가 이미
          `aria-pressed` 를 그렇게 쓰고 있다
  - **옅은 면을 얹는다면** 밟을 것 셋:
    - **테마가 둘이다** (기본 어둠 + `prefers-color-scheme: light`).
          색을 박으면 한쪽에서 사라진다 — `:root` 에 변수를 하나 더 두고
          **양쪽 테마에 다 준다**. 배너들이 이미 그렇게 되어 있다(`--ok-bg` 등)
    - **`--panel` 을 쓰면 안 된다.** `.dslist tbody tr:hover` 가 그 색이라
          보통 줄에 마우스만 올려도 묶음 머리줄과 같아 보인다
    - **뜻 있는 색을 빌리지 말 것** — `--accent`·`--warn`·`--danger` 는
          이미 다른 것을 말하고, 분류 색과도 부딪친다. 중립 면이어야 한다
  - 표 머리(`thead`)가 `position: sticky` 다. 묶음 머리줄까지 붙이면 둘이
        겹쳐 쌓인다 — 붙일지 말지는 화면에서 보고 정한다
  - **묶음이 열 줄을 넘으면 그 안에서 스크롤**한다. 한 코어에 슬라이드가
        100장 넘는 날이 온다 — 그때 묶음 하나가 화면을 통째로 먹으면 접어 둔
        보람이 없다. 카드는 `max-height`+`overflow-y:auto` 로 끝나지만,
        **표는 `<tbody>` 가 스크롤 상자가 못 된다.** 묶음마다 표를 따로 두면
        열 너비가 묶음마다 제각각이 되어 세로가 안 맞는다 —
        `table-layout: fixed` 로 열 너비를 못 박는 것이 전제다.
        `thead` 가 sticky 라 묶음 안에서는 머리가 안 따라온다는 것도 함께 본다.
        **그러나 그 수술은 하지 않는다** — 아래의 코어 페이지가 생기면 이유가
        통째로 사라진다. 임시 방편에 되돌리기 번거로운 값을 치르는 꼴이다.
        표에서는 **처음 열 줄만 내고 "나머지 N장 더 보기"** 로 간다 — 뼈대를
        안 건드리고, 스크롤이 겹쳐 휠이 갇히는 일도 없다. 카드만 스크롤한다
  - **한 코어에 슬라이드가 정말 많아지면 답은 접기가 아니라 코어 페이지다.**
        목록(`/`)은 코어를 한 줄씩 내고, `/core/<code>/` 가 그 코어의 슬라이드와
        깊이에 따른 변화를 맡는다. **깊이 방향으로 보는 것이 분석의 목적**이라
        (`data.py:774` 가 정렬을 그렇게 두는 이유) 그 화면은 어차피 필요하다.

        **그런데 지금 가면 손해다 — 묶기가 먼저다.** 판단의 근거를 남긴다:

        - **지금은 10장이 한 화면에 다 들어온다.** 목록의 값은 표를 통째로 놓고
              **슬라이드끼리 견주는** 데 있다 — 분류별 열이 나란히 서고 아래
              합계 줄이 있는 이유가 그것이다. 코어 다섯 줄만 내고 나머지를
              페이지 뒤로 보내면 **지금 되는 일이 클릭 다섯 번**이 된다
        - **코어 페이지가 값을 하려면 목록의 부분집합 이상이어야 한다.**
              깊이 대 개체수, 깊이 방향 조성 변화 — 그것이 있어야 "코어를 보는
              화면" 이지, 걸러 낸 슬라이드 목록은 지금 목록이 이미 하는 일이다.
              **새 기능이라 설계가 붙는다. 한나절 일이 아니다**
        - **묶기는 그 페이지가 생겨도 안 버려진다.** 걷어낼 것은 접기 하나뿐이고
              ▶·들여쓰기·머리줄 합계는 코어 페이지의 머리로 그대로 간다.
              `datasets_total()` 을 묶음마다 부르는 자리도 그대로 쓰인다
        - **넘어가는 때**: 코어 하나의 슬라이드가 한 화면을 넘거나, 깊이 대
              조성을 그려 달라는 말이 나올 때. **둘 중 먼저 오는 쪽이다.**
              그때 이 접기 항목은 걷어낸다
  - **100장이 실제로 오면 먼저 무너지는 것은 스크롤이 아니라 `data.datasets()` 다.**
        `_slide_summary()` 가 **슬라이드마다** 다섯 안팎의 쿼리를 돈다(시야 수 ·
        프레임 합 · 검출 SQL · 교정 집계 · 시야 검토). 지금 10장이라 ~50 이고,
        100장이면 ~500 이다. 목록 화면이 1.3초였던 적이 이미 있다(CLAUDE.md).
        **묶기와 같은 판에 고칠 일은 아니지만, 스크롤을 넣는 순간
        "많아도 된다" 고 말하는 셈이라 여기 함께 적어 둔다**
  - `tfoot` 의 전체 합계는 그대로 둔다
- [x] **실제 ZEN XML 로 스케일 파서 확인** — 촬영 XML 로 확인
- [x] **합성 품질 지표 49개 시야 결측** — 다시 합성해 되찾았다 (devlog 010)

## 운영

- [x] **백업 lane 을 cron 에** — 시간별 `backup_db.py`(:20, `--keep 24`) ·
      일별 `sync_backup_nas.py`(04:40, `--newest-only --prune`). 신선도 게이트도
      켰다(`DIARUGA_BACKUP_MAX_AGE_H=3`) (034)
- [x] **손으로 뜬 스냅샷과 시간별 스냅샷을 갈라 두기** — `backup/manual/` 로
      갈랐다. 로테이션·NAS 대상 밖이다 (034)
- [x] **NAS 의 `manual/` 정리** — 지웠다(1.3 GB). 43개가 전부 로컬
      `backup/manual/` 에 있는 것을 확인한 뒤에. 수동은 오프사이트로 안 간다 (034)
- [x] **사진도 오프사이트로** — `photos_YYYYMMDD.tar`, 일주일 rolling.
      압축하지 않는다(JPEG 이라 5%) — 묶는 목적이 파일 하나이지 용량이 아니다 (034)
- [ ] **저장소 `backup/` 정리** — 머신 이전 때 쓴 스냅샷·DB 사본 3.4 GB.
      `/` 가 80% 다 (`/data3` 는 9%)
- [ ] **배율이 다른 슬라이드를 `state="failed"` 로 세우기** — 지금은 경고만 찍고
      40x 로 계산하고 넘어간다 (devlog 015)
- [x] **`/healthz` 가 백업·무결성 상태를 보게** — `db_sentinel.py` 가 DB 옆에
      깃발을 세우고 `/healthz` 가 `degraded`(200)로 낸다 (034)
- [ ] **표준 deploy 동사 5개** (preflight/deploy/seed/smoke/rollback) — 016
  - [x] `deploy` (019) · `smoke` (034)
  - [ ] `rollback` · `preflight` · `seed`("(none)" 선언)
- [ ] **계단식 보관의 첫 실삭제를 확인** — NAS 사본이 아직 전부 오늘 것이라
      지울 것이 없었다. 로직은 가짜 이름 366개로만 봤다(→ 24개). 7일 경계를
      넘는 **8월 11일** 에 `logs/nas-sync.log` 를 볼 것 (034)
- [ ] **뷰어 인증** — 80 으로 나오면서 노출 면이 넓어졌다.
      `DiaRUGA-subpath.conf` 에 `auth_basic` 을 걸면 된다 (018)

## 외부에 걸린 것

- [ ] **촬영 쪽 ZEN 대물렌즈 설정** — 소프트웨어에서 선택된 렌즈와 실제 교환대가
      어긋나 있다. 다음 촬영 전에 맞춰 두면 배율 사고가 다시 안 생긴다 (015)
