# TODOs

큰 그림은 [devlog/20260729_P01_roadmap.md](devlog/20260729_P01_roadmap.md),
DB 설계는 [devlog/20260730_P02_db-schema.md](devlog/20260730_P02_db-schema.md).
지금 상태와 인수 사항은 [HANDOFF.md](HANDOFF.md).

> **2026-07-30 현재**
> 124개 시야 **전수 검토 완료** (삭제 2,044 · 되살림 133 · 분류 지정 174).
> 데이터·설정을 **SQLite 로 이전 중** — 뷰어는 DB 로만 돌고, 파이프라인 스크립트
> 4개는 아직 JSON 을 쓴다(그 뒤 `import_json.py` 를 돌려야 DB 에 들어간다).

## DB 이전 (P02, 진행 중)

- [x] **1. 모델 + 마이그레이션 + `DATABASES`** — 14개 모델, WAL
- [x] **2. `import_json.py`** — 멱등, 8초
- [x] **3. 대조 `verify_db.py`** — 검사 37개 전부 일치
- [x] **4. 뷰어를 DB 로** — JSON 판과 개체 dict 하나하나까지 같은 값
- [ ] **5. `export_review.py`** — DB → `review/*.json` (git 에 남길 감사 기록).
      당분간 DB 위주로 작업하기로 해서 미뤘다. 그동안은 `backup_db.py` 가 안전망
- [ ] **6. 스크립트 4개를 DB 에 쓰게** ← **다음 작업**.
      `refilter.py`(119줄) 부터 — 가장 작고, 문턱 이력이 바로 생기고,
      `segment_diatoms.py`(489줄) 에 필요한 조각을 먼저 검증할 수 있다
- [ ] **7. JSON 을 원본에서 내리기** — `README`·주석 갱신
- [ ] **8. 재바인딩 + 고아 화면**(`/orphans/`) — 새 run 과 기존 교정을 잇는다.
      **학습·엔진 교체 전에 반드시** (없으면 교정이 조용히 버려진다)

## 계획

- [ ] **NAS 연결 (NFS)** — 슬라이드별로 올라오는 폴더를 가져와 시야 그룹핑부터
      합성·검출까지 자동 처리. `Slide.state` 로 상태 관리는 스키마에 자리를 잡아 뒀다
- [ ] **데이터셋 + 학습 파이프라인** — 뷰어 교정을 정답으로 YOLO11 커스텀 학습.
      `ObjectReview.geom` 에 지운 것까지 기하가 있어 **어려운 음성 표본**으로 쓸 수 있다
- [ ] **검출 엔진 교체** — 학습 완료 후 SAM + 필터를 커스텀 모델로.
      **8번(고아 화면)이 전제다** — 엔진이 바뀌면 `exact` 바인딩이 거의 0 이 된다
- [ ] **Identification** — 논문·handbook 이미지로 종 동정 (별개 트랙, 병행).
      `ClassDef` 표에 속을 더하면 배포 없이 늘릴 수 있다(Eucampia 가 첫 사례)
- [ ] **컨테이너화** — 뷰어와 처리를 분리한 Docker 이미지.
      **ORM 을 고른 값**으로 처리 이미지에도 Django 가 필요해졌다
- [ ] **UI · 편의성 개선** — 지속

## 당면 과제

- [ ] **원형(중심목) 판정 기준 검증** — 1순위. `--round-texture-min`(기본 1500)을
      걸어 areolae 가 약한 원반을 떨어뜨렸다(원형 669 → 514). 형태 지표가 텍스처
      구간과 무관하게 평평해서 형태로는 가릴 수 없었기 때문이다. **문턱값 자체와,
      이것이 작은 중심목을 함께 떨어뜨리지 않는지는 실물로 확인해야 한다** —
      크롭 갤러리(`/crops/?cls=round`)에서 방향을 세워 훑을 수 있다
- [ ] **재현율 실측** — 2순위. 전수 검토가 끝나 **정밀도는 26% 로 잡혔다**
      (자동 2,522 중 사람이 남긴 627). 재현율은 뷰어의 `탈락` 레이어를 켜고
      **"점선만 있는 것"(문턱이 놓침)** 과 **"점선조차 없는 것"(분할이 놓침)** 을
      갈라 세면 나온다 — 두 숫자를 나눠야 투자 판단이 선다(앞은 `refilter` 로
      2초, 뒤는 분할을 고쳐야 한다)
- [ ] **bbox 본체 정렬** — `tighten_bbox.py` 를 만들어 dry-run 까지 했으나
      **적용하지 않았다.** 떠돌이 픽셀 하나가 bbox 를 부풀리고(통과분의 27.5%),
      그 때문에 중복 정리가 문턱 0.8 을 0.794 로 아슬아슬하게 피한다.
      `mask_key` 를 바꾸는 작업이라 **DB 이전 뒤에 8번과 함께** 하는 것이 낫다.
      함께: 크기 관문을 타원 장축으로 통일(지금 bbox 긴 변으로도 한 번 더 거른다)
- [ ] **분할 단계 개선** — 재현율 문제의 근본 원인. 엔진 교체로 무의미해질 수
      있으므로 투자 판단이 필요하다
- [ ] **텍스처 가드 방향 수정** — `max_period_px` 만 보는데 앨리어싱은 짧은 쪽에서
      먼저 생긴다. 현재 장비(원본 해상도)에서는 실害 없음
- [ ] **116cm 그룹핑 임계값** — 단독 시야 26개가 촬영 문제인지 임계값 문제인지
      미확인. NAS 연결의 전제
- [ ] **`Site.name`·`region` 채우기** — 지역 코드의 정식 명칭. 코드만으로 단정할 수
      없어 비워 두었다 (`RS23`, `WAP13`)
- [ ] **합성 품질 지표 49개 시야 결측** — `stack_report.json` 이 덮어써져 32/81 만
      남았다. 다시 합성하면 채워진다(재생성 가능하니 급하지 않다)
- [x] **실제 ZEN XML 로 스케일 파서 확인** — 촬영 XML 345개로 확인(`source='xml'`,
      0.112599 µm/px). 기존 결과 124개는 `backfill_scale_source.py` 로 출처를 채웠다
