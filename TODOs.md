# TODOs

큰 그림은 [devlog/20260729_P01_roadmap.md](devlog/20260729_P01_roadmap.md),
DB 설계는 [devlog/20260730_P02_db-schema.md](devlog/20260730_P02_db-schema.md).
지금 상태와 인수 사항은 [HANDOFF.md](HANDOFF.md).

> **2026-08-13 현재**
> 파이프라인이 전부 DB 를 쓰고, NAS 에 슬라이드가 올라오면 1분 안에 검출까지
> 스스로 간다. **슬라이드 12 · 시야 570 · 교정 7,924** 이고, 1차 검토는 전수
> 완료(508/508) · **남극 2차 검토가 309/511** 진행 중이다.
> 그 위에 **같은 개체 묶기**(P11)와 **개체 카탈로그**(105)가 얹혔다 — 개체마다
> 번호가 서고 **종명·등급·자세·코멘트를 적을 자리**가 생겼다(**넷 다 0건**).
> 시스템 설정은 넷이다.
> 판은 **`v0.12.0`** · 파이프라인 `v0.5.2`. 뷰어는 http://172.16.116.98/DiaRUGA/ 다.
> 운영은 `/srv/DiaRUGA` 하나로 완결된다(100). 검토 대상은 **`sam2-전수`** 다.

> **끝난 일은 여기 안 쌓는다** — 아래 "최근에 끝난 것" 만 남기고 지웠다.
> 그때의 판단과 근거는 `devlog/` 에 있다.

## 최근에 끝난 것 (2026-08-10)

- [x] **여러 프레임에서 잡힌 같은 개체를 하나로 묶는다** — P11 (`v0.9.0` ·
      devlog 090·091·099). **사람이 골라 묶는다** — 우클릭 → 판마다 근처 후보를
      펼쳐 하나씩 · 별이 대표 · "(없음)" 도 선택지. 원안(자동 계산 층,
      `Candidate` FK + CASCADE)은 **뒤집혔다**: 사람이 고르는 순간 재생성 불가
      자료가 되어 `ObjectReview` 규약을 따라야 한다.

      지금 **묶음 4개 · 멤버 12개**(전부 `yolo-3차`). 다음 계획: **개체 수 집계에
      묶음 반영**(프레임 중복 없이 "규조각 N개") · **학습 자료에서 대표만 뽑기**.
      목록·통계의 뜻이 바뀌므로 따로 결정한다.
- [x] **묶기 팝업에 탈락 후보를 낸다 — 고르면 되살린다** (102). 초점이 흐린 판은
      텍스처가 모자라 탈락하는데 그 프레임에서 가장 좋은 마스크가 그것일 때가
      있다. **되살리기는 서버가 행 하나만 좁게** 한다
- [x] **프레임 판을 보고 한 교정이 통째로 거절되던 것** (103 · `v0.9.3`).
      시야 11개에서 **어떤 교정도 저장된 적이 없었다** — 되살릴 방법이 없어 다시
      검토해야 한다. 저장 실패는 이제 **안 사라지는 띠**로 서버의 말을 그대로 적는다
- [x] **묶인 개체는 분류를 함께 받는다** (104 · `v0.9.4`) — 서버가 번지게 하고
      화면이 그것을 받는다. **삭제·되살림·코멘트·기하는 안 번진다**(판마다 하는 판단)
- [x] **개체 카탈로그 — 카탈로그 번호와 동정** (105 · `v0.10.0`).
      `/d/<슬러그>/catalog/` · 번호는 **저장하지 않고 파생한다** ·
      종명(`species`)은 자유 입력 + 자동완성 · `check_db` 9번이 지킨다
- [x] **카탈로그에서 유형을 바꿔도 묶음에 번지게 한다** (105 덧 · `v0.10.1`)
      — 번지게 하는 코드가 `save_review` 안에만 있었다. 함께 `geom` 스냅샷의
      키가 두 벌이던 것(`bbox` / `bbox_xywh`)을 `geom_box()` 로 모았다
- [x] **묶을 때 분류·종명이 엇갈리면 알리고 고르게 한다** (107 · `v0.10.2`).
      **기본은 `그대로`** — 덮을 판단이 있는 자리에서는 사람이 손을 대야 한다.
      P12 뒤로는 "그대로" 가 성립하지 않아 **서버가 409 로 거절한다**(108)
- [x] **개체와 판정을 가른다 — `DiatomObject`** (P12 · 108). `ObjectLink` +
      `ObjectLinkMember` + `ObjectReview` 를 둘로 일원화했다. 분류·종명이
      개체에 살아 **번지게 할 것이 없다**(104 의 전파 코드가 사라졌다).
      **순서를 바꿔 4차 전에 넣었다** — 회차를 안 넘긴다는 방침이 서면서 미룰
      이유가 없어졌고, 종명 0건인 지금이 이전기가 가장 쌌다.
      **08-11 에 배포했다** — `v0.11.0` + 파이프라인 `v0.5.2` 를 폴러를 잠근
      채 함께 올렸다. 옛 파이프라인 이미지가 교정 있는 시야를 지울 때
      `FOREIGN KEY constraint failed` 로 죽는 것을 조인 사본에서 실측했다
- [x] **파이프라인 상태를 화면이 말한다** (098) — 시스템 설정 · 파이프라인 탭:
      마지막 정찰 · 끝나지 않은 슬라이드와 단계별 진행 · 최근 실행.
      **097 이 이 화면을 만들게 했다**(폴러 3단계가 사흘 죽어 있었다).
      남은 구멍: **뷰어를 안 띄워 둔 날**에는 여전히 아무도 안 알려 준다
- [x] **저장소를 넷으로 가르고 운영을 `/srv` 하나로 세운다** (100 · `v0.9.1`) —
      `pipeline/`·`ops/` 는 `/srv` 로 가고 `migrate/`·`tools/` 는 안 간다.
      **운영 서버에 저장소가 없어도 돈다**
- [x] **`/srv/DiaRUGA/scripts/fix_bp09.py` 를 지웠다** — 063 때의 일회성
      스크립트인데 **이미 죽어 있었다**(`from viewer.models import Core`).
      이제 `/srv` 스크립트가 저장소와 완전히 같다

## 지금 가장 급한 것

- [ ] **개체 카탈로그를 채운다 — 종명 0건.** 한국(BP)은 SAM 으로, 남극은 YOLO 로
      채운다는 방침이라 **검토 대상 묶음을 갈아 가며** 쓴다(갈면 검토·크롭·계측·
      카탈로그가 함께 옮겨 간다). **등급·자세 칸은 카드에 붙었다**(2026-08-11 ·
      배포 전) — "계획" 절의 **개체 등급·자세** 항목에 남은 것이 적혀 있다.
      **진행도의 "동정" 이 이제 종명·등급·자세 셋을 요구한다**
- [ ] **남극 2차 검토 — 직접 그리기 포함 (309/511)** (2026-08-09, 092).
      마스크 그리기(P09)가 생겨 남극 슬라이드의 완료 표시를 걷고 다시 본다.
      교정·코멘트는 그대로다. 진척과 그린 개체 수는
      `dbrun.sh review_progress.py` 가 슬라이드별로 보여준다 — **그린 개체 수가
      곧 엔진이 놓친 것의 실측**이다
- [x] **106 의 접어 둔 2·3단계 — 그린 마스크를 같은 시야의 모든 판에 번지게 하기**
      (2026-08-12 · `sclee`). 서버 + 화면 + 시험 17을 **한 커밋**으로 넣었다 —
      반쪽으로 나가면 자료를 잃는 것이 브라우저 시험으로 확인됐다(화면 조각만
      끄면 셋이 실패한다). 판들이 **한 개체를 나눠 갖고**(`merge_into_object`
      — 사람이 화면에서 묶을 때와 같은 문), 복제 대상은 *이 회차에 현재 검출이
      있는 판*이라 SAM 은 저절로 지나간다. 대표를 잃은 개체를 세우는
      `_reelect_reps` 가 새로 필요했다.

      **사용감은 사용자가 봤다** (2026-08-12) — *"나쁘지 않다. 묶음이 있으면
      자동으로 사슬 표시가 뜰 테니 사용자도 충분히 인지할 수 있다."* 그래서
      저장 띠에 "판 4곳에 번졌다" 를 따로 적지 않기로 했다.

      **그 전제가 절반만 성립하던 것을 고쳤다** (`f7bb2ef`). 사슬은
      `object_links_of` 가 내는데 화면의 `links` 는 **페이지가 열릴 때 한 번**
      받고 `/link` 응답으로만 갱신됐다 — `/review` 저장이 그것을 안 실어 보내
      **새로고침 전에는 배지가 안 떴다.** 이제 **묶음이 움직인 저장에만** 싣고
      (매번 실으면 시야의 개체를 다시 물질화한다), 방금 그린 것은 잠깐
      밝힌다(`linkflash`)

      **stash 는 버렸다** (2026-08-11 — 그 코드를 쓴 쪽이 정했다). 서버
      조각(1단계, `data.py` +128줄)은 **`8ad77ba`(08-10 16:54) 위에서 떴고
      P12 가 그 다음 날 같은 파일을 337줄 고쳤다.** 충돌보다 **안 나는 부분이
      위험하다** — 그 코드는 `ObjectReview` 가 판정과 동정을 겸하던 시절의
      전제로 짰고, 지금은 판정 행이 개체 없이 설 수 없다(`data.judgement_for`
      문을 안 지나면 `IntegrityError`, 또는 대표 없는 개체). **P12 위에서 다시
      짜는 편이 128줄치고는 싸다.**

      **새로 정할 것 하나**: 복제한 마스크가 판마다 **제 개체를 갖는가, 한
      개체를 나눠 갖는가**. 그때는 `DiatomObject` 가 없어서 물을 수 없던
      질문이고, 뜻으로는 후자다 — 같은 규조각을 옮겨 그린 것이니까
- [x] **등급·코멘트를 개체로 옮기고, 코멘트는 카탈로그에서만 적는다**
      (2026-08-12 · `sclee` · `0035`·`0036` · devlog 111·112). 번지기가 실무를
      갈랐다 — 한 규조각이 판 넷에 퍼지는데 등급을 네 번 매기고 같은 말을 네 번
      적을 이유가 없다. **등급은 `is_rep`(대표)이 이미 하던 일을 겸하고 있었다.**
      코멘트는 **적는 화면도 하나로 모았다**(사용자 결정): 검토 화면의 목록·팝업·
      우클릭 항목을 걷고 **읽기만** 남겼다 — 그러면 `/review` 가 코멘트를 아예
      안 보내므로 **종명·등급·자세와 같은 갈래**가 되어 017·027·053 계열의 사고가
      구조적으로 안 생긴다. 비는 자리는 **Diadiction(P14)이 쓴다.**
      곁가지로 **묶음을 풀 때 등급·자세가 사라지던 것**(`_split_off`)을 고쳤다

- [ ] **카탈로그가 YOLO 개체의 절반을 못 본다** (106). 프레임에만 있는 개체
      **5,123** 이 카드에 아예 안 나온다 — 저장할 자리는 있고 화면이 안 보여줄
      뿐이다. "프레임도 다 내자" 로는 안 끝난다(같은 규조각이 2~10장 중복되고
      번호가 판마다 달라진다). **묶기가 채워지면 묶음 단위로 세면 된다**

      **그때 접어 둔 C안(프레임 개체 수를 화면에 적기)은 버렸다** (2026-08-11).
      접은 이유가 *"같은 개체 판정에 근거가 없다"* 였는데 **P12 가 그 근거를
      만들었다** — 이제 `DiatomObject` 로 셀 수 있으니 옛 시안을 되살릴 것이
      아니라 그 위에서 새로 짠다. 다만 **아직 묶은 것에만 걸린다**: 사람이
      안 묶은 통과분은 프레임마다 따로 세어진다
- [x] **검토 완료가 남는 개체에 확인 표시를 단다** (109 · `v0.11.1`).
      `ObjectReview.auto_confirmed` — 완료 한 번이 그 시야 **모든 판**의 남는
      개체(통과분 + 되살린 탈락분)에 퍼진다. **마스크마다 누른 것이 아니라서
      이름이 `auto_` 다** — 학습에서 손그림과 같은 무게로 쓰면 안 된다.
      자동 분류(`Candidate.cls`)는 `label` 로 안 옮긴다
- [ ] **`AM22-GC10B 25cm` 그룹핑 확인 — 검토는 끝났고, 기준 재측정만 남았다.**
      08-04 저녁에 `regroup.apply_split` 이 네 번 돌아 시야가 **26 → 30** 이
      됐다. **검토는 30/30 완료다** (2026-08-09 검토자 확인). 경고 기준
      (여유 0.2 · 최소 상관 0.594)이 지금도 걸리는지는 다시 재야 한다

## DB 이전 (P02)

- [ ] **8. 재바인딩 + 고아 화면**(`/orphans/`) — `migrate/rebind.py` 는 만들었다(011).
      **화면이 없다**(`urls.py` 에 `/orphans/` 가 없다). 학습·엔진 교체의 전제 —
      없으면 교정이 조용히 버려진다. 지금 교정 **7,726건이 전부 `exact`** 라
      이 길이 한 번도 시험대에 오른 적이 없다 (`ops/check_db.py` 3번이 센다).
      **보류다 — YOLO 4차를 돌린 뒤에 만든다** (2026-08-12 사용자). 지금 고아가
      거의 0 이라 **되살려서 잡히는 것을 볼 수가 없다.** 3차 검토 → 학습 자료 →
      4차 → **그때 고아가 얼마나 나오고 무엇에 쓸모가 있는지 보고 정한다.**
      계획은 [P13](devlog/20260812_P13_orphans.md) 에 그대로 있다 — 고아의 정의,
      목록에서 사람이 그린 것을 빼야 하는 이유, 좁은 문, 시험 자료까지.
      **108 이 만든 거짓 `orphan` 둘도 이 화면이 없어서 더 안 보였다**

(1~7 은 끝났다 — devlog 010·011·012 · P06.)

## 층 (063) 에서 남은 것

- [ ] **육상 지역을 폴더에서 뽑는 규칙이 짐작이다.** `BP09` → `BP` 로 숫자를
      떼는데, 노두 코드가 `<지역문자><번호>` 꼴이 아니면 **없는 지역이 만들어진다.**
      비워 두는 쪽이 더 나빠서(어느 권역 탭에도 안 나온다) 이렇게 두었고 관리
      화면이 고칠 수 있게 했다. 들어올 때 사람에게 물어보는 편이 나은지 볼 것
- [ ] **지점 코드를 바꾸면 노두 사진이 떨어진다.** 파일 이름(`<지점코드> (n).jpg`)이
      지점과 사진을 잇는 유일한 근거라 그렇다 — 간단함과 맞바꾼 대가다.
      코드를 바꾸는 일이 드물어 지금은 그대로 둔다
- [ ] **`migrate/import_json.py` 를 새 층으로 고쳐 두었지만 여전히 돌리면 안 된다** —
      JSON 이 DB 보다 한참 낡았다. 이전기 도구로만 남아 있다

## 계획

- [ ] **뷰어에서 도감을 부른다 (Diadiction)** — 2026-08-12, 사용자 계획 ·
      [P14](devlog/20260812_P14_diadiction-lookup.md). 검토·카탈로그에서 속을
      짚으면 **그 자리에서 도감의 등록 정보와 도판을 본다.**
      자료는 저장소 밖 NAS 에 이미 있다 (`N:\DiaRUGA\Diadiction` =
      `/nfs/temp-share/DiaRUGA/Diadiction`, 2.7 GB) — 한국동식물도감 512종 ·
      Schmidt Atlas 학명 1,504개 · 동남극 도판집 43종. 붙는 자리는
      **`ClassDef.is_taxon` 이 참인 분류** 하나뿐이다.
      **P14 4.3(동남극 도판집 색인)은 닫혔다**([117](devlog/20260813_117_east-antarctic-plate-index.md),
      2026-08-13) — 이제 도감 셋이 다 md 색인을 갖는다. 우리 자료와 제일 가까운
      그 도감에는 **그림마다 시료의 구간·깊이(cm)** 까지 붙어 있다.
      **형식은 셋이 같지 않아 파서는 도감마다 하나여야 한다.**
      **읽는 방식은 [P15](devlog/20260813_P15_atlas-db.md) 로 넘어갔다** — 아래 참조
- [ ] **도감을 DB 에 넣고 뷰어에서 본다 (P15)** — 2026-08-13, admin 방침 ·
      [P15](devlog/20260813_P15_atlas-db.md). 색인 셋의 **표제어 2,059개**(이명법
      1,854 · 속 149)를 `Atlas`·`AtlasEntry` 로 넣는다. **P14 가 고른 갈래 (c)를
      뒤집는 것이고, 그 대신 4.4 의 막힌 자리를 푼다** — 자료가 DB 로 들어오면
      뷰어가 NAS 를 안 보므로 **글자 자료는 마운트를 안 기다려도 된다**(도판
      크롭만 남는다). 붙는 자리가 둘인데 **지금 큰 쪽은 `DiatomObject.species`**
      다 — 자유 입력이고 아직 0건이라 규칙을 세우기에 지금이 가장 싸다.
      **원본은 md 이고 DB 는 사본이다**(언제든 지우고 다시 만든다) — 그래서
      **사람이 만든 학명 판정은 이 표에 안 담는다**(063 계열의 사고를 막는다).
      철자는 **흐린 검색으로 후보를 내고 AlgaeBase 가 가른다**(사용자 방침).
      속 층은 9쌍인데 **그중 둘은 실제로 다른 속이라** 자동으로 이으면 안 된다.

      **08-13 저녁까지 온 자리** — 반입 전에 자료를 믿을 수 있는지부터 물었고,
      **못 믿을 자리가 나와서 아직 반입을 안 했다**:
      - `tools/harvest_worms.py` 로 **이명법 1,845개 전량 조회**. 규조로 찾은 것
        1,640 · 못 찾은 것 205 · 화석 63. 목록은 NAS `Diadiction/temp/`
        (`species_1845.md`·`.tsv`·`worms_20260813.json`)
      - **AlgaeBase 41건은 사람이 브라우저로 끝냈다**(Windows 앱). 결과는
        `Diadiction/md/name_validity_log.md` 에 합쳐 뒀다 — **16쌍이 합치면 안 되는
        별개 이름**이고, 속 셋(*Cosmiodiscus*·*Pyrgodiscus*·*Porodiscus*)은 전부
        실재하는 규조속이었다
      - **Schmidt 색인의 속명이 잘못 펴진 자리를 찾았다**
        ([119](devlog/20260813_119_schmidt-genus-restoration-bug.md)). PDF 로 확인한
        것 둘 — Tafel 26 의 `Amphiprora` 13건은 *Amphora*, Tafel 57 의
        `Cosmiodiscus` 7건은 *Coscinodiscus*. **`(속명 추정)` 표시가 없는
        것들이라** 폴더 문서의 "1,899건은 확정" 이 틀렸다. 색인은 아직 안 고치고
        **경고만 달아 뒀다**(색인 머리말·README·notes/02·SKILL.md·`.skill` 재빌드)
      - **속이 실재하는가와 그 항목이 그 속인가는 다른 물음이다.** *Cosmiodiscus*
        는 유효한 화석속이지만 우리 색인의 8종 중 7종은 그 속이 아니다
        (Tafel 229 의 *C. elegans* 하나만 진짜)

      **다음** — ① 후보 33개 목록을 **다시 뽑는다**(`extant_only` 없이 만든 것이라
      화석 55개가 빠졌다) ② 남은 Tafel 을 열어 속명을 확인한다 ③ 한국 도감 41개는
      `outputs/png/` 에 렌더가 이미 있어 더 싸다 ④ *Craspedodiscus nitidus/nitidulus*
      는 양쪽 DB 에 다 없어 **Schmidt 원문 재판독**이 필요하다 ⑤ 1차 퍼지 매칭에서
      **저자명 조각 21건·독일어 낱말 43건**이 종소명 자리에 있는 것이 확인됐다 —
      색인에서 지울 항목들이다.
      **08-14 에 ①③⑤ 를 했다** ([121](devlog/20260814_121_name-triage-and-index-marks.md)).
      자료는 `Diadiction/temp/` 에서 **`names/` 로 옮겼다** — 그 자리는 "언제든
      비워도 됨" 이라 적힌 곳인데 다시 못 만드는 것이 살고 있었다.

      - **표 다섯을 한 표로 모았다** — `names/worms/worms_master_20260814.tsv`
        4,064줄. 도감 출처를 `harvest_worms` JSON 의 `atlases` 와 다시 이었다
        (표 다섯에는 그 칸이 없었고, 색인에 없는 이름이 2,219개 섞여 있었다)
      - **색인 표제어 2,059개 전부에 대조 결과를 붙였다**(`annotate_index.py`).
        지우지 않고 붙이기만 했고 `--strip` 이 원본으로 되돌린다
      - **AlgaeBase 300종이 들어왔다** — 앞으로도 300종씩. `ingest_algaebase.py`
        가 몇 벌이 쌓여도 다시 얹는다. **AlgaeBase↔WoRMS 엇갈림 17건**
      - **후보 33개를 다시 뽑아 246개가 됐다**(①). **Tafel 58·29 를 새로 확인**해
        속명 고장이 넷이 됐다(26·57·58·29)
      - **`index_remove` 196건이 진짜 학명 7건을 지울 뻔했다** — "종소명이 짧다"·
        "저자명을 닮았다" 가 근거가 못 된다. `sol`·`ovum`·`major` 는 정당하고
        `-ii` 로 끝나는 `schmidtii` 는 Schmidt 를 기린 이름이다
      - **덤으로 Tafel 번호 114건을 고쳤다** — 해설 OCR 이 머리 숫자를 잘못 읽어
        여러 쪽으로 번진 것이다. **쪽은 맞았으니 도판 여는 것은 그대로다**

      **⑥ 을 했다** ([124](devlog/20260814_124_algaebase-round-trip.md)) —
      급한 148건을 추려 브라우저 되는 세션에 넘겼고 전부 채워져 왔다(누적
      **448/1,845**). 학명이 아닌 것은 원문으로 확인해 미리 뺐다. **길이 규칙을
      걷은 것이 여기서 값을 했다** — `Coscinodiscus boliv`·`Navicula transit`·
      `Ditylium sol` 이 전부 진짜 이름이었다. **"AlgaeBase 에 없음" 68건을
      "가짜 이름" 으로 읽으면 안 된다** — 화석속은 두 DB 다 종 수준이 성글고,
      `Rouxia leventerae` 는 **두 DB 에 다 없는데 실제로 쓰는 이름**이다.

      **08-18 에 301~600 이 들어왔다** ([127](devlog/20260818_127_algaebase-day7-12.md))
      — 50종씩 여섯 벌. 누적 **706/1,845** 이고 **1~600 은 빈 자리가 없다.**
      먹이는 자리에서 셋을 고쳤는데 셋 다 **조용히 틀리는** 종류였다: 이름 칸의
      괄호(`Chaetoceros affine (Chaetoceras affine)`)가 열쇠를 어긋내 5줄이 안
      붙을 뻔했고(번호 검산이 잡았다), `**고친 이름**(그대로 유효)` 꼴이 오면서
      **고친 이름을 버리고 틀린 철자를 "그대로 유효" 로 적고 있었다.** 148건과
      41개가 겹치는데 **무엇이 이길지가 파일 이름 정렬에 달려 있어서**(`day10`
      이 `day7` 보다 앞이다) 알아낸 것이 많은 쪽이 이기게 바꿨다 — 진 기록은
      비고에 남긴다. **그 규칙에 맡기면 안 되는 것 둘은 원문 재판독으로 넘겼다**
      (사용자). ⑦ 로 넘긴 것은 이 둘을 합쳐 **11건**이고 같은 날
      [126](devlog/20260818_126_schmidt-render-verify.md) 이 **여섯을 닫았다** —
      #423 `Chaetoceros ikari` 는 한국 도감 색인이 `Chaetoceras Ikari SKVORTZOW`
      로 저자를 달고 있어 `C. ikarii` 로 닫혔고, `Cocconeis` 셋은 원문에 `A. S.`
      가 찍혀 있어 Schmidt 판으로 닫혔다. **#431 `Chaetoceros paradoxum` 만
      남았는데 물음이 좁아졌다** — 색인이 `PAVILLARD` 라 148건의 `C. paradoxus`
      (Cleve 판)도 9일차가 든 동명이종(Cleve·Peragallo)도 아니다. **Pavillard 판
      이름으로 AlgaeBase 에 다시 물어야 한다 — 다음 꾸러미에 끼운다.**
      닫은 것은 `RESOLVED`, 안 닫힌 것은 `HOLD` 에 근거와 함께 두어서
      **다시 돌려도 규칙이 도로 채우지 않는다**

      **08-19 에 601~900 이 들어왔다** ([134](devlog/20260819_134_algaebase-day13-18.md))
      — 50종씩 여섯 벌. 누적 **972/1,845** 이고 **1~900 은 빈 자리가 없다.**
      먹이는 자리에서 하나 고쳤다: **표가 발음부호를 벗겨 적어 온다**
      (#637 `Cyclotella kutzingiana`, 색인 표제어는 `kützingiana`). 열쇠가
      안 맞아 그 줄이 통째로 안 붙는 자리인데 번호 검산이 잡아 세웠다 —
      **번호가 가리키는 줄과 발음부호만 다를 때에 한해** 색인 표기로 되돌리고
      적어 온 철자는 비고에 남긴다. **발음부호 든 표제어가 일곱 더 남아 있다**
      (#1069 부터). 148건과 **열일곱이 갈렸고** 넷은 새 표가 이겼다
      (`Eupodiscus catifornicus` → `californicus` 등, 148건은 "없다" 였다).
      **#431 `Chaetoceros paradoxum` 은 두 꾸러미째 안 왔다** — `HOLD` 에 그대로
      있어 규칙이 도로 채우지 않는다. **`확인 필요` 가 22 → 55 로 늘었다** —
      변종만 걸리는 자리를 그대로 넘기고 있다. 반입은 안 막지만 쌓이면 한 번에
      다시 물어야 한다. **AlgaeBase↔WoRMS 엇갈림 50건**(08-18 에는 28건)

      **08-23 에 901~1200 이 들어왔다** ([146](devlog/20260823_146_algaebase-day19-24.md))
      — 누적 **1,254/1,845** 이고 **1~1200 은 빈 자리가 없다.** 이 구간에서
      `검색 안 함` 다섯이 전부 같은 꼴이었다 — **속명은 멀쩡한데 종소명 자리에
      독일어 동사가 앉아 있다**(`vermochte`·`eingereiht`·`gerechnet`·
      `erinnere`·`bildet`). Schmidt Atlas 가 독일어 도감이라 본문 낱말이
      색인으로 흘러든 것이고, **글자를 틀리게 읽은 OCR 오독과 갈래가 다르다**
      — 교정해도 이름이 안 나온다. 지우지 않고 대조표가 말하게 뒀다(121).
      **`확인 필요` 가 55 → 69 로 늘었는데 는 열넷 중 열둘이 동명이종이다** —
      저자마다 목적지가 갈리는데 도감이 저자를 안 적은 자리라 **AlgaeBase 에
      다시 물어도 안 풀린다.** 발음부호는 이번에 안 걸렸다(#1069 을 `é` 그대로
      적어 왔다 — 여섯 남았다). **AlgaeBase↔WoRMS 엇갈림 83건**

      **잔여는 1,845 를 끝낸 뒤 한 벌로 낸다** (사용자 2026-08-23). 순번
      꾸러미에 예외를 안 끼운다 — #431 을 두 꾸러미째 끼웠다가 두 번 다 안 왔다.
      그때 낼 것은 `AlgaeBase 에 없다` 93 · `아직 안 찾았다` 20 · `안 적혀 있다`
      9 · `미확인` 4 · **#431**(Pavillard 판)이고, **`확인 필요` 는 안 넣는다**
      (묻는 대상이 등록부가 아니다). **순번이 지나간 것은 끝까지 가도 저절로
      안 걸린다** — 그래서 이 줄이 있다

      **⑦ 를 닫았다** ([126](devlog/20260818_126_schmidt-render-verify.md),
      2026-08-18 · `tools/render_verify.py`). 해설 OCR 로 갈리지 않던 자리를
      **쪽 20개를 렌더해 29건** 확인했다. 근거는 항목마다 원문 구절까지
      `names/worms/render_verify_20260818.md` 에 있고, 대조표의 `원문확인` 칸과
      색인 표시(`원문 렌더 확인: …`)에 실렸다. **표제어는 하나도 안 고쳤다** —
      표시를 다는 도구는 `annotate_index.py` 하나여야 한다(124).

      - **해설 OCR 의 Tafel 번호가 묶음째로 어긋나 있다** — Band4 p.74–86 일곱
        쪽이 전부 `Tafel 377` 이다(실제 371–377). `read_notes` 가 이어 붙이니
        **다른 쪽 본문이 한 Tafel 아래로 들어가** 판정 둘이 틀려 있었다
        (`Cymbella amphi` 는 `amphioxys` 가 아니라 줄바꿈에 잘린
        `amphi- / cephala` 다). **열쇠는 번호가 아니라 쪽이다** — 053·103·119 와
        같은 줄이고, 이 경고를 색인 머리말에 적었다
      - **`Triceratium venustun` 은 원문이 그렇게 찍혀 있다** — 색인은 안 틀렸다.
        바른 이름(`venustum`)은 표시가 말한다. **원문 오식은 고쳐 쓰지 않는다**
      - **Tafel 149 가 119 계열 속명 고장의 다섯 번째 쪽이다** —
        `Actinoptychus ellipticus` 는 *Auliscus* 다(fig 4). `genus_screen` 이
        이미 후보로 냈던 것을 원문이 확정했고, 같은 쪽 후보 둘은 기각됐다.
        **`확정` 으로 통과해 있던 항목이라 손봐야 할 목록만 따라갔으면 못 봤다**

      **다음** — ⑥' AlgaeBase 나머지 **591종**(1201번부터. 순번대로 오면 된다)
      ⑧ Tafel 번호에서 **손대지 않은 묶음 셋**(미간행 421–432 을 건너거나 권
      끝이라 셈이 안 선다) — **126 이 그 셈의 전제를 흔든다. 쪽으로 다시 셀 것**
      ⑨ **119 의 속명 후보 30개**(33개 중 셋이 닫혔다). 판별식이 두 번 맞았으니
      쪽을 열어 확정하는 순서로 간다 — ⑤·⑦·⑨ 는 AlgaeBase 로는 원리적으로 안
      풀린다. 이름이 아닌 것을 물어봐야 "없다" 만 나온다

      **1단계를 만들었다** ([128](devlog/20260818_128_atlas-parsers.md),
      2026-08-18 · `tools/parse_atlas.py`). 색인 셋을 `atlas/*.json` 으로 뽑는다 —
      **표제어 2,059 · 자리 2,480 · 속 149** 가 다 들어왔고 시험이 NAS 없이 돈다
      (CI 에도 붙였다). **색인을 더 안 해도 반입은 시작할 수 있다** — ⑥'·⑧ 은
      반입을 안 막고, ⑨ 는 기다릴 것이 아니라 **P15 8.2 의 "안 맞는 속" 검사로
      센다**. 이름·표시를 다루는 규칙은 새로 안 짜고 `harvest_worms.binomial` 과
      `annotate_index.MARK` 를 가져다 썼다 — 표시를 아무 `〔…〕` 로 걷었으면
      **색인의 Verzeichnis 주석 21건이 조용히 사라졌을 것이다**.
      **색인·계획의 수 셋이 지금 본문과 안 맞는 것**도 검산이 잡았다(128 5절):
      출현 기록 1,968→1,911 · `(속명 추정)` 69→46 · 이명법 1,854→1,845.
      **2단계까지 섰다** ([130](devlog/20260818_130_atlas-schema-and-import.md),
      2026-08-18). `0038` 로 표 셋(`Atlas`·`AtlasEntry`·**`AtlasPlacement`**)을
      세우고 `ops/import_atlas.py` 가 **2,059 항목 · 2,480 자리**를 넣는다(0.46초).
      **P15 4.1 을 한 자리 고쳤다** — 자리를 `AtlasEntry` 의 칸으로 두려 했는데
      Schmidt 254건이 자리를 여럿 갖는다(최다 11). 반입은 도감마다 통째로
      갈아치우고 **JSON↔DB 를 검산해 어긋나면 되돌린다**(표제어는 한 글자까지
      본다 — 인용이 원문과 어긋나는 자리다). `check_db` **11번**이 붙었다:
      `ClassDef`(is_taxon)의 속과 `DiatomObject.species` 가 도감에 있는가.
      **`AtlasEntry` 에 FK 를 매달지 말 것** — 반입이 도는 날 CASCADE 로 사라진다.
      **남은 것은 `TaxonName`(P15 8.1 방침 대기)과 화면(129)이다.**

      **속명 오류를 안 고치고 반입하면 `ClassDef` 와 맞추는 자리에서 조용히 빠진다**
      **막힌 자리가 하나 있다 — 뷰어(`web`)가 이 공유를 못 본다**(확인함,
      P14 4.4). `/srv/DiaRUGA/docker-compose.yml` 에 마운트 한 줄을 더하고 `web`
      만 다시 띄우면 되고 이미지는 안 구워도 되는데, **`/srv` 는 `paleoadmin`
      의 자리다.** 그때까지 갈래 (a)~(c)가 전부 성립하지 않는다.
      **도구 자체는 이 자리에서 이미 쓸 수 있다**([113](devlog/20260812_113_diadiction-tooling.md))
      — 종 하나를 4단계로 끝까지 돌려 확인했다. 다만 **AlgaeBase 는 브라우저가
      있어야 한다**(`curl` 은 302·0바이트) · `WebFetch` 는 사내망이 막는다.
      함정은 **도감이 1968년 이름을 쓴다는 것** — 안 맞는 속이 나오면 "도감에
      없음" 이 아니라 이름이 다른 것이고, 그것을 화면이 말해야 한다
- [ ] **도감 화면을 영어로 — 캠벨도 쓸 수 있게** (2026-08-19, 사용자 방침).
      지금 도감은 화면 문구가 전부 한국어라 밖에서 온 사람이 못 쓴다.

      **범위는 템플릿 넷이다** — `atlas.html`(51개 문구로 제일 많다. 검색 화면
      이라 안내·상태 문구가 몰려 있다) · `atlas_page.html`(7) ·
      `atlas_spread.html`(9) · `atlas_volume.html`(9), 그리고 `base.html` 의
      머리줄 링크 "도감". **`atlas.py` 는 안 건드려도 된다** — 거기 한국어는
      전부 주석·머리말이고 화면으로 나가는 문구가 없다(실측).

      **넷에서 다섯이 됐다** ([141](devlog/20260823_141_atlas-view-controls.md),
      2026-08-23). 도판 조작을 `_atlasview.html` 한 조각으로 모으면서 **조작
      문구 열셋이 거기 모였고**(넘긴다·격자·한 장·두 쪽·맞춤·쪽으로·간다 …),
      `atlas_page`·`atlas_spread` 는 그만큼 줄었다. `atlas.html` 은 셋 늘었다
      (속 거르개를 빼는 문·미리보기의 "아직 안 떴습니다"). **한 조각에 모인
      쪽이 옮기기는 쉽다** — 두 화면이 같은 문구를 나눠 쓴다.

      **정할 것 셋:**

      ① **바꾸는가, 둘 다 두는가.** "바꾼다" 면 한국어 사용자가 잃는다.
      Django 의 `{% trans %}` + `LocaleMiddleware` 로 두 벌을 두면 잃는 것이
      없지만 자리가 늘고, 이 저장소에 i18n 이 아직 하나도 없다.

      ② **도감만 영어면 화면이 섞인다.** 머리줄·워터마크·판 표시·오류 띠는
      `base.html` 것이라 한국어로 남는다. 도감만 쓰는 사람에게 그 정도가
      괜찮은지, 아니면 공통 뼈대까지 가야 하는지.

      ③ **자료는 한국어로 남는다.** 도감 이름(`한국동식물도감`)과 한국 도감의
      해설·생태·분포는 **원문이라 번역 대상이 아니다** — 반입이 원문 오식도
      안 고치기로 한 것과 같은 자리다(P15). 화면 껍데기만 영어가 된다.

      **이것은 CLAUDE.md 의 "화면 문구도 한국어" 규칙에서 일부러 빠지는
      자리다** — 정하고 나면 그 규칙에 예외를 한 줄 적는다.

- [x] **도감을 읽을 수 있게 만든다 — 도판 조작·검색 거르개**
      ([141](devlog/20260823_141_atlas-view-controls.md), 2026-08-23).
      129·131 이 **띄웠고** 이번에 **읽게** 했다. 도판이 창 하나에 들어가고,
      `←` `→` `g` `s` 로 넘기고, 휠·`+` `-` `0` 으로 확대하고, 끌어서 옮기고,
      쪽 번호로 곧장 간다(`PDF p.N` — Tafel 이 아니다). 검색 쪽은 도감 칩이
      **속을 안 떨어뜨리게** 하고, 속만 따로 뺄 수 있게 하고, `해설 p.`·
      `도판 p.` 칩에 **미리보기**를 붙였다(디스크를 안 짚는다).
      **남은 것 둘** — ① 모바일 두 손가락 확대는 안 붙였다(`touch-action` 만
      걸어 뒀다) ② 확대한 자리를 쪽 넘어서도 지키는 것은 `sessionStorage` 라
      창을 닫으면 없어진다. 오래 지켜야 한다는 말이 나오면 그때 옮긴다

- [ ] **P17 코어 자료에서 남은 셋** (2026-08-19 · `v0.15.0` 배포 뒤).

      ① **XRF 의 단위를 모른다.** `coredata/mapping.toml` 에 빈칸으로 뒀다 —
      모르는 것을 적지 않았다. 원본을 낸 쪽에 물어 한 줄 채우면 된다.

      ② **`RS14-GC04` 의 자기감수율이 한 점에 축을 다 먹힌다**
      ([135](devlog/20260819_135_core-profile-chart.md) 8절). 대부분 수십인데
      340 cm 부근에서 599 까지 튄다. **손대지 않았다** — 축을 자르거나 로그로
      바꾸면 그림은 나아지지만 자료가 아닌 것을 보여주게 된다. 구간을 잘라
      보는 기능이 필요한지가 정할 일이다.

      ③ **종 단위 파생 계열이 없다.** 지금 `Chaetoceros` 는 `ClassDef` 의 속
      분류로 세어진다. `DiatomObject.species` 가 **아직 0건**이라 종 단위로는
      셀 것이 없다 — 카탈로그가 채워지는 만큼 저절로 열린다.

- [ ] **데이터셋 + 학습 파이프라인** — 뷰어 교정을 정답으로 YOLO11 커스텀 학습.
      `ObjectReview.geom` 에 지운 것까지 기하가 있어 **어려운 음성 표본**으로 쓴다.
      **무엇을 먼저 학습시킬지 고르는 근거는 아래 "개체 등급·자세" 가 만든다**
- [x] **개체 등급·자세 — 우수한 개체를 골라 우선 학습한다**
      (2026-08-11, 사용자 계획 · [110](devlog/20260812_110_grade-pose.md)) —
      **`v0.11.2` 로 나갔고, `v0.12.0` 에서 등급이 개체로 옮겨졌다**(`0035` · 111).
      배포는 2026-08-13 ([114](devlog/20260813_114_deploy-and-pr-flow.md)).

      **남은 것은 사람이 매기는 일이다** — 등급·자세 모두 **0건**이라
      `check_db` 10번이 아직 절 전체를 건너뛴다(매긴 개체가 생겨야 검사가
      시작된다). 학습 쪽 연결(`ops/export_yolo.py` 의 거르개)은 값이 쌓인 뒤다.

      > **한 것** (2026-08-11 · 마이그레이션 `0034`)
      > — `ObjectReview.grade`·`DiatomObject.pose` · `/review` 청소가 두 칸만 있는
      > 행을 안 지운다 · 파편에는 서버(`data.check_grade_pose`)가 안 받는다 ·
      > 카탈로그 카드에 두 칸(파편이면 안 뜨고, 유형을 파편으로 바꾸면 묻는다) ·
      > 파편 기본 감춤 + 체크박스 · 진행도에서 파편을 빼고 **완료 = 종명·등급·자세**.
      > 시험 3겹 12 · 4겹 13.
      >
      > **감사 기록에 실었다** (2026-08-12) — `ops/export_review.py` 가 두 칸을
      > 낸다. **매긴 것에만 실어 형식 번호를 안 올렸다**(`species` 와 같은 규칙 ·
      > 값이 없는 파일은 한 글자도 안 바뀐다). 둘 다 **판정 한 줄**에 실린다 —
      > 자세는 개체에 살지만 **묶음이 아닌 개체는 `links` 에 안 나와서**, 묶음
      > 머리에만 실으면 대부분의 개체에서 조용히 사라진다. 옛 판(0034 이전 백업)은
      > 두 테이블을 따로 살펴 빈 값을 끼운다. 시험 7.
      >
      > **불변식을 넣었다** (2026-08-12) — `ops/check_db.py` **10번**. 파편에
      > 등급·자세가 안 붙어 있는가 · 지운 판이 등급을 들고 있지 않은가 · 판이
      > 모두 오검출인 개체가 자세를 들고 있지 않은가. **세 번째 자리다**(화면 ·
      > 서버 다음) — 옆문(마이그레이션·손질한 SQL)은 예외를 안 낸다. 시험 9.
      >
      > **자세가 묶음 안에서 어긋나는 검사는 안 넣었다** — P12 뒤로 자세가
      > `DiatomObject` 한 행에 살아 **그 상태가 성립하지 않는다.** 늘 0 이면
      > 덮은 줄 알게 된다. 묶을 때 엇갈리면 거절하는 쪽(108)이 그 자리를 맡는다
      >
      > **등급은 하루 만에 자리를 옮겼다** (`0035` · 111) — *"어느 판이 잘
      > 보이는가"* 는 `is_rep`(대표)이 이미 하고 있었다. 아래 표의 *"등급은
      > 판에 대한 판단"* 은 **그때의 판단이고 지금은 아니다.** 0건이라 옮기는
      > 값도 없었다.
      >
      > **배포했다** (2026-08-13 · 114). 뷰어만 올렸다 — `0035`·`0036` 이 칼럼을
      > 걷지만 사본에 `v0.5.2` 파이프라인을 붙여 안 닿는 것을 실측했다.
      > **진행도가 0% 로 보인다** — 완료의 뜻이 셋으로 바뀌었고 값이 0건이라서다

      개체 카탈로그(105) 카드에 **등급**과 **자세** 두 칸을 더하고, 우수한 것을
      **우선 학습 대상**으로 삼는다. 화면도 카탈로그에 붙인다.

      **둘 다 완형에만 매긴다.** 파편(`round_frag`·`rod_frag`)은 완형을 유추할
      수 있어도 확실하지 않고, 무엇보다 **한 개체로 인정하는 규칙을 만족하지
      못한 것**이라 우수성을 물을 자리가 아니다(사용자). 즉 `ClassDef.counted=1`
      인 것만이고, 지금 살아 있는 교정 1,794 중 **576건**이다(파편 1,168 ·
      분류 없음 50).

      **그런데 두 칸의 축이 반대다** (사용자 결정). 이것이 나머지를 다 정한다.

      | | 무엇에 대한 판단인가 | 번지나 | P12 에서 |
      |---|---|---|---|
      | **등급** | **판(초점면)** — 같은 규조각도 초점면마다 areolae 가 보이고 안 보인다 | **안 번진다** | `MaskJudgement` 에 **남는다** |
      | **자세** | **개체** — 스테이지가 안 움직이니 초점을 옮겨도 누운 자세는 그대로다 | **번진다** | `Object` 로 **옮겨야 한다** |

      - **등급은 `ObjectReview` 에 칸을 더하는 것이 구조적으로 맞다** —
        `removed`·`accepted` 와 같은 자리(판에 대한 판정)다. P12 가 와도 그대로
        따라가므로 **이전 비용이 안 는다**
      - **자세는 `label`·`species` 와 같은 자리다** — 104 의 `_spread_link_labels`
        에 얹어 묶음을 따라 번지게 하고, **107 의 충돌 팝업에도 넣는다**(묶을 때
        자세가 엇갈리면 물어야 한다). P12 이전 때 **옮길 것이 하나 는다**
      - **대표(별)와 안 겹친다** — 대표는 묶인 것에만 있고 묶음이 4개뿐이다.
        오히려 등급이 쌓이면 나중에 대표를 자동 제안하는 근거가 된다
      - **자세가 등급을 설명한다** — 대면관으로 누우면 판면의 동정키가 안 보여
        B·C 가 된다. 둘을 같이 적어 두면 나중에 *"C 인 것들이 자세 때문인가"*
        를 물을 수 있다

      **등급은 셋이다** (2026-08-11, 사용자).

      | | 뜻 |
      |---|---|
      | **A** | 동정키도 잘 보이고 **완형이 잘 드러난다** |
      | **B** | 완형이지만 초점 등의 문제로 형태가 잘 안 드러난다, **또는** 상태가 나쁘지만 동정키는 남아 있다 |
      | **C** | 완형도 동정키도 잘 안 드러난다 |

      **자세도 셋이다** — `valve`(valve view) · `girdle`(girdle view) ·
      `other`(other position).

      **규칙 둘 — 하나는 매기는 사람의 기준이고, 하나는 시스템이 막는다.**

      - **A 는 종명까지 동정된 것을 뜻한다.** 다만 **시스템에 묶지 않는다**
        (2026-08-11 사용자). 등급을 먼저 매기고 종명은 나중에 적는 것이 실제
        순서라, 저장 때 거절하면 그 순서를 막는다. `check_db` 불변식으로도 안
        센다 — **매기는 사람이 지키는 기준**으로 적어만 둔다
      - **파편에는 등급도 자세도 주지 않는다.** 두 칸이 도는 자리는 `counted=1`
        인 분류(`round`·`rod`·`eucampia`·`chaetoceros`)뿐이고, 파편으로 지정된
        개체의 카드에는 **두 칸이 아예 안 뜬다.** 분류를 파편으로 바꾸면 이미
        매긴 값을 어떻게 할지도 정해야 한다 — **조용히 지우지 말고 물어보는
        쪽**이다(사람이 매긴 것이라 재생성 불가다). **서버가 다시 검사한다** —
        화면에서 막는 것은 막는 것이 아니다(063)

      둘 다 값이 셋뿐이라 **짧은 키를 `choices` 로 가두면 된다**(등급은 순서가
      있고 자세는 없다). `ClassDef` 처럼 테이블로 둘 이유가 없다 — 분류를 더할
      때 여덟 칸 + CSS 를 따라 고쳐야 하는 그 비용을 안 치른다(038). 안 매긴
      상태가 있어야 하므로 빈 값을 허용한다. **색·배지를 붙이는 순간 CSS 축에
      걸린다**(아래).

      **함정 — 105 가 종명에서 이미 당한 자리다.**

      - **`/review` 가 지운다.** 검토 화면은 등급도 자세도 모르는데 POST 는 그
        (이미지, 묶음)의 교정 중 payload 에 없는 행을 지운다. 105 에서 종명만
        채운 행이 **아무것도 안 하고 "검토 완료" 만 눌러도** 사라졌다.
        `data.save_review` 의 보존 목록에 두 칸이 든 행의 키를 **갈래 없이 늘**
        얹고, 이 저장소 방식대로 **시험으로 먼저 재현해 놓고** 막는다
      - **빈 껍데기 청소와의 경계** — 등급·자세만 있고 나머지가 빈 행이
        지워지면 안 된다. 105 가 같은 자리에서 실패 2개를 봤다
      - **`ops/export_review.py` 에 싣는다** — 사람이 눈으로 매긴 것이라 재생성
        불가다. 105 처럼 **값이 있는 행에만 키를 실으면 형식 번호를 안 올려도
        된다**(없는 파일은 한 글자도 안 바뀐다)
      - **응답이 방금 친 값을 덮는 사고** — 카드의 편집 칸이 **다섯**이 된다
        (종명·유형·코멘트 + 등급·자세). 105 에서 코멘트가 그렇게 날아갔고
        **브라우저 겹으로만 잡힌다**
      - 색·배지를 쓰면 `base.html` 의 CSS 축에 걸린다 (038)
      - **`check_db.py` 불변식** (2026-08-12 · 10번) — 파편이나 지운 마스크에
        등급·자세가 붙어 있지 않은가. **`A` 인데 종명이 빈 것은 안 센다** —
        위의 규칙대로 기준이지 제약이 아니다(수만 적어 준다).
        **묶음 안에서 자세가 어긋나는 것은 셀 수 없다** — 자세가 개체 한 행에
        살아 그 상태가 아예 성립하지 않는다. 늘 0 인 검사는 덮은 줄 알게 한다.
        그리고 **이 검사는 `0034` 이전 DB 에서 못 돈다** — Django 가 모델의 모든
        칼럼을 SELECT 해서 3번(교정)이 먼저 죽는다. `check_db.py` 전체가 뷰어
        판과 함께 가는 축이다(057)

      **학습 쪽 연결.** `ops/export_yolo.py` 가 지금 검토 완료 시야를 통째로
      쓴다 — 등급·자세 거르개가 붙을 자리가 거기고, P11 이 남긴 "학습 자료에서
      대표만 뽑기" 와 한 묶음이다. **다만 "우선" 이 "만" 이 되지 않게 단계를
      적어 둘 것** — 우수한 것만 학습시키면 쉬운 것만 배우는데 지금 정밀도
      문제(23.6%)는 애매한 것에서 난다. 지운 4,039건을 어려운 음성 표본으로
      쓴 것과 짝이다.

      **적어 둘 것 둘.**

      - **`A → 종명` 은 축을 가로지르는 규칙이다.** 등급은 판의 성질이고
        종명은 개체의 성질이라, **P12 가 오면 두 표에 나뉘어 앉는다** —
        지금은 한 행 안이라 검사가 쉽지만 그때는 조인이 된다. 규칙 자체는
        그대로 남는다
      - **B 가 두 뜻을 겸한다** — 앞은 판의 문제(초점), 뒤는 개체의 보존
        상태다. 실무에서 "A 는 아니지만 쓸 수는 있다" 로 한 칸이면 충분해
        그렇게 두지만, **나중에 "이 개체가 나쁜 것" 과 "이 판이 흐린 것" 을
        갈라야 하면 B 로는 못 가른다**

      **정할 것: `A → 종명` 을 어떻게 물릴 것인가.** 저장을 막으면 *현미경으로
      A 라고 판단해 두고 종명은 문헌을 찾아 나중에 적는* 흐름과 부딪힌다.
      **저장은 받되 드러내는 쪽**을 권한다 — 카드에 "종명이 비어 있습니다" 를
      남기고, 카탈로그 거르개에 **`A인데 종명 없음`** 칩을 두고(105 의
      `동정함`·`동정 안 함` 옆), `check_db.py` 가 그 수를 센다. 화면에서
      막는 것은 막는 것이 아니고(063), 반대로 못 하게 막으면 사람이 등급을
      안 매기게 된다

      **P12 를 앞당기는 것이 아니다.** P12 의 순서 1번(*"카탈로그가 자리를
      잡는다 — 동정이 실제로 쌓이고 쓰임이 드러난다"*)을 채우는 쪽이다.
      종명은 아직 **0건**이라 그 조건이 비어 있었다
- [ ] **검출 엔진 교체** — 학습 완료 후 SAM + 필터를 커스텀 모델로.
      **8번(고아 화면)이 전제다** — 엔진이 바뀌면 `exact` 바인딩이 거의 0 이 된다.
      교정에 이미지 판별자를 다는 일은 끝났다(P06 · 055)
- [ ] **Identification** — 논문·handbook 이미지로 종 동정 (별개 트랙, 병행).
      **종명 칸은 105 가 만들었다**(자유 입력 + 자동완성). 목록으로 굳으면 그때
      테이블로 옮긴다. `ClassDef` 에 속을 더할 때는 **아직 배포가 필요하다** —
      색이 `base.html` 에 키로 박혀 있다(038, 아래 항목)
- [ ] **데스크탑 앱** (P05) — GPU 서버 없는 연구자에게 배포하기 위한 갈래.
      라이선스(AGPL-3.0)·엔진(YOLO)·스택(PySide6)은 정해졌다. **1단계는 읽기
      전용 뷰어**이고, 거기서 PyInstaller + Django 가 얼려지는지를 먼저 본다.
      근거는 `docs/20260805_desktop-app-review.md`, 실측은 049
- [ ] **프레임별 검출을 검토한다 — 화면만 남았다** (사용자 방침 2026-08-05)

      **스키마는 끝났다** (P06 5a · devlog 055). 교정의 열쇠가
      `(image, batch, mask_key)` 이고, `save_review` 는 지우는 범위를 이미
      **이미지로** 좁혀 뒀다. 결정도 나 있다 — **검토 완료는 시야 단위 유지**,
      **교정은 현재 검출에만**.

      남은 것:

      - **화면.** 프레임별 검출이 `is_current` 가 되면 시야 하나에 현재 검출이
        여럿(프레임마다 하나)이 되는데, 검토 화면이 그것을 어떻게 내는가
      - **집계가 부푼다.** 같은 규조각을 프레임 5장에서 교정하면 목록의 "검토"
        수도 5배다. 세는 곳을 이미지로 갈라야 한다

      **사람의 품이 몇 배가 된다 — "같은 개체 묶기" 와 짝이다.**
- [ ] **회차를 넘어 같은 개체를 묶는다 — `YOLO 4차` 가 들어올 때** (2026-08-10,
      사용자 판단: *"YOLO 4차가 들어가야 의미있는 내용이니까 아직 서두를 필요는
      없어"*).

      지금 `ObjectLink` 는 **시야도 회차도 못 넘는다** — 멤버가
      `(image, batch, mask_key)` 로 마스크를 짚고, 묶음 자체도 `batch` 하나에
      매여 있다. 그래서 같은 규조각이 `sam2-전수` 와 `yolo-3차` 에서 각각
      잡혀 있어도 **그것이 하나라는 것을 담을 자리가 없다.**

      그것이 개체 카탈로그(105)에서 그대로 드러난다: 카탈로그 번호와 동정이
      **회차마다 별개**다. 회차가 셋이 되면 같은 규조각에 번호가 셋 생기고
      동정도 셋 적게 된다.

      **스키마는 4차가 실제로 들어온 뒤에 박는다** — 시야 안 묶기가 그랬듯이
      (원안이 뒤집혔다) 자료를 보고 나서 정할 것이 있다: 회차끼리 무엇을 근거로
      맺는가(IoU 문턱은 실측), 그리고 **사람이 고칠 수 있게 할 것인가**(고치게
      하면 재생성 불가 자료가 된다). 그 층을 어떻게 그을지가
      [P12](devlog/20260810_P12_object-and-judgement.md) 다
- [ ] **UI · 편의성 개선** — 지속. 만든 것: 문턱 조정(`/thresholds/`),
      시료 속성 편집(`/d/<slug>/edit/`), 검토 화면 안의 엔진 비교(051),
      개체 카탈로그(105). 남은 것: 시야 병합, 계측 결과 CSV 내보내기
- [ ] **분류 색을 테이블에서 뿜어내기** — `ClassDef.color` 칸이 있는데 아무도 안
      쓴다. 색은 `base.html` 이 키로 잡는다(여섯 자리 × 분류마다 한 줄 + 배지).
      **속을 더할 때마다 CSS 를 따라 고쳐야 한다** — 안 고치면 마스크가 투명해
      "지정은 되는데 화면에 안 보이는" 상태가 된다. `--cls` 변수를 분류마다
      뿜고 규칙은 문맥별로 하나씩 두면 30줄이 6줄 + 분류당 한 줄이 된다.
      검토 화면 렌더링을 통째로 건드리는 일이라 따로 잡는다 (038)

## 당면 과제

- [ ] **이미 검출한 마스크를 다른 판에도 번지게 한다** (2026-08-14, 사용자 요청) —
      111 이 **사람이 그린** 마스크를 같은 시야의 모든 판에 앉히고 한 개체로
      묶는다(`data._spread_drawn`). 같은 것을 **엔진이 잡아 놓은 개체**에도 하고
      싶다는 것이다. 되는 근거는 111 과 같다 — **같은 시야는 스테이지가 안
      움직여 좌표가 그대로 맞는다.**

      **사람이 개체를 골라 누를 때만 번진다** (사용자 방침 2026-08-14). 저장할
      때 자동으로 번지게 하지 않는다: 그린 마스크에는 *이건 실재한다* 는 사람의
      판단이 이미 붙어 있지만, **엔진이 옆 판에서 안 잡은 것은 초점이 안 맞아
      놓친 것일 수도 있고 거기 없는 것일 수도 있다.** 자동으로 얹으면 엔진의
      놓침과 사람의 판단이 한 행에 섞인다.

      들여다볼 것 넷:

      - **번진 행은 무엇으로 앉나.** `Candidate` 는 검출기의 산출물이라 사람이
        만들 수 없다. 그린 마스크와 같은 자리(`ObjectReview` + `geom`)에 앉는데,
        `source` 를 `manual` 로 둘지 새 값을 둘지가 정해져 있지 않다 —
        **학습 자료를 뽑을 때 이 구분이 곧 "사람이 그린 것" 의 정의가 된다**
      - **묶음이 저절로 생긴다.** 번지면 판들이 한 개체를 나눠 갖는데(`is_rep` ·
        분류 · 종명 · 등급 · 코멘트가 한 벌), **P11 은 묶는 것을 사람의 판단으로
        두기로 한 자리**다. 111 은 *그린 것은 옮겨 그린 것이라 뜻이 그렇다* 로
        풀었는데, 엔진 검출에도 같은 말이 서는지 봐야 한다
      - **검출이 없는 판에는 넣으면 안 된다** (`_spread_drawn` 이 이미 그렇다).
        그 판의 화면은 마스크를 안 그리고 저장도 못 받아 **손댈 수 없는 유령**이
        된다
      - **화면이 번진 것을 받아야 한다.** `/review` 는 payload 에 없는 행을
        지우므로, 응답에 안 실으면 옆 판으로 넘어가 저장하는 순간 사라진다
        (111 의 "반쪽으로 넣으면 잃는다")

      **106 이 여기에 걸린다** — 프레임에만 있는 개체 5,123은 카탈로그에 아예
      안 나온다. 그것을 합성본으로 번지게 하면 카드에 나오지만, **번진 것과
      원래 거기서 잡힌 것을 화면이 갈라 보여야** 한다

- [x] **검토 화면에서 깊이 맵을 안 보이게 한다** (123 · `v0.13.1`). `_shots.html`
      의 한 덩이만 걷었고 자료는 그대로 뒀다. **116 의 회귀 시험이 깊이 맵을
      밟고 있어** 그 케이스를 걷었는데, 같은 갈래(`curKey=null`)를 밟는 *그
      묶음에 검출이 없는 프레임* 시험이 같은 파일에 있어 가드는 남아 있다
- [ ] **미리보기 정지 화면 치우기** — `/srv/paleolab/DiaRUGA-preview/` 와
      `N:\DiaRUGA\preview\`. **nginx 조각은 결국 설치하지 않았다** — 걷어낼
      조각이 없다. 날짜별로 쌓이는 자리라 급하지 않다. 치울 때는 디렉토리째
      지운다 (049·052)

      > **085 가 눌러 볼 수 있는 자리를 만들었다** (`/DiaRUGATest/`). 정지 화면이
      > 하던 일의 절반은 이쪽이 더 잘한다 — 다만 정지 화면은 **VPN 밖에서도
      > 보이고 남겨 두면 그때 모습이 남는다.** 둘의 쓰임이 다르니 함께 둔다
- [ ] **테스트 인스턴스에 표시가 없다** (085) — 두 화면이 똑같이 생겨 어느 쪽인지
      알 수 없고, `localStorage` 를 운영과 공유한다(오리진이 같다). 지금
      `/DiaRUGATest/` 는 운영과 같은 판(`v0.10.2`)으로 떠 있다
- [ ] **암상(lithology) 모델** — 지점 페이지(`/loc/<지역>/<지점>/`)에 **띠만
      비워 두었다**(049). 구간 상·하한 · 암상 코드 · 색 · 조직 · 화석 함량 같은
      칸이 정해져야 표가 생긴다. **기재 규칙이 정해지기 전에는 스키마를 박지
      않는다** — 지금 박으면 나중에 이미 붙은 자료를 옮겨야 한다
- [ ] **지점 페이지에 깊이 대 조성** — 지금은 깊이 축·시료 위치·현황·속성뿐이다.
      "깊이 대 개체수", "깊이 방향 조성 변화" 가 붙어야 이 화면이 제 몫을 한다.
      **처리 방법을 달리한 관찰을 합치면 안 된다**(비교하려고 만든 것이다) —
      지금은 사람이 `집계 제외` 를 켜서 고른다 (049)
- [ ] **원형(중심목) 판정 기준 검증** — 1순위. `--round-texture-min` 을 걸어
      areolae 가 약한 원반을 떨어뜨렸다. **문턱값 자체와, 이것이 작은 중심목을
      함께 떨어뜨리지 않는지는 실물로 확인해야 한다** — `/crops/?cls=round` 에서
      방향을 세워 훑을 수 있다
- [ ] **재현율 실측** — 2순위. **전수 검토가 끝나 이제 잴 수 있다.** 3장 시절
      **정밀도는 23.6%** 였다(자동 2,522 중 사람이 남긴 594). 재현율은 `탈락`
      레이어를 켜고 **"점선만 있는 것"(문턱이 놓침)** 과 **"점선조차 없는 것"
      (분할이 놓침)** 을 갈라 세면 나온다 — 앞은 `refilter` 로 2초, 뒤는 분할을
      고쳐야 한다
- [ ] **bbox 본체 정렬** — `migrate/tighten_bbox.py` 를 만들어 dry-run 까지 했으나
      **적용하지 않았다.** `mask_key` 를 바꾸는 작업이라 **8번과 함께** 한다.
      dry-run 숫자는 슬라이드 3장 시절 것이라 다시 재야 한다.
      함께: 크기 관문을 타원 장축으로 통일
- [ ] **분할 단계 개선** — 재현율 문제의 근본 원인. 엔진 교체로 무의미해질 수
      있으므로 투자 판단이 필요하다
- [ ] **텍스처 가드 방향 수정** — `max_period_px` 만 보는데 앨리어싱은 짧은 쪽에서
      먼저 생긴다. 현재 장비(원본 해상도)에서는 실害 없음
- [ ] **116cm 그룹핑 임계값** — 단독 시야 26개가 촬영 문제인지 임계값 문제인지
      미확인
- [ ] **`Site.name`·`region` 채우기 — 5개 중 3개가 찼다.** 남은 둘의 정식 명칭.
      **화면이 둘이다** — `/d/<slug>/edit/` 의 지역 탭, 그리고 시스템 설정 · 자료

## 운영

- [ ] **옛 `/var/lib/docker` 를 치운다 (약 72 GB)** — 도커 자리는 `/data3/docker`
      로 옮겼는데(081) 옛 자리가 그대로 남아 있다.

      ```bash
      # ① 데몬이 정말 새 자리에서 도는지 먼저 본다 — 이것부터 확인하지 않으면
      #    지우는 것이 곧 이미지·컨테이너를 통째로 지우는 일이 된다
      docker info --format '{{.DockerRootDir}}'     # /data3/docker 여야 한다

      # ② 이름만 바꾼다. **바로 지우지 않는다** — 되돌릴 길이
      #    `daemon.json` 의 data-root 한 줄과 이름 되돌리기로 끝나야 한다
      sudo mv /var/lib/docker /var/lib/docker.old

      # ③ 네 프로젝트가 다 성한지 본다 (며칠 두고 본다)
      docker ps          # diaruga · phyloserver · scoda-server · refserver
      cd /srv/DiaRUGA && bin/smoke.sh
      docker compose run --rm pipeline python -c "import torch; print(torch.cuda.is_available())"

      # ④ 그 뒤에 지운다
      sudo rm -rf /var/lib/docker.old
      ```

      **급하지 않다** — `/` 가 81%(43 GB 남음)이고 도커는 더 이상 그쪽을 안
      키운다. 서두르는 것보다 되돌릴 수 있는 상태로 며칠 두는 편이 싸다.
      ②를 건너뛰고 바로 지우면 **daemon.json 을 되돌려도 돌아갈 자리가 없다**
- [ ] **배율이 다른 슬라이드를 `state="failed"` 로 세우기** — 지금은 경고만 찍고
      40x 로 계산하고 넘어간다 (devlog 015)
- [ ] **표준 deploy 동사 5개** (preflight/deploy/seed/smoke/rollback) — 016.
      만든 것은 `deploy`(019)·`smoke`(034)이고 **`rollback`·`preflight`·
      `seed`("(none)" 선언)가 없다.** `DEPLOY.md` 도 없다
- [ ] **CI 가 이미지를 Docker Hub 로 올리는지 다음 판에서 확인한다.**
      시크릿 둘(`DOCKERHUB_USERNAME`·`DOCKERHUB_TOKEN`)이 **2026-08-10 저녁에
      저장소 설정에 들어왔다**(사용자). 그전까지는 굽기 레이어가
      `Username and password required` 로 실패해 **v0.8.0~v0.10.2 를 전부 이
      머신에서 손으로 올렸다** — "시험을 통과한 것만 이미지가 된다" 는 관문이
      실제로는 사람 손에 걸려 있었다.

      **다음 태그를 밀 때 볼 것**: 굽기 레이어가 초록인가 · `docker pull
      honestjung/diaruga:<판>` 이 되는가(굽기가 성공해도 push 가 빠지면 모른다).
      되면 손으로 올리는 단계를 배포 절차에서 뺀다
- [ ] **`smoke.sh` 가 슬라이드마다 화면을 하나만 연다** — 시야 목록뿐이라
      `/crops/`·`/detections/`·`/catalog/`·`/g/<n>/` 이 죽어도 통과한다.
      **086 이 그 구멍으로 나왔다**(두 화면이 v0.8.0 이후 내내 500, 배포 게이트를
      다섯 번 통과). 한 슬라이드에서 화면 여섯을 다 열면 몇 초 더 걸릴 뿐이다
- [ ] **교정 수가 줄어드는 것을 아무도 안 알려 준다** — 백업 로그에서만 보인다
      (027 을 알아챈 것이 그것이다). `ops/check_db.py` 에 "직전 백업 대비 교정
      수" 를 넣는 것을 검토한다
- [ ] **계단식 보관의 첫 실삭제를 확인** — NAS 사본이 아직 전부 최근 것이라 지울
      것이 없었다. 로직은 가짜 이름 366개로만 봤다(→ 24개). 7일 경계를 넘는
      **8월 11일** 에 `logs/nas-sync.log` 를 볼 것 (034)
- [ ] **뷰어 인증** — 80 으로 나오면서 노출 면이 넓어졌다.
      `DiaRUGA-subpath.conf` 에 `auth_basic` 을 걸면 된다 (018)
- [ ] **`warm_thumbs.sh` 가 도감을 안 덮는다** (131). 그 스크립트는
      `/d/<slug>/` 를 훑어 화면이 부르는 축소본 주소를 되부르는데, 도감
      화면(`/atlas/…`)은 그 경로에 없다. **v0.14.0 배포 뒤 5,344자리를 손으로
      구웠다**(9분 · 220·300·1100·1600). 도감이 늘거나 `render_atlas_pages.py`
      를 다시 돌리면 또 손으로 굽게 된다 — 도감 주소도 태우는 것이 맞다.
      격자가 한 판에 60장을 부르므로 찬 상태로 열면 몇 초가 걸린다
- [ ] **`/healthz` 가 "쓸 수 있는가" 를 안 본다** (132). 08-18 에 운영 DB 가
      5시간 46분 동안 읽기 전용이었는데 `/healthz` 는 내내 `ok` 였다 — 읽기만
      하는 검사라서다. `writable` 칸을 하나 두거나 `smoke.sh` 가 임시 테이블을
      만들었다 지우는 길이 있는데, **`deploy.sh` 의 기동 게이트가 `/healthz` 를
      딛고 있어**(034 — `degraded` 를 503 으로 바꾸면 배포가 스스로 멈춘다)
      손댈 때 그쪽을 함께 봐야 한다
- [ ] **옛 자료의 프레임 축소본이 아직 차다** (125). 새 자료는
      `poll_nas.sh` 가 `warm_thumbs.sh` 로 굽지만, 옛 슬라이드의 필름스트립
      (w=220)·판 교체용(w=1600)은 안 구워져 있다 — 처음 여는 사람이 판마다
      100~146ms 를 기다린다. 필요하면 `bin/warm_thumbs.sh <slug>` 를 손으로
      부른다(슬라이드 하나 25초쯤, 멱등이다). 합성본은 08-18 에 342자리를 구웠다
- [ ] **무거운 시야는 HTML 이 1 MB 다** (125). 915 KB 중 591 KB 가 `shot_dets`
      이고 그 절반 넘는 **320 KB 가 탈락 후보**다 — 판 9개마다 통과 438 + 탈락
      480개를 지표 21칸과 폴리곤까지 통째로 싣는다. 서버 렌더는 20ms 라 문제가
      아니고, **사내망 너머에서만 드러난다.** 탈락분을 펼칠 때 따로 청하는 것을
      검토한다
- [ ] **그린 개체의 지표를 열 때마다 다시 잰다** (125). `_orphan_dict` →
      `shape.measure`(`_clip_convex`)가 판마다·개체마다·요청마다 돈다. 전체
      4% 지만 교정이 쌓인 시야에서 42ms 다(최대 페이지 84 → 122ms).
      **106 번지기가 그린 마스크를 판 전부에 퍼뜨리므로 판 수만큼 곱해진다** —
      검토를 할수록 그 시야가 느려진다. 기하를 고칠 때만 다시 재고 캐시하는 것을
      검토하되, `geom_edited` 와 어긋나면 낡은 값이 남는다는 것이 원래 이유였다
- [ ] **운영 DB 의 빈 페이지가 27.9%(30 MB)다** (125, 108 MB 중). 질의 시간은
      0 에 가까워 급하지 않다. 큰 작업 전 사본을 뜬 김에 `VACUUM` 을 검토한다

## 외부에 걸린 것

- [ ] **촬영 쪽 ZEN 대물렌즈 설정** — 소프트웨어에서 선택된 렌즈와 실제 교환대가
      어긋나 있다. 다음 촬영 전에 맞춰 두면 배율 사고가 다시 안 생긴다 (015)
- [ ] **`AM22` 가 아문센해인지 Amery(동남극)인지** — 코어 대장에서 확인
- [ ] **`Site.lat/lon`** — 좌표가 들어오면 지도가 저절로 정확해진다
