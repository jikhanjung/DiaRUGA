# TODOs

세부 내용은 [devlog/20260729_P01_roadmap.md](devlog/20260729_P01_roadmap.md).

> **2026-07-30 현재** 124개 시야 **전수 검토 완료** (삭제 2,027 · 되살림 133 ·
> 분류 지정 123). 자세한 것은 [devlog 005](devlog/20260730_005_review-tooling-and-full-first-pass.md).

## 계획

- [ ] **NAS 연결 (NFS)** — 슬라이드별로 올라오는 폴더를 가져와 시야 그룹핑부터
      합성·검출까지 자동 처리
- [ ] **데이터셋 + 학습 파이프라인** — 뷰어 교정을 정답으로 YOLO11 커스텀 학습.
      지속적 재학습이 가능하게
- [ ] **검출 엔진 교체** — 학습 완료 후 SAM + 필터를 커스텀 모델로
- [ ] **Identification** — 논문·handbook 이미지로 종 동정 (별개 트랙, 병행)
- [ ] **컨테이너화** — 뷰어와 처리를 분리한 Docker 이미지
- [ ] **UI · 편의성 개선** — 지속

## 당면 과제

- [ ] **원형(중심목) 판정 기준 검증** — 1순위. `--round-texture-min`(기본 1500)을
      걸어 areolae 가 약한 원반을 떨어뜨렸다(원형 669 → 514). 형태 지표가 텍스처
      구간과 무관하게 평평해서 형태로는 가릴 수 없었기 때문이다. **문턱값 자체와,
      이것이 작은 중심목을 함께 떨어뜨리지 않는지는 실물로 확인해야 한다**
- [ ] **재현율 실측** — 50~60% 는 육안 추정치. 학습 데이터셋 구축의 전제.
      **전수 검토가 끝나 정밀도는 26% 로 잡혔다**(자동 2,522 중 사람이 남긴 653).
      재현율은 뷰어의 `탈락` 레이어로 "점선만 있는 것"(문턱이 놓침)과 "점선조차
      없는 것"(분할이 놓침)을 세면 나온다
- [ ] **분할 단계 개선** — 재현율 문제의 근본 원인
- [x] **실제 ZEN XML 로 스케일 파서 확인** — 촬영 XML 345개로 확인했다
      (`source='xml'`, 0.112599 µm/px). 기존 검출 결과 124개는 zen_meta 이전에
      나와 출처가 없었으므로 `backfill_scale_source.py` 로 채웠다(원본 43 `xml`,
      합성본 81 `sidecar` + 없던 사이드카 생성). 남은 것은 실제 검출 한 번을
      돌려 파이프라인 전체에서 찍히는지 보는 것뿐이다
- [ ] **텍스처 가드 방향 수정** — 현재 장비에서는 실害 없음
- [ ] **116cm 그룹핑 임계값** — 촬영 문제인지 임계값 문제인지 미확인.
      슬라이드가 계속 들어오면 운영 문제가 되므로 NAS 연결의 전제
