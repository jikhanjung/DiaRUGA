# 089 — 시스템 설정을 최상위 메뉴로 (`/system-settings/`, 톱니)

2026-08-08 · 사용자 요청

## 무엇이 문제였나

관리 화면은 **데이터셋 목록의 하위처럼 놓여 있었다.**

- 들어가는 길이 목록 화면 제목 옆의 `관리 →` **하나뿐**이었다
- 화면 머리줄이 `데이터셋 / 관리 · 운영` 이라 **목록에 속한 것**으로 읽혔다
- 그래서 다른 화면(시야 목록·갤러리·계측 표·시야 화면)에서는 **길이 아예 없었다**

그런데 그 화면이 하는 일은 목록의 하위가 아니다 — 층을 만들고 옮기고 지우고,
**뷰어 전체가 무엇을 검토할지**를 정한다. `/manage/` 는 소속을 잃은 관찰을 잡아
주는 **유일한** 화면이기도 하다(063). "길이 없으면 없는 화면이다" 를 적어 놓고
길을 한 자리에만 뒀다.

사용자 방침: **데이터셋 목록과 같은 층의 최상위 메뉴**로 본다.

## 한 일 넷

### 1. 이름 — 관리 → **시스템 설정**

화면 제목·머리줄·다른 화면이 부르는 이름을 전부 바꿨다. 머리줄에서 `데이터셋 /`
breadcrumb 을 뗐다 — 같은 층이므로 앞에 붙을 것이 없다.

```
데이터셋 / 관리 · 운영      →      시스템 설정 · 운영
```

### 2. 주소 — `/manage/` → `/system-settings/`

**옛 주소는 302 로 살린다.** 사내에 퍼진 링크와 브라우저 기록이 조용히 깨지면
안 된다 — `core/` → `loc/` 와 같은 갈래이고, 301 이 아닌 이유도 같다(브라우저가
캐시하면 나중에 규칙을 다시 손볼 때 되돌릴 방법이 없다).

**쿼리스트링을 그대로 넘긴다.** 저장 뒤 `?msg=…` 로 돌아오는 길이 있어서, 옛
주소를 북마크한 사람이 그 메시지를 잃으면 "저장이 됐나" 를 묻게 된다.

**`settings` 라는 이름은 안 쓴다.** `django.conf.settings` 와 부딪혀서 뷰
함수를 그 이름으로 둘 수 없고, 그러면 주소·URL 이름과 함수 이름이 갈린다 —
반쪽 개명은 다음 사람에게 "어느 쪽이 참인가" 를 묻게 한다. `system_settings` 는
아무것도 안 가려서 **끝까지 같은 이름**으로 갔다:

| 자리 | 이름 |
|---|---|
| 주소 | `/system-settings/` · `/ops/` · `/dataset/` |
| URL 이름 | `system_settings` · `system_settings_ops` · `system_settings_dataset` |
| 뷰 함수 | 같음 |
| 템플릿 | `system_settings*.html` · `_system_settings_nav.html` |

**주소는 붙임표, 파이썬 이름은 밑줄이다.** 주소 쪽은 이 저장소의 `mark-all/` 과
같은 관례이고, 이름 쪽은 식별자라 밑줄이어야 한다.

> 중간에 `/settings/` 를 거쳤다(같은 날 몇 분). **리다이렉트를 안 남겼다** —
> 테스트 인스턴스에만 있었고 운영에 나간 적이 없다. 아무도 적어 두지 않은
> 주소를 영구히 이고 갈 이유가 없다. 되살릴 근거는 `/manage/` 쪽에만 있다.

### 3. 길 — 머리줄 오른쪽 끝의 톱니

`base.html` 에 뒀으므로 **어느 화면에서나 같은 자리**다. 목록 제목 옆의
`관리 →` 는 걷었다.

**아이콘을 채운 실루엣으로 그렸다.** 이 앱의 로고가 동심원 + 방사 눈금이라
가는 선으로 그린 기어는 **로고와 구별이 안 된다.** 8개 톱니를 극좌표로 계산해
`fill-rule="evenodd"` 로 가운데를 뚫었다(외부 아이콘 세트를 안 쓰므로 라이선스도
안 딸려 온다).

### 4. 미리보기 — 머리줄에서 시스템 설정의 탭으로

늘 떠 있을 만큼 자주 보는 자리가 아니고, 성격이 "설정·운영 쪽에서 한 번씩 여는
것" 이라 그 탭줄이 제자리다. 다만 **다른 셋과 성격이 둘 다르고 그것을 화면이
말한다**:

- **이 앱의 화면이 아니다** — nginx 가 여는 이웃 자리라 눌러 나가면 탭줄이 아예
  없다. `↗` 를 붙이고 오른쪽 끝으로 밀어 안쪽 셋과 갈라 세웠다
- **없을 수 있다** — `preview_url` 이 비면 탭을 안 낸다. 죽은 탭은 눌러 보고서야 안다

## 밟은 자리 — `only` 가 컨텍스트를 끊는다

미리보기 탭을 넣었는데 **화면에 안 나왔다.** 예외도 경고도 없었다.

```
{% include "viewer/_managenav.html" with here="ops" only %}
```

`only` 가 바깥 컨텍스트를 끊으므로 **컨텍스트 프로세서**(`viewer/context.py`)가
넣어 준 `preview_url` 도 조각 안에서는 안 보인다. `{% if preview_url %}` 가 늘
거짓이 되어 탭이 조용히 사라진다.

`only` 를 뺄 수도 있었지만 그대로 두고 **손으로 넘겼다** — 조각이 무엇을 받는지
호출부에 적혀 있는 편이 낫고, 이 저장소의 다른 include 도 그 규칙이다.

## 시험 (7개)

`tests/test_topnav.py`

- 화면 **일곱** 전부에 톱니가 있다 (목록 · 시야 목록 · 갤러리 · 계측 표 ·
  시야 화면 · 설정 둘). 아이콘만 있는 링크라 **읽어 줄 이름**(`aria-label`)까지 본다
- 목록 제목 옆의 옛 `관리 →` 가 없다
- **미리보기 탭이 실제로 나온다** — 위의 `only` 자리를 못 박는다
- 경로가 없으면 탭도 없다
- 미리보기가 머리줄에서 빠졌다 (두 자리에 있으면 어느 쪽이 정본인지 묻게 된다)
- **옛 `/manage/` 주소가 새 주소로 넘긴다** · **쿼리를 잃지 않는다**

되살려 확인했다 — `preview_url=preview_url` 을 빼면 정확히 그 시험이 깨진다:

```
AssertionError: '미리보기' not found in '<nav class="mnav">…' : 미리보기 탭이 없다
```

347개 전부 통과(브라우저 포함).

## 함께 걷은 것

**문턱 조정 링크 둘**(목록의 `문턱 조정 →` · 시야 목록 nav)을 걷었다(사용자 요청,
`4866d1d`). 화면은 그대로 살아 있고 링크만 없다 — `CSS 로 감추지 않았다`.
`display:none` 은 이 저장소에서 세 번 안 먹었다(`.tools` 051 · `.nowrow` 083 ·
`[hidden]` 063).

## 남은 것

- **`manage_data.py` 는 그대로 뒀다.** 그 이름은 화면이 아니라 하는 일을
  가리킨다(층을 만들고 옮기고 지우는 **쓰기 문**) — 화면 이름을 따라갈 자리가
  아니다. `CLAUDE.md` 의 구조 절도 그 뜻으로 적고 있다
- 시스템 설정 안에 **설정다운 것이 아직 없다** — 지금 셋은 자료·운영·학습 자료다.
  문턱 조정(방금 링크를 걷은)이 여기로 들어올 자리로 보인다
