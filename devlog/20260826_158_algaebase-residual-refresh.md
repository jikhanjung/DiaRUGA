# 잔여 140건을 다시 낸다 — 막힌 꾸러미를 푼 뒤 (2026-08-26)

`tools/algaebase_worklist.py --residual --stamp 20260826`

## 왜 다시 냈나

잔여 목록은 **08-25 에 한 번 냈다**(`temp/algaebase_ask_20260825.md` · 153 7절).
그런데 그때 **꾸러미 일곱(27·28·29·31·32·33·37)이 NAS 에서 안 읽혀** 건너뛴
채로 대조표가 서 있었고, 08-26 아침에 사본으로 우회해 **45개 전부를 다시
먹였다.** 대조표가 바뀌었으면 잔여도 바뀔 수 있으니 **다시 내고 대조했다.**

## 결과 — 항목은 하나도 안 바뀌었다

```
색인 이름 1,845 · 잔여 140건
   0층    1  원문 재판독 — 판을 지정해 다시 묻는다
   1층   68  원문이 학명이라고 보증한다 — 등록부에만 없다
   2층   31  원문을 아직 안 봤다
   3층    9  그 밖 — 등록부가 답할 수 있다
   9층   31  물어도 안 나온다 — 원문이 학명이 아니다

   물어볼 것 109건 · 물어도 안 나올 것 31건
```

상태별로도 08-25 와 같다 — `AlgaeBase 에 없다` 100 · `아직 안 찾았다` 20 ·
`안 적혀 있다` 15 · `미확인` 4 · `원문 재판독` 1(#431).

08-25 판과의 차이는 **두 줄의 출처 표기뿐**이고, 그것도 근거 파일(`residual`)
쪽에만 있다 — #1381 `Navicula séhrensis` 와 #1418 `Neidium preschevalski` 가
`day7_priority-148` → `day28`·`day29` 로 돌아왔다. **채울 표(`ask`)는 날짜 줄
하나 빼고 08-25 와 바이트가 같다.**

**막힌 일곱이 담당한 345줄은 이미 빈 칸이 0이었다** — 자료를 잃은 적이 없고,
08-26 의 재반입은 출처 표기를 제자리로 돌린 정리였다. 잔여가 안 움직인 것이
그 사실의 검산이다.

## 낸 것

| 무엇 | 어디 |
|---|---|
| **채울 표** (사람이 조회해 채운다) | `Diadiction/temp/algaebase_ask_20260826.md` |
| 근거 (층·자리·지금 판정) | `Diadiction/names/algaebase/algaebase_residual_20260826.md` |

**돌아오면 `names/algaebase/algaebase_day38_*.md` 로 이름을 바꿔 놓아야 먹는다**
— `ingest_algaebase.py` 의 glob 이 `algaebase_day*.md` 라 이름이 다르면
**예외 없이 그냥 안 들어간다.**

**빈 칸으로 돌아와도 지금 판정이 남는다** — `RANK` 의 `안 적혀 있다` 가 -1 이라
`AlgaeBase 에 없다` 를 못 이긴다(153 7절에서 사본으로 되살려 확인했다).

## 저장소에는 목록이 안 들어온다

두 파일 다 NAS(`Diadiction/`)에 있다 — 색인 자료는 저장소 밖이고
`atlas/*.json` 만 넘어온다. **이번에는 `atlas/*.json` 이 한 바이트도 안 바뀌어
DB 반입도 필요 없다**(08-26 재반입 때 이미 확인했다).
