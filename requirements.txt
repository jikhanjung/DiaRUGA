# 규조류 현미경 사진 분석 파이프라인
#
# CUDA 휠은 기본 PyPI에 없으므로 torch/torchvision은 아래 인덱스에서 받는다.
# 설치:
#   python -m venv .venv && . .venv/bin/activate
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu126
#
# CUDA 12.6 기준. 드라이버가 더 낮으면 cu121 등으로 바꾸고, CPU만 쓸 경우
# --extra-index-url 없이 설치하면 CPU 휠이 잡힌다 (매우 느림).

torch==2.13.0
torchvision==0.28.0

# SAM2.1 automatic mask generation.
# facebook/sam3 는 HF gated 모델이라 별도 승인 + HF_TOKEN 필요.
SAM-2 @ git+https://github.com/facebookresearch/sam2.git@2b90b9f5ceec907a1c18123530e92e794ad901a4
huggingface_hub==1.25.1

# sam2가 config 로딩에 사용
hydra-core==1.3.4
omegaconf==2.3.1
iopath==0.1.10

# 이미지 입출력, 초점 시리즈 그룹핑, focus stacking
numpy==2.4.4
pillow==12.2.0
opencv-python-headless==5.0.0.93
tqdm==4.70.0
