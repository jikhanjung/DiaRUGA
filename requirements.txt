# 규조류 현미경 사진 분석 파이프라인
#
# 이 파일은 호스트 venv 하나에 전부 까는 입구다. 컨테이너는 갈라 놓은 두 파일을
# 따로 쓴다 — web 이미지에 torch 6 GB 를 넣지 않기 위해서다 (P03):
#
#   requirements-web.txt       뷰어       Django, pillow, gunicorn
#   requirements-pipeline.txt  파이프라인  torch, SAM2, opencv (web 을 포함한다)
#
# 설치:
#   python -m venv ~/venv/DiaRUGA && . ~/venv/DiaRUGA/bin/activate
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu126
#
# 이 저장소의 venv 는 ~/venv/DiaRUGA 이다. README 의 .venv 가 아니다 (HANDOFF 8.3).
# CUDA 12.6 기준 — 자세한 것은 requirements-pipeline.txt 를 볼 것.

-r requirements-pipeline.txt

# 보고서 .md -> .docx 변환 (md2docx.py). 분석 파이프라인에도 뷰어에도 쓰이지
# 않으므로 어느 컨테이너 이미지에도 넣지 않는다.
python-docx==1.2.0

# 코어 자료 xlsx 읽기 (tools/coredata_extract.py, P17). **앞단 전용이다** —
# 반입의 뒷단(ops/import_coredata.py)은 CSV 만 읽으므로 웹 이미지에는 안 넣는다.
openpyxl==3.1.5
