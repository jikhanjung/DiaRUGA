# HANDOFF — 2026-08-07 현재 상태

이어서 작업할 사람(또는 다음 세션)을 위한 인수 문서. 무엇이 돌아가고 있고, 무엇이
반쯤 되어 있고, 어디를 밟으면 안 되는지를 적는다.

**뷰어의 이름은 `DiaRUGA` 다** (Diatom Refocusing Using Genus-level AI, 041).
표기는 이 하나뿐이다 — `Diaruga`·`DIARUGA` 로 쓰지 않는다. **대문자 자리가 곧
약자의 자리다.**

**048 에서 저장소·경로·URL·DB 이름을 전부 `DiaRUGA` 로 옮겼다.** 소문자
`diaruga` 는 기술이 강제하는 자리뿐이다 — Docker Hub 이미지, 파이썬 패키지
`diarugaweb`, `localStorage` 키. 아직 `diatom` 인 것은 **생물 이름**이다
(`segment_diatoms.py`·YOLO 클래스·NAS 의 `DiatomPhotos/`). devlog 와 `docs/` 의
지난 기록은 그때 이름 그대로 두었다.

**브랜치** main · 판 `v0.8.2` (파이프라인 `v0.5.1`)

---

## 1. 한 줄 요약

파이프라인이 전부 DB 를 쓰고(P02 6단계 끝), 뷰어·파이프라인이 컨테이너로 돌며,
NAS 에 새 슬라이드가 올라오면 1분 안에 검출까지 스스로 간다(P03). 슬라이드 **11** ·
시야 **508** · 교정 **7,472** 건.

**08-07 에 세 판이 나갔다** — `v0.8.0`(P09·P10) · `v0.8.1`(076~079) ·
`v0.8.2`(080~086). 바뀐 것이 넷이다:

- **마스크를 그리고 고친다** (P09). 검출이 못 낸 개체를 사람이 그려 넣고, 낸
  것의 모양을 고친다. 교정의 열쇠가 `(image, batch, mask_key)` 가 됐다
- **검토할 묶음을 관리 화면에서 고른다** (P10). `RunBatch.for_review` 가
  "뷰어가 볼 것" 을 정하고, `Detection.is_current` 는 **그 묶음 안에서 최신**
  으로 뜻이 좁아졌다. 검토 완료 표시도 묶음마다 따로다(073)
- **새 자료가 묶음마다 채워진다** (079·084). `RunBatch.recipe` 에 "어떻게
  돌리는가" 를 적어 두면 폴러가 검토 중인 묶음부터, 나머지는 최근 것부터 돈다.
  묶음을 **화면에서 만든다**(084) — 검출을 한 번 돌려야 묶음이 생기던 순환을 끊었다
- **관리 화면이 셋으로 갈렸다** (083) — 자료 · 운영 · 학습 자료

**그리고 판을 내보내기 전에 눌러 볼 자리가 생겼다** (085). `/DiaRUGATest/` 가
**사본 DB 로** 도는 두 번째 인스턴스다(9.1절). 올리자마자 값을 했다 —
**크롭 갤러리와 계측 표가 v0.8.0 이후 내내 500** 이었던 것을 거기서 찾았다(086).
운영에서도 죽어 있었는데 `/healthz` 도 `smoke.sh` 도 그 화면을 안 열고, 시험은
그 URL 을 열면서도 **자료가 전부 합성본이라 갈래를 안 밟고 있었다.**

**08-06 에 자료의 층을 말과 맞췄다**(063 · `v0.7.0`). 사용자가 용어를 정리해
주면서 스키마가 실제 자료와 어긋나 있던 것이 드러났다:

```
권역   한국 · 남극                       Site.area
 └ 지역   BP(북평분지) · RS23            Site
    └ 지점   BP09(노두) · GC03(시추코어)  Locality   ← 예전 이름이 Core 였다
       └ 시료   0901 · 71cm              Sample     ← 없던 층
          └ 관찰   (1) · (2)             Slide
```

**시료 층이 없던 것이 사고의 뿌리였다.** `Slide` 하나가 시료와 관찰을 겸해서
소속(`core`·`depth_cm`·`sample_kind`)이 **관찰 행마다** 앉아 있었고, 그래서 같은
시료의 관찰 둘이 서로 다른 소속을 가질 수 있었다 — `BP09-0901 (1)` 이 아무 데도
안 붙어 화면에서 통째로 사라졌다. 자세한 것은 [063](devlog/20260806_063_layers-and-locality.md).

함께 들어온 것: **관리 화면**(`/manage/`) · **노두 현장 사진**(지점 페이지) ·
검토 흐름 넷(완료 숨기기 · 다음 미검토 · 밝기/대비 · 전체 표시).

**검토가 전수 완료됐다 — 508/508.** 마지막까지 남아 있던 `bp09-0901` 이 08-06 에
끝났다. 슬라이드 11장이 전부 사람 손을 거쳤다(교정 7,472건). **다음 판의 학습
자료가 여기서 나온다.**

**08-05 에 이름을 한 겹으로 모았다**(048). 저장소·경로·URL·DB·이미지가 전부
`DiaRUGA` 다. 기능은 그대로고 **계약만 바뀌었다** — 옛 `.env` 로는 뜨지 않는다.

**08-03 에 갈래가 하나 늘었다.** 사람의 검토 264 시야를 자료로 **YOLO11m-seg 를
학습시켰고, 한 바퀴를 돌았다**(P04 4단계). 재현율을 지금 파이프라인과 같게 맞춰
놓고 재면 **정밀도가 SAM2+judge 의 1.7배, 속도는 320배**다 — 갈아탈 근거는 섰다.
아직 갈아타지는 않았다. 검출은 `is_current` 를 건드리지 않고 나란히 쌓아 두었고,
**검토 화면에서 `◉ SAM / ○ YOLO` 라디오로 같은 시야를 두 엔진으로 비교한다**(051).
YOLO 쪽은 읽기 전용이다.

---

## 2. 지금 돌아가는 것

### 뷰어 — DiaRUGA (Django 5.2, DB 기반)

**http://172.16.116.98/DiaRUGA/** — 랜딩 페이지(`/`)의 카드에서도 간다.
nginx 가 80 에서 `/DiaRUGA/` 을 떼고 `127.0.0.1:8090` 의 컨테이너로 넘긴다.

> **옛 주소는 전부 301 로 여기 모인다** — `:9090`(사내 VPN 이 9090 을 통과시키지
> 않아 옮겼다, devlog 018), 이름 변경 전의 `/diatom/`, 그리고 **대소문자를 틀리게
> 친 `/diaruga/`·`/DIARUGA/`** 까지. 서빙하는 경로는 하나다.
>
> **URL 은 대소문자를 가린다.** 정식 주소는 `/DiaRUGA/` 하나이고, 나머지는
> 리다이렉트다. 링크를 적을 때는 정식 표기를 쓴다.

```
데이터셋 목록  /                  [한국 | 남극 | 전체] 권역 탭 · 표·카드·지도 전환
시야 목록      /d/<slug>/
시야 화면      /d/<slug>/g/<n>/   ← 사람이 교정하는 화면
                 ?batch=<실행>    같은 화면을 다른 엔진으로. **읽기 전용** (051)
검출 갤러리    /d/<slug>/crops/
계측 표        /d/<slug>/detections/
문턱 조정      /thresholds/ · /d/<slug>/thresholds/
시료 속성 편집 /d/<slug>/edit/    지역·코어·슬라이드를 한 트랜잭션으로 (탭)
관리 (셋)      /manage/           자료 — 지역·지점·시료 (083)
               /manage/ops/       운영 — 검토할 묶음 · 조리법 · 새 묶음 (083·084)
               /manage/dataset/   학습 자료 — 검토에서 정답을 얼마나 뽑나 (083)
```

**엔진 고르기는 시야 화면 안에 있다** (051). 사진 위 탭줄의 `◉ SAM 0 ○ YOLO 3`
라디오다 — 누르면 **시야는 그대로 두고** 그림 위의 개체만 바뀐다. 이웃 시야로
넘어가도 고른 엔진을 들고 간다.

**묶음을 한 표로 훑던 `/engine/` 은 075 에서 지웠다** — 라디오가 같은 일을
자리를 안 잃고 한다. 옛 주소는 남아 있지 않다.

지도는 권역마다 투영이 다르다 — 남극 EPSG:3031(극구면), 한국 EPSG:5179. `전체`
탭에는 지도가 없다(섞을 수 없다). 좌표가 없는 지역은 **주황 마커 + 오차 원**으로
대략값임을 드러낸다. `Site.lat/lon` 을 채우면 저절로 파란 점이 된다.

**목록의 "검출" 칸과 상세 화면의 검출 수는 다른 것을 센다** (037). 목록은
`ClassDef.counted` 가 켜진 분류(봉상·원형·Eucampia·Chaetoceros)만 더한다 —
파편을 개체로 세면 밀도가 부푼다(369cm 923 → 444). 상세 화면의
"검출 N → 검토 후 M" 은 검토가 얼마나 걸러 냈는가를 보는 줄이라 파편도 센다.

### 분류는 `ClassDef` 테이블이 정한다 (037~040)

08-04 에 분류 쪽이 크게 정리됐다. **화면 규칙을 코드에서 테이블로 옮기는 작업**이다 —
`ClassDef` 를 테이블로 둔 목적이 "분류를 더할 때 배포가 필요 없게" 였는데, 실제로
`Chaetoceros` 를 넣어 보니 코드에 박힌 자리가 여럿 남아 있었다(038).

| 분류 | 색 | 배지 | 키 | 센다 |
|---|---|---|---|---|
| 원형 `round` | 진한 초록 `60,220,120` | `rnd` | `q` | ○ |
| 원형 파편 `round_frag` | 옅은 초록 `140,235,170` | `rndf` | `q` (한 번 더) | ✕ |
| 봉상 `rod` | 진한 파랑 `70,140,255` | `rod` | `w` | ○ |
| 봉상 파편 `rod_frag` | 옅은 파랑 `120,195,255` | `rodf` | `w` (한 번 더) | ✕ |
| Eucampia | 분홍 `255,110,190` | `euc` | `e` | ○ |
| Chaetoceros (`CRS`) | 연두 `150,225,75` | `cha` | `r` | ○ |

- **색상이 "무엇인가", 진하기가 "온전한가"** 를 말한다(039). 본체와 파편이 같은
  색 가족이라 화면에서 관계가 읽힌다. 축이 둘로 갈라져 분류가 늘어도 규칙이 선다
- **단축키도 같은 모양**이다(040) — `q` 를 한 번 더 누르면 파편. 여럿을 고른
  채로는 **전부 같은 분류일 때만** 다음으로 넘어간다
- **속명이 긴 것은 `ClassDef.short` 로 줄인다**(`Chaetoceros → CRS`). 자리가
  좁은 곳만 — **말풍선과 분류 지정 메뉴는 전체 이름**이다

> **분류를 더할 때 채울 것이 여덟이다** — `label` · `short` · `badge` · `color` ·
> `hotkey` · `counted` · `is_taxon` · `sort_order`, 그리고 **`base.html` 의 CSS**.
> 목록은 `ClassDef` 머리말에 있다. 하나라도 비면 **예외는 안 나고 그 분류만
> 조용히 다르게 굴러간다** — 마스크가 투명해 "지정은 되는데 안 보이는" 상태가
> 실제로 될 뻔했다. `check_db.py` 가 단축키·색 두 개는 잡아 준다
>
> **색은 아직 테이블과 CSS 두 곳에 있다.** 테이블에서 뿜어내는 것이 근본인데 검토 화면
> 렌더링을 통째로 건드리는 일이라 미뤘다. 그동안은 어긋날 수 있는 값이다
>
> **되돌리기는 지우는 것이 아니라 `active=False` 다.** 행을 지우면 그 분류로
> 붙인 교정이 이름 없는 분류가 되어 화면에서 안 읽힌다

```bash
cd /srv/DiaRUGA && docker compose up -d web
```

읽기·쓰기 모두 DB 다. 9절을 볼 것.

### DB (`DiaRUGA.db`, WAL)

**08-07 21:00 기준** (v0.8.2 배포 직후). 정확한 값은 이 문서가 아니라
`/healthz` 나 `smoke.sh` 가 낸다.

| 테이블 | 행 |
|---|---|
| Site / **Locality** / **Sample** / Slide | 5 / **5** / **10** / **11** |
| Viewpoint / Frame / Stack | **508** / **1,444** / **355** |
| **Image** | **2,154** (프레임 1,444 · 합성본 355 · 깊이맵 355) |
| Detection / Candidate | **2,307** / **100,259** |
| ViewpointReview / ObjectReview | **514** / **7,472** |
| ThresholdSet / ClassDef / Run / RunBatch | 2 / **6** / **203** / **2** |

> **`ViewpointReview` 가 시야 수보다 많다** (514 > 508). 완료 표시가 **묶음마다**
> 남기 때문이다(073) — 한 시야가 묶음 둘에서 검토되면 줄이 둘이다. 시야 단위로
> 세면 508/508 이다.

> **`Core` 가 `Locality`(지점)로 바뀌고 그 아래 `Sample`(시료)이 생겼다** (063).
> `Slide` 는 이제 **관찰만** 담는다 — `core`·`depth_cm`·`sample_kind` 는 위로
> 올라갔다. 자세한 것은 1절과 [063](devlog/20260806_063_layers-and-locality.md).

**검토가 전수 완료됐다 — 508/508 시야.** 08-06 에 마지막 `bp09-0901` 이 끝났다.

### 층별 상태

| 지역 | 지점 | 유형 | 시료 | 관찰 | 시야 | 검토 |
|---|---|---|---|---|---|---|
| RS23 | GC03 | 시추코어 | 71cm · 231cm · 369cm | 3 | 127 | **전수** |
| WAP13 | GC47 | 시추코어 | 116cm · 450cm | 2 | 90 | **전수** |
| AM22 | GC10B | 시추코어 | 25cm · 261cm | 2 | 57 | **전수** |
| GC03 | C1 | 시추코어 | 72cm · 816cm | 2 | 92 | **전수** |
| **BP** | **BP09** | **노두** | **0901** | **2** | **142** | **전수** |

**북평분지는 별개 갈래다** — 한국 동해안 육상 노두라 남극 시료와 따로 다룬다
(7절). 시료 `0901` 하나에 관찰이 둘이다(`BP09-0901` · `BP09-0901 (1)`).

| 관찰(슬라이드) | 시야 | 비고 |
|---|---|---|
| RS23-GC03 71cm | 34 | 첫 전수 검토 |
| RS23-GC03 231cm | 19 | |
| RS23-GC03 369cm | 74 | 가장 큰 슬라이드 |
| WAP13-GC47 116cm | 47 | 단독 시야 26개 (그룹핑 임계값 미확인) |
| WAP13-GC47 450cm | 43 | YOLO 의 `val_slide` — 학습에 안 쓴 슬라이드 |
| AM22-GC10B 25cm | 30 | 그룹핑 의심은 풀렸다 (3.6절) · 08-04 에 시야를 갈랐다 |
| AM22-GC10B 261cm | 27 | 27 중 26이 단독 시야 — 그룹핑 확인 표본 부족 |
| GC03-C1 72cm | 61 | 260803 · 프레임 이름 겹침으로 한 번 멈췄다 (031) |
| GC03-C1 816cm | 31 | 260803 |
| **BP09-0901** | 86 | 260803 · 관찰 `0` |
| **BP09-0901 (1)** | 56 | 260805 · **같은 시료의 다른 관찰**(`obs_no=1`, 056) |

11장 모두 `state=done`, 검토 전수 완료.

### 한 시료에 관찰이 여럿일 수 있다 (056)

> **063 이 이 구조를 제자리에 놓았다.** 056 때는 시료 층이 없어서 소속
> (`core`·`depth_cm`)이 관찰마다 따로 앉아 있었고, 같은 시료의 관찰 둘이 **다른
> 소속을 가질 수 있었다.** 지금은 관찰이 `Sample` 행을 가리키기만 하므로 어긋날
> 수가 없고, "같은 시료의 다른 관찰" 이 **조인 한 번**이다
> (`Slide.sibling_observations`).

같은 시료를 처리 방법을 달리해(산처리·체 크기) 여러 번 촬영한 것을 갈라 담는다.
**폴더 접미사는 숫자만 받는다** — `(산처리)` 처럼 글자를 받으면 슬러그가 뭉개져
서로 다른 관찰이 같은 슬러그로 부딪히고, `update_or_create(slug=…)` 라 **한쪽이
다른 쪽을 조용히 덮어쓴다.**

| 칸 | 누가 채우나 |
|---|---|
| `obs_no` | 폴더 접미사에서 자동. 없으면 `0` |
| `obs_label` | **사람** (산처리 · 체 20µm — 10자) |
| `hide_in_list` | 사람 — 목록에서 숨긴다 |
| `exclude_from_totals` | 사람 — 합계에서 뺀다 |

- **자동값과 사람 값을 가른 이유**는 한 칸이면 폴더에서 다시 읽을 때 사람이 적은
  것을 덮기 때문이다. 반입·그룹핑은 `obs_no` 만 쓰고 `obs_label` 은 안 건드린다
- **합계는 `exclude_from_totals` 만 읽는다.** 숨김을 읽으면 보기 토글 한 번에
  같은 자료가 다른 숫자를 낸다 — 그 대가로 생기는 "보이는 줄의 합 ≠ 합계" 는
  화면이 적는다
- **슬러그는 URL 이다.** 허용 문자가 아닌 것은 `slide_slug()` 가 `_` 로 눕힌다
  (`BP09-0901 (1)` → `260805_bp09-0901_1`). 이 규칙은 **파이프라인 이미지 안**에
  있다 — 고쳤으면 그 판을 올려야 한다(057)

### 관리 화면 — 셋이다 (063 · 083 · 084)

목록 머리줄의 **관리 →** 로 들어간다. **083 에서 셋으로 갈랐다** — 묻는 것이
다르고 쓰는 때가 다른데 한 화면에 있으면 자주 쓰는 것이 드물게 쓰는 것에 묻힌다.

| 화면 | 무엇을 묻는가 | 언제 |
|---|---|---|
| `/manage/` 자료 | 이 관찰이 어느 지점·시료의 것인가 | 새 슬라이드가 들어올 때 |
| `/manage/ops/` 운영 | 지금 무엇을 보고 있고 새 자료를 어떻게 채우는가 | 회차를 돌릴 때 |
| `/manage/dataset/` 학습 자료 | 검토한 것에서 정답을 얼마나 뽑을 수 있나 | 다음 회차를 준비할 때 |

**운영 화면이 묶음을 쥔다** — 검토 대상 고르기(P10) · 조리법 적기(079) ·
**새 묶음 만들기**(084). 새 묶음은 조리법을 **베껴 올 수 있다**(대개 지난 회차에서
가중치만 바뀐다). `weights_guessed` 는 안 물려준다 — 추측 표시가 영영 따라다니면
안 된다. **묶음을 만들었다고 검출이 생기지는 않는다**(화면과 응답이 둘 다 그렇게
말한다) — 몇 시간짜리 GPU 작업을 버튼 하나로 조용히 시작하지 않는다.

아래는 **자료 화면**(`/manage/`)의 이야기다. 지역·지점·시료를 만들고 지운다.

**관찰은 만들지도 지우지도 못한다** — 폴더가 만드는 것이고 그 아래에 재생성
불가한 교정이 달려 있다. 되는 것은 **소속을 옮기고 떼는 것**뿐이다.

**소속 없는 관찰을 잡아 주는 유일한 화면이다.** 그것은 어느 권역 탭에도 안 걸려
목록·지도·지점 페이지 어디에도 안 나온다 — 500 도 404 도 아니라 그냥 사라진다.
여기서는 붙일 곳까지 추천해서 낸다(같은 시료의 다른 관찰이 어디 있는지를 본다).

**지우기에 문턱이 있다.** `Slide.sample` 이 `SET_NULL` 이라 시료를 지우면 관찰이
조용히 소속을 잃는다 — 딸린 것이 있으면 막고, **무엇이 몇 개 걸려 있는지 버튼에
미리 적는다.** 서버가 다시 검사한다(화면에서 막는 것은 막는 것이 아니다).

`check_db.py` 의 **7번(층)** 이 같은 것을 센다: 소속 없는 관찰 · 지점 유형과 안
맞는 위치 칸 · 위치 없는 시료 · 빈 지점.

### 노두 현장 사진 (063)

노두 지점의 페이지에 현장 사진이 붙는다. 시추코어 쪽에는 암상 띠가 들어올 자리가
잡혀 있는데 노두 쪽은 그 자리가 비어 있었다.

**파일이 곧 자료다 — DB 에 행이 없다.** NAS 공유(`N:\DiaRUGA\outcrop` =
`/nfs/temp-share/DiaRUGA/outcrop`)에 이름이 **`<지점코드> (n).jpg`** 인 것이
지점과 사진을 잇는 유일한 근거다. 탐색기로 직접 올려도 화면에 나타나고, 새 노두
지점을 만들고 사진만 두면 저절로 붙는다.

- 화면에서도 올린다 — 끌어다 놓기·파일 고르기. **이름은 서버가 짓는다**
- **세 장까지다**(용량 관리). 올릴 때 긴 변 2400px·q85 로 줄인다 (12 MB → 0.9 MB)
- 주소에 파일 이름이 없다 — 서빙·삭제가 `(지점, 순번)` 으로만 짚는다
- **NAS 가 없어도 죽지 않는다** — 읽기가 `OSError` 를 삼키고 빈 목록을 낸다
- **web 컨테이너에 그 폴더를 rw 로 물렸다.** 쓰는 곳은 `viewer/outcrop.py` 하나다
- **지점 코드를 바꾸면 사진이 떨어진다.** 이름이 유일한 근거라 그렇다

이미 있던 사진은 `shrink_outcrop.py` 로 줄였다(37.4 → 2.9 MB, 원본 사본은
`backup/outcrop-original/`).

### 파이프라인 (전부 DB)

```
scan_nas → ingest_nas → group_focus_series → focus_stack --slide → segment_diatoms --slide
```

`import_json.py` 는 더 이상 파이프라인에 없다. `groups_*.json` 도 빠졌다 —
`-o` 를 줄 때만 내보낸다. 이 흐름을 `deploy/poll_nas.sh` 가 1분마다 돌린다.

**GPU 를 쓰는 작업은 한 번에 하나만 돈다.** 잠금이 `segment_diatoms` 안에 있어서
부르는 쪽이 기억할 필요가 없다 — 폴러가 도는 중에 손으로 돌려도 기다렸다 이어 간다.

### 검출 엔진이 두 벌이다

`segment_diatoms.py --backend {sam2,yolo}`. **YOLO 는 `--keep-current` 로 넣어
기존 검출·교정을 건드리지 않고 나란히 쌓았다.** 무엇을 무엇과 비교하는지가 화면에
잡히도록 `RunBatch` 로 묶는다(028).

| 묶음 | 실행 | 검출 | `is_current` | `for_review` |
|---|---|---|---|---|
| `sam2-전수` | 21 | 508 | 508 | **켜짐** ← 뷰어가 보는 것 |
| `yolo-3차` | 44 | 1,799 | 1,799 | 꺼짐 |

(08-07 현재. `yolo-1차`·`yolo-2차` 는 046 에서 걷어 냈다.)

> **`is_current` 가 양쪽 다 켜져 있는 것이 정상이다.** P10 에서 뜻이 **"그 묶음
> 안에서 최신"** 으로 좁아졌기 때문이다 — 예전 표가 YOLO 쪽에 `현재 0` 이라고
> 적었던 그 칸은 이제 없다. **뷰어가 볼 것을 정하는 것은 `for_review` 다.**
> 둘을 함께 보는 것이 `Detection.objects.reviewing()` 이고, 화면은 전부 그것을
> 지난다. 묶음에 안 든 `detect` 실행 70개는 **전부 검출을 하나도 안 남긴 것**
> (죽은 시도 38 · 할 일이 없던 실행 32)이라 묶지 않았다.

**비교하는 자리는 검토 화면이다** (051). 시야 화면의 `◉ SAM / ○ YOLO` 라디오가
`?batch=` 를 붙여 같은 시야를 다시 연다. **묶음이 아니라 엔진으로 접는다** —
한 엔진에 묶음이 여럿이면 *지금 보는 것 → 현재 검출을 낸 것 → 나중 것* 순으로
대표를 세운다(`data.engines_from_batches`). 그래서 `yolo-4차` 를 쌓아도 칸이
늘지 않고 대표만 바뀐다.

폴러도 스스로 묶는다(`DETECT_BATCH`, 기본 `sam2-전수`). **079 부터는 묶음의
조리법을 따른다** — 검토 중인 묶음부터, 나머지는 최근 것부터 채운다.

### 이미지·검출·교정 (P06 · 055)

```
Slide 1:N Viewpoint 1:N Image(kind = stack | frame | depth) 1:N Detection 1:N Candidate
                                    ↑
                           ObjectReview.image      UNIQUE (image, mask_key)
```

**`Image` 는 검출을 돌릴 수 있는 이미지 한 장**이고 열쇠는 `path` 다(파일 하나에
행 하나 — 2,154개). 예전에는 합성본이 `Frame` 이 아니라 **테이블이 둘**이어서
`Detection` 이 `target`(`stack|frame`) + nullable `frame` 으로 다형 연관을 흉내
냈다. 그것이 교정의 열쇠까지 흔들었다(`(viewpoint, mask_key)` 는 시야마다 볼
이미지가 한 장일 때만 성립한다).

- **촬영·합성 메타는 `Frame`·`Stack` 에 그대로 있다.** `Image` 는 정체만 맡는다
- **`kind="depth"` 에는 검출이 안 붙는다** — 관행이 아니라 스키마에 적혀 있다
- **만드는 문은 `web/viewer/images.py` 하나다.** 파이프라인 네 자리와 `regroup`
  이 전부 여기를 지난다
- **`Image.viewpoint` 는 `SET_NULL`, `Image.stack` 은 `CASCADE`** — 시야를 가르면
  **프레임은 살아남고 합성본·깊이맵은 죽는다.** 방향이 반대인 것이 요점이다
- 앞뒤가 맞는가는 `backfill_images.py --verify` 가 본다(아홉 항목)

---

## 3. 지금 조심할 것

### 3.1 교정의 원본은 DB 다 — 감사 기록은 `review/` 로 나간다

**6,732건이고 재생성 불가다.** 원본은 `DiaRUGA.db` 이고, `export_review.py` 가
`review/<슬라이드>/g<n>.json` 으로 내보낸다(P02 5단계 · P06 — 2026-08-05).
git 에 남으므로 **`git diff` 로 "언제 무엇이 달라졌나" 가 보인다.**

```bash
python export_review.py            # 저장소 review/ 로 (호스트 venv 로 돈다)
python export_review.py --check    # 파일 ↔ DB 대조. 아무것도 쓰지 않는다
python export_review.py --db /data3/DiaRUGA/backup/<사본>.db --out /tmp/before
diff -r /tmp/before review/        # 두 시점을 견준다
```

> **왜 호스트에서 도는가** — Django 를 임포트하지 않고 sqlite3 로 **읽기 전용**
> 으로만 열어서 `backup_db.py` 와 같은 자리다(9.2절의 예외 근거가 그대로 성립).
> 그래야 저장소에 바로 쓰고, 스키마가 바뀌는 동안에도 같은 도구로 견주고,
> 백업 파일을 `--db` 로 그대로 읽는다.
>
> **파일 이름이 `(슬라이드, 시야)` 인 이유**: 옛 평면 형식(`<stem>_review.json`)은
> stem 이 슬라이드끼리 겹쳐 **파일이 서로를 덮어썼다** — 053 과 같은 원인이다.

**그래도 `backup_db.py` 가 첫 안전망이다** — 내보내기는 교정만 담는다.

- `DiaRUGA.db` 는 gitignore 다. 실물은 `/srv/DiaRUGA/db/`
- **큰 작업 전에 반드시** `deploy/host/dbrun.sh backup_db.py --note <설명>`
- `cp DiaRUGA.db` 로 뜨지 말 것 — WAL 이라 불완전한 사본이 된다
- 사본은 `/data3/DiaRUGA/backup/`, 오프사이트는 NAS (9.6절)

### 3.2 이상하면 `check_db.py` 부터

`refilter`·`segment_diatoms` 를 돌린 뒤, `judge.py` 의 판정 규칙을 고친 뒤, 그리고
숫자가 이상할 때 돌린다(1초). 여기서 잡는 것은 **예외가 나지 않고 그냥 틀린 상태**다.
실제로 `--scale 1.0` 을 빠뜨려 두 시야가 절반 해상도로 검출된 것을 이 검사가 잡았다.

현재 **13개** 검사 전부 OK, 고아 교정 0. 040 에서 "활성 분류에 단축키가 있다" ·
"활성 분류에 색이 있다" 가 들어왔다 — 분류를 더하고 한 칸 비워 둔 것을 잡는다.

```bash
deploy/host/dbrun.sh check_db.py            # 9.2절 — DB 는 컨테이너 문으로만
```

### 3.3 뷰어 동작은 눌러 봐야 안다 — 운영 DB 에 붙이지 않는다 (**두 번 당했다**)

`data.save_review()` 는 마지막에 이렇게 한다.

```python
ObjectReview.objects.filter(viewpoint=vp).exclude(mask_key__in=keys).delete()
```

**"뷰어는 늘 그 시야의 교정 전체를 보낸다"** 는 전제다. 전제가 깨지면 나머지를
전부 지운다.

- **017** — 검토 차단을 시험하다 빈 키 목록을 운영 DB 로 보내 **교정 14건**을 지웠다
- **027** — `/engine/` 을 "읽기 전용" 이라 적어 놓고 CSS 로 버튼만 감췄다. 마스크
  자체가 클릭 대상이라 한 번의 클릭이 **교정 37건**을 지웠다. 백업 숫자가
  아니었으면 몰랐다
- **053** — **저장이 남의 시야로 가고 있었다.** `save_review` 가 `stem` 하나로
  시야를 찾았는데 **프레임 이름은 슬라이드끼리 겹친다**(143종). 싱글턴 시야
  135개 중 **12개가 다른 슬라이드의 시야를 열고 있었고**, 개체를 하나도 안
  건드리고 **"검토 완료" 만 눌러도** 그쪽 교정이 통째로 지워졌다 — 키가 없으면
  아래 "남의 검출 키 409" 방어가 검사할 것이 없어 비껴간다. 200 이 돌아오고
  화면에는 "저장됨" 이 뜬다. 노출됐던 교정 138행.
  지금은 **`(slug, gid)` 로 짚고**(이름은 겹쳐도 주소는 안 겹친다) `stem` 은
  검증에만 쓴다. 이름으로 찾는 길은 **모호하면 거절**한다

두 겹으로 막아 두었다 — 화면은 `readOnly` 면 POST 를 **아예 안 보내고**(나가는 문
하나를 잠근다), 서버는 **남의 검출 키가 섞이면 409** 로 아무것도 바꾸지 않는다.

> **051 — 막는 것으로는 모자란다. "되는 것처럼 보이면" 안 된다.** 엔진 고르기를
> 검토 화면으로 들이면서 읽기 전용 화면이 늘었는데, 저장은 막혀 있는데 **우클릭
> 메뉴는 살아 있었다.** "오검출로 삭제" 를 누르면 화면에서 마스크가 지워지고,
> 탈락 펼침판에서 고르면 검출로 올라간다 — 저장만 안 나간다. 그렇게 한 시야를
> 검토하고 새로고침하면 판단이 통째로 사라지고, 사람은 자기가 무엇을 했는지
> 모른다. **`/review` 가 전체를 갈아치우는 설계라 로컬 상태는 나중에 보낼 수도,
> 병합할 수도 없다.**
>
> 지금은 읽기 전용에서 **우클릭 메뉴가 고치는 항목을 아예 안 내고**(대신
> `Read only — 검토는 현재 검출에서 하십시오` 를 적는다), 탈락 펼침판 칸이
> `disabled` 다. 개체 선택과 말풍선은 남긴다 — 지표를 읽는 일이다.
>
> `readOnly` 갈래를 더할 때는 **저장 경로만 보지 말고 화면이 반응하는 자리를
> 전부 센다**: 키보드 · 우클릭 메뉴 · 펼침판 · 코멘트 칸.

**여전히 빈 키 목록 POST 는 전부 지운다.** 그것이 "교정 전체 초기화" 의 정상
경로라서다. **화면을 눌러 보는 길이 이제 둘이고, 규칙이 다르다** — 자동 시험은
아래 P08 상자, 손으로 눌러 볼 때는 사본 DB(`DIARUGA_DB=…/test.db`)에 붙이고
시험 코드에 `assert` 로 사본인지 확인하는 줄을 넣는다.

> **08-04: 브라우저가 생겼다 (045).** `playwright` + 헤드리스 크로미움을 넣었다 —
> 키 입력·클릭·페이지 이동·**콘솔 오류**·화면 캡처가 된다. 아래 "못 보는 것" 의
> 첫째(이벤트 배선)는 이제 닿는다. 실제로 `?shot=last` 가 JS 를 죽이던 것을
> `page.on("pageerror")` 한 줄로 잡았다.
>
> ```bash
> ~/venv/DiaRUGA/bin/pip install playwright
> NODE_EXTRA_CA_CERTS=/etc/ssl/certs/ca-certificates.crt \
>   ~/venv/DiaRUGA/bin/playwright install chromium
> ```
>
> **`NODE_EXTRA_CA_CERTS` 를 빠뜨리면 `SELF_SIGNED_CERT_IN_CHAIN` 으로 죽는다**
> (9.5 — 사내망이 TLS 를 가로채는데 node 는 시스템 CA 저장소를 안 본다).
> `deploy/ca/` 의 인증서 하나로는 안 되고 **시스템 번들을 통째로** 줘야 했다.
>
> **손으로 붙일 때는 반드시 사본 DB 다.** 저장소에서
> `DIARUGA_DB=…/사본.db DIARUGA_SCRIPT_NAME= manage.py runserver 127.0.0.1:8099`
> 로 띄우고 거기에 붙인다 — 운영에 붙이면 클릭 한 번이 교정을 지운다(027).
> **자동 시험은 이 규칙이 필요 없다** (아래 P08 상자).

> **08-06: 자동 시험이 생겼다 (P08 · 064·065). 지금 333개다.**
>
> ```bash
> python web/manage.py test viewer --exclude-tag browser   # 262개 · 2.5초
> python web/manage.py test viewer                         # 333개 · 84초
> ```
>
> **여기서 "사본 DB 에 붙인다" 규약이 필요 없어진다.** 4겹이
> `StaticLiveServerTestCase` 라 **테스트 DB 위에 서버가 뜬다** — 운영 DB 에
> 붙을 경로 자체가 없다. 손으로 `:8099` 를 띄우고 사본을 물리는 것보다 안전하다.
> **손으로 눌러 볼 때는 위 규칙이 그대로다** — 그쪽은 사람이 주소를 고른다.
>
> **막을 문이 둘이라는 것을 시험 장치가 먼저 배웠다.** DB 만 확인하고 파일
> 뿌리를 안 봤더니 **픽스처가 `/data3/DiaRUGA` 에 사진을 썼다**(064). 덮어쓴
> 것은 없었지만 운이었다 — 진짜 `stacked/` 가 평면이고 `260801` 이 없던
> 촬영일이라 안 겹쳤을 뿐이다. 지금은 `tests/base.py` 가 **DB 와 `DATA_ROOT` 를
> 둘 다** 확인하고, 파일 쓰는 문을 하나로 모아 거기서 다시 본다 —
> **순서에 기대는 안전은 다음에 순서가 바뀌면 조용히 풀린다.**
>
> **읽기 전용 넷을 실제로 눌러 본다** (051 이 난 자리다) — 키보드 · 우클릭
> 메뉴 · 탈락 펼침판 · 코멘트 칸. 갈래마다 **보통 화면 대조군**이 붙어 있다.
> 그것이 없으면 배선이 끊겨도 초록이다.
>
> **되살려서 잡히는 것을 보고 나서 "있다" 고 말한다.** 회귀 여덟 가지를 그렇게
> 확인했다(P08 §6 의 표). 실제로 **실패할 수 없는 시험을 하나 만들었다가**
> 저장 차단을 통째로 풀어 보고서야 알았다 — 마스크 클릭은 고르는 일이라
> `save()` 를 안 부른다. **초록은 증거가 아니다.**
>
> **GitHub Actions 가 push 마다 돈다.** 시험이 통과한 것만 뷰어 이미지가 된다.
> 파이프라인 이미지는 거기서 안 굽는다(러너에 GPU 가 없어 구워도 확인이 안
> 된다). `deploy.sh` 는 그대로 사람이 부른다 — P08 §5.

**이 시험이 못 닿는 자리가 있(었)다.** 테스트 클라이언트로는 "200 이 뜨고 이런
HTML 이 나온다" 까지만 본다. 못 보는 것:

- **이벤트 배선** — 키가 실제로 듣는가, 활성 패널 판정이 맞는가, 입력칸 안에서
  안 듣는가. 단축키(040)가 그렇게 나갔다
- **눈에 보이는 것** — 색 대비, 로고 크기·굵기, 좁은 칸에서 글자가 넘치는가

닿는 데까지는 간다. 순환 규칙 같은 **분기 로직은 렌더한 페이지에서 그 조각을
떼어 내 `node` 로 돌렸고**(040), 색은 테이블(`ClassDef.color`)과 CSS 를 긁어
대조했다(039). 손으로 옮겨 적으면 사본을 시험하는 것이 되니 **렌더 결과에서
떼어 오는 것**이 요령이다.

> **08-06: 위 둘 중 첫째는 이제 자동이다** (P08). 이벤트 배선은 4겹이 진짜
> 크로미움으로 눌러 보고, 039 에서 손으로 하던 `ClassDef` ↔ CSS 대조는
> `tests/test_classdef_css.py` 가 매번 한다 — **렌더 결과에서 떼어 온다**
> 는 요령이 그대로 시험이 됐다. 템플릿 위생도 함께 본다(`{% comment %}` 대신
> 여러 줄 `{# #}` 를 쓴 것이 **렌더 결과로 새고 있었다** — 그 시험이 첫판에
> 잡았다).
>
> **여전히 사람이 봐야 하는 것은 "눈에 보이는 것" 하나다** — 색 대비, 로고
> 크기·굵기, 좁은 칸에서 글자가 넘치는가.

**남는 것은 배포 뒤 사람이 한 번 열어 봐야 한다** — 판을 낼 때 "무엇이 안
덮였는지" 를 함께 적어 두는 편이 낫다.

### 3.4 `import_json.py` 를 아무 때나 돌리지 말 것

멱등이지만 **`Candidate` 를 지우고 다시 만든다.** 교정은 `mask_key` 로 붙으므로
사라지지 않지만, DB 에서만 한 교정이 있는 상태에서 옛 JSON 을 다시 넣으면
JSON 쪽 값으로 되돌아간다. **지금은 JSON 이 DB 보다 한참 낡았다 — 돌리지 말 것.**

### 3.5 `tighten_bbox.py` 는 만들어 뒀지만 **적용하지 않았다**

떠돌이 픽셀 하나가 bbox 를 부풀리는 문제(devlog 005 §5)를 고치는 스크립트다.
dry-run 까지 했다. **`mask_key` 를 바꾸는 작업이라** P02 8단계(재바인딩·고아 화면)와
한 번에 푸는 것이 낫다. dry-run 숫자는 슬라이드 3장 시절 것이라 다시 재야 한다.

### 3.6 그룹핑 — 의심은 풀렸고, 기준이 바뀌었다

`AM22-GC10B 25cm` 이 옛 기준(여유 0.2)에 걸렸지만 **사람이 확인해 보니 정상**이었고,
**틀린 것은 지표 쪽**이었다(018). 최솟값·최댓값으로 재던 것이라 표본이 많은
슬라이드가 불리했다. 지금 기준은 이렇다.

- **판정은 겹침으로만 한다** — 그룹 안 상관과 경계 상관이 겹치는 쌍이 있을 때만
  사람을 부른다. 여유(margin)는 좁아도 기록만 한다
- **표본이 모자라면 모른다고 말한다** — `AM22 261cm` 은 그룹 안 쌍이 1개뿐이라
  "확인 필요(표본 부족)" 다. 27 시야 중 26개가 단독인 것이 촬영 방식인지 그룹핑
  문제인지는 아직 모른다

이미 처리된 슬라이드는 `done` 인 채로 두었다 — **앞으로 들어오는 것부터** 새 기준이
걸린다.

### 3.7 `NOT NULL` 칸을 더할 때는 `db_default` 를 함께 준다

**한 DB 를 판이 다른 두 이미지가 쓴다.** 뷰어는 화면을 고칠 때마다 굽고,
파이프라인은 알고리즘이 바뀔 때만 굽는다 — 두 판이 같아질 일이 없다.

`Site.area` 를 `default="ant"` 로만 더했더니 **Django 의 `default` 는 파이썬
쪽**이라 옛 파이프라인 이미지의 INSERT 에 칼럼이 아예 안 들어갔고, NAS 반입이
`IntegrityError` 로 죽었다(029). `db_default` 를 함께 주면 DB 가 채운다 —
6.92 GB 이미지를 다시 굽지 않고 풀린다.

### 3.8 파이프라인이 도는 중에 검토를 저장하면 DB 가 잠긴다

WAL 이라 읽기는 여럿이 되지만 **쓰기는 한 번에 하나**다. 파이프라인이 개체
수천 개를 한 트랜잭션으로 넣는 동안 사람이 검토를 저장하자 20초를 기다리다 죽었고,
**`bp09-0901` 의 프레임 229장이 통째로 날아갔다**(043).

트랜잭션을 둘로 나눠 고쳤다(개체 넣기 / `is_current` 옮기기). **지금까지 안 터진
이유는 파이프라인이 돌 때 사람이 검토하지 않았기 때문**이다. 그것이 일상이 되면
SQLite 를 다시 볼 문제가 된다.

---

## 4. 어디까지 왔나

### 4.1 P02 — DB 이전 (8단계)

| 단계 | 상태 |
|---|---|
| 1. 모델 + 마이그레이션 + `DATABASES` | **끝** |
| 2. `import_json.py` (멱등) | **끝** — 8초 |
| 3. 대조 `verify_db.py` | **끝** — 검사 37개 전부 일치 |
| 4. 뷰어를 DB 로 | **끝** |
| 5. `export_review.py` | **끝** — 2026-08-05. 시야 432 · 교정 6,732 (P06) |
| 6. 스크립트 4개를 DB 에 쓰게 | **끝** — devlog 010·011·012 |
| 7. JSON 을 원본에서 내리기 | **끝** — 2026-08-05. overlay JPG 를 아예 걷고(1,496장 752 MB · 아무도 안 읽었다), `out/*.json` 은 `--export-json` 뒤로 보냈다. 읽는 쪽은 전부 이전기·일회성 도구뿐이다 |
| 8. 재바인딩 + 고아 화면 | **반** — `rebind.py` 는 만들었다(011). **`/orphans/` 화면이 없다** |

8단계는 **학습·엔진 교체의 전제다.** 엔진이 바뀌면 `exact` 바인딩이 거의 0 이 되고,
고아가 된 교정을 볼 화면이 없으면 사람의 판단이 조용히 버려진다. **YOLO 로 갈아탈
결심을 하기 전에 이것이 있어야 한다.**

### 4.2 P04 — 사람의 검토로 검출기를 학습시킨다

| 단계 | 상태 |
|---|---|
| 0. 전수 표시로 재현율 기준선 | **버렸다** — 비용 대비 얻는 것이 "SAM2 상한 대비 재현율" 뿐이었다 (023) |
| 1~3. 자료 꾸러미 (`export_yolo.py`) | **끝** — 264 시야 · 개체 1,503 · 상자/폴리곤 두 벌 (023) |
| 4. 첫 학습 (11m, `imgsz=1280`) | **끝** — RTX 8000 에서 20분 (025) |
| 5. 비교 | **끝** — conf 훑기까지 (025 §8) |
| 6. 운영 반입 | **반** — 이미지에 ultralytics 는 들어갔다(032). **엔진은 안 갈았다** |

**자료.** 사람이 인정한 개체 1,503 · **지운 것 4,039** · 검출기가 놓쳐 되살린 것
376. 음성 4,039가 이 자료의 핵심이다 — SAM2 가 실제로 헷갈린 자리에 사람의
"아니다" 가 붙어 있고, YOLO 는 **라벨 없는 자리를 배경으로 배우므로** 별도 처리
없이 효력이 있다. 꾸러미는 `/data3/DiaRUGA/datasets/yolo-v1{,-seg}` (688 MB,
저장소 밖 · 재생성 가능).

**성적** — `val_slide`(WAP450, 학습에 안 쓴 슬라이드) 기준.

| | 재현율\* | 정밀도 | 시야당 |
|---|---|---|---|
| SAM2 + judge | 79.6% | 14.8% | 23.7초 |
| **YOLO seg 11m** (conf 0.033, 재현율을 맞춘 자리) | 79.6% | **25.2%** | **0.07초** |

\* "SAM2 가 마스크를 만든 범위 안에서" 라는 한정이 붙는다 (023 §2).

**이 모델은 남극 전용이다.** 꾸러미 264 시야에 `bp09` 는 **0개**다 — 08-03 자료를
뽑을 때 BP 는 검토가 1 시야뿐이라 자연히 빠졌는데, **앞으로도 당분간 일부러
뺀다**(7절). 그래서 위 성적은 *남극 시료 기준*이고, BP 에 그대로 적용된다고
보면 안 된다. BP 를 학습에 넣을 때는 **섞지 말고 갈래를 나눠서** 판단한다.

**같은 재현율에서 정밀도 1.7배, 속도 320배.** 기본 conf 한 점만 보면 재현율이
22%p 지는 것으로 보이는데, 그것은 **곡선 위의 한 점**일 뿐이다 — YOLO 의 재현율
상한은 0.96 이고, SAM2+judge 의 14.8% 는 문턱으로 못 사는 **곡선 자체의 높이**다.

**seg 를 기본으로 삼는다.** mAP50-95 가 detect 0.39 → seg 0.52 로 뛰는데 mAP50 은
0.61 → 0.64 뿐이다 — **못 찾던 것을 찾게 된 것이 아니라 찾은 것을 제대로 감싸게
된 것**이다. 봉상 개체의 축정렬 상자가 중앙값 52%가 배경이라던 관측과 맞는다.

가중치는 `runs/segment/11m-v1seg-1280/weights/best.pt` (45 MB,
`sha256 d9e20a26…`, `PROVENANCE.md` 동봉). `runs/` 는 gitignore 다.

**아직 안 한 것 (025 §9 의 순서 그대로)**

1. **싱글턴/합성본 차이가 모델 탓인지 라벨 탓인지 가른다.** 합성본 쪽 mAP50-95 가
   일관되게 낮은데, 정답 폴리곤도 **합성본 위에서 SAM2 가 만든 것**이라 "모델이
   못 맞히는 것" 과 "정답이 흐린 것" 이 안 갈린다. 싱글턴 시야를 인위적으로 합성해
   라벨이 얼마나 흔들리는지 보면 갈린다. **이것이 다른 것들보다 먼저다**
2. **conf 0.1 로 새 시야를 뽑아 검토에 건다.** 검토 부담이 시야당 25.1 → 8.0 개로
   **3.1배** 준다(재현율 0.796 → 0.687). 재현율을 지금과 같게 두려면 conf 0.033 에
   14.8개, **1.7배**. 여기서 나오는 교정이 곧 다음 판의 자료다
3. 재현율 상한에 걸린 8개를 눈으로 본다 (못 찾은 것인가, IoU 0.5 를 못 넘긴 것인가)
4. **AM 계열에서 재 본다.** judge 의 정밀도가 AM261 에서 1.0%, 231cm 에서 4.7% 다 —
   문턱 하나로는 안 되는 슬라이드들이고 YOLO 의 이득이 가장 클 곳으로 보이지만
   **아직 재 보지 않았다.** 지금 `val_slide` 는 WAP450 한 장뿐이다
5. 깊이 맵을 채널로 (`stacked/*_depth.npz`) · s·l·x 비교 · 슬라이드별 분해

**엔진을 갈면 합성이 유일한 병목이 된다.** 같은 기계에서 합성 10.0초 + SAM2 23.7초
= 33.7초가 합성 10.0초 + 추론 0.07초 = 10.1초가 된다(3.3배). 그 앞은 1번이 먼저
답해야 하는 문제다.

---

## 5. 주요 파일

| 파일 | 무엇 |
|---|---|
| `web/viewer/models.py` | 스키마 18개 모델. 읽기 전에 파일 첫 주석부터. **`Site.area`(한국/남극)와 `Site.region`(해역 이름)은 다른 칸이다**. 층은 `Site → Locality → Sample → Slide` (063) |
| `web/viewer/naming.py` | **폴더 이름 → 층. 규칙은 여기 하나뿐이다** — 뷰어·파이프라인·마이그레이션이 같은 것을 본다. Django 도 cv2 도 안 부른다 (063) |
| `web/viewer/manage_data.py` | 관리 화면이 쓰는 문 — 만들고 옮기고 지운다. **지우기 문턱이 여기 있다** (063) |
| `web/viewer/outcrop.py` | 노두 현장 사진. NAS 공유에 **파일로만** 산다 — DB 에 행이 없다 (063) |
| `web/viewer/data.py` | DB → 뷰가 쓰는 dict. **읽기 전용이라는 약속이 있다** — **함수 이름·반환 형태를 JSON 시절과 같게 유지**했다. `_summary_by_sql`·`slide_label` 머리말에 "비싸다/싸다" 를 적어 뒀다 |
| `web/viewer/antarctica.py` · `korea.py` | 미리 구운 해안선(EPSG:3031 · 5179). 투영식이 `tools/proj*.py` 와 **두 곳에 있다 — 함께 고쳐야 한다** |
| `check_db.py` | **DB 무결성**. 검사 단위가 **슬라이드**다. 1초. **7번이 층을 본다** — 소속 없는 관찰을 여기서 잡는다 (063) |
| `judge.py` | 판정 규칙. **여기가 유일한 정의다** (torch 없이 돈다) |
| `zen_meta.py` | ZEN XML → 배율. **대물렌즈는 40x 로 고정 계산**한다 (015) |
| `runlog.py` | `Run`·`RunBatch` 이력. 새 실행이 같은 종류의 오래된 `running` 을 닫는다 |
| `batch_runs.py` | 흩어진 실행을 되돌아가 묶는다. `--auto` 는 규칙이지 진실이 아니다 |
| `prune_detections.py` | 한 묶음 안에 같은 이미지로 겹쳐 쌓인 검출을 정리. **묶음을 넘어서는 안 지운다** |
| `rebind.py` | 새 검출과 기존 교정을 다시 맺는다 |
| `export_yolo.py` | 검토 → YOLO 꾸러미. **판정을 다시 쓰지 않고 `viewer.data` 를 부른다** |
| `pr_sweep.py` | conf 를 훑어 P–R 곡선. 025 §8 의 숫자를 다시 만드는 것이 이것뿐이다 |
| `scan_nas.py` · `ingest_nas.py` | NAS 감시·수집 |
| `backup_db.py` | DB 사본 (WAL 안전). **Django 를 임포트하지 않는 마지막 안전망** |
| `db_sentinel.py` | 무결성 깃발. `/healthz` 가 읽는다 |
| `sync_backup_nas.py` | 검증된 스냅샷만 NAS 로 |
| `shrink_outcrop.py` | 노두 사진을 줄인다. **NAS 원본을 제자리에서 바꾼다** — `--dry-run` 이 기본이고 사본을 먼저 뜬다 (063) |
| `tighten_bbox.py` | bbox 본체 정렬 — **미적용** |
| `verify_db.py` | DB ↔ JSON 대조. **이전 직후용.** 지금은 어긋나는 것이 정상이다 |
| `deploy/host/dbsync.sh` · `dbrun.sh` | DB 를 만지는 스크립트가 들어가는 **유일한 문** (9.2절) |
| `deploy/poll_nas.sh` | 1분 폴러 |

읽을 devlog:

| 문서 | 무엇 |
|---|---|
| `20260730_P02_db-schema.md` | DB 설계와 근거. **DB 작업 전에 읽을 것** |
| `docs/20260804_db-specification.md` · `db-erd.md` | **DB 명세와 ERD.** 스키마 전체를 한자리에서 보려면 여기 (08-04 신설) |
| `20260803_P04_yolo-training.md` · `023` · `025` | 학습 계획 · 자료 꾸러미 · 첫 판 성적 |
| `20260731_015_scale-mismatch.md` | 배율 사고. **계측이 사람 눈과 어긋나면 계측을 의심할 것** |
| `20260731_016_data-safety-contract.md` | `.guides` 규약 대조. 못 지키는 조항까지 적어 뒀다 |
| `20260802_018_subpath-on-80.md` | 80 의 `/DiaRUGA/` 으로 옮긴 것. **URL 을 만드는 곳이 어디어디인지** |
| `20260803_018_separability-metric.md` | 그룹핑 지표를 두 번 고친 것. **꼬리 통계는 표본 수에 딸려 간다** |
| `20260803_027_engine-view-wiped-reviews.md` | "읽기 전용" 이라 적어 놓고 교정 37건을 지운 것 |
| `20260804_034_smoke-and-sentinel.md` | 무결성 깃발 · smoke · 백업 lane |
| `20260804_036_snapshot-into-container.md` | DB 를 만지는 문을 하나로 |
| `20260804_037_counted-classes.md` | 목록의 검출 수는 세는 분류만 (`ClassDef.counted`) |
| `20260804_038_chaetoceros.md` | 분류를 더하니 **"테이블에 넣으면 끝" 이 아니었다.** 무엇이 코드에 남아 있었나 |
| `20260804_039_fragment-family-colors.md` | 색상은 본체, 진하기는 온전한가 |
| `20260804_040_class-hotkeys.md` | 단축키 · 약칭 · **분류를 더할 때 채울 것 목록** |
| `20260804_041_diaruga-identity.md` | 이름을 DiaRUGA 로, 로고를 심다 |
| `20260804_042_release-v030.md` | 037~041 을 한 판으로 낸 기록. **되돌리기의 실제 모양** |
| `20260806_063_layers-and-locality.md` | **층을 말과 맞춘 것.** 사고 하나에서 시작해 `Sample` 을 뽑기까지 · 자동 생성 마이그레이션이 자료를 지우려 한 것 · 눌러 보는 시험이 잡은 조용한 고장 넷 |
| `20260805_048_rename-to-diaruga.md` | 이름을 한 겹으로. **`diatom` 을 남긴 자리와 그 이유**, 안전망이 이름으로 대상을 찾다 같이 눈먼 것 |
| `20260807_085_test-instance.md` | 판을 내보내기 전에 눌러 볼 자리. **테스트를 운영과 갈라 두는 넷**과 각각의 이유 |
| `20260807_086_crops-dead.md` | 화면 둘이 v0.8.0 내내 500 이었던 것. **URL 을 덮는 시험과 갈래를 덮는 시험은 다르다** |
| `docs/20260803_diatom-analysis-system-progress-4.md` | 4차 진척 보고서 (devlog 018~025 종합) |

마이그레이션은 `0029_batch_recipe` 가 마지막이다 (079 · `v0.8.1`).
**`v0.8.2` 는 스키마를 안 건드렸다** — 뷰어만 올리면 되는 판이다.
`0025`~`0029` 가 08-07 에 나갔다 — 교정을 묶음에 매달고(0025), 검토 대상을
묶음이 스스로 알게 하고(0026·0027), 검토 완료를 묶음마다 남기고(0028),
묶음마다 조리법을 두었다(0029). 그 앞의 `0024_locality_and_sample` 은 (063 ·
`v0.7.0`).
**자동 생성본을 그대로 쓰면 안 됐다** — Django 가 `Core` 를 지우고 `Locality` 를
빈 채로 만드는 순서를 내서, 그대로 돌렸으면 소속이 통째로 날아갔다. 손으로
**새 표 → `RunPython` 자료 이동 → 옛 칸 걷기** 로 잡았고 `reverse` 도 적었다.

08-04 에는 다섯이 한 판(`v0.3.0`)으로 나갔다 — `0013 counted` ·
`0014 chaetoceros` · `0015 fragment_family_colors` · `0016 hotkey` · `0017 short`.
하나씩 냈으면 배포·스냅샷·기동 게이트를 다섯 번 지났을 일이라 쌓아 두고 갔다.

---

## 6. 알아 두면 좋은 것들 (밟기 쉬운 곳)

**데이터·스키마**

- **교정은 `Candidate` 가 아니라 `mask_key` 에 붙는다.** FK 로 매면 재검출에서
  사람의 판단이 사라진다
- **모든 교정 행이 `geom` 에 기하를 들고 있다.** 검출기가 바뀌어도 읽혀야 하고,
  지운 것도 학습의 어려운 음성 표본이다 — 실제로 P04 에서 그렇게 쓰였다
- **검출은 덮어쓰지 않고 쌓는다.** `Detection.is_current` 가 뷰어가 볼 것을 가리킨다
- **검출·교정은 `Image` 에 붙는다** (055). `Detection.target`·`frame` 은 없다 —
  무엇에 붙은 검출인가는 `image.kind`, 어느 프레임인가는 `image.frame` 이 말한다.
  **`mask_key` 가 문자열인 것은 이것과 다른 이야기다** — 그건 소유 방향의 선택이고
  `Image` 를 뽑아도 그대로다
- **캐시된 dict 를 고치지 말 것.** `detection_for()` 가 후보를 복사한 뒤 교정을
  얹는 이유다 (005 §4.2)
- **프레임 이름은 슬라이드끼리 겹친다.** 카메라 일련번호라 같은 날 이어 찍으면
  번호대가 이어진다 — 260803 두 슬라이드에서 **143종이 겹쳤다**. 이름만으로 찾으면
  안 된다(`find_viewpoint(stem, slide=…)`). `Frame` 에 `(slide, name)` 유일 제약이
  **이미 있었다** — 제약이 있다는 것이 곧 "이름만으로는 못 찾는다" 는 뜻이다 (031)
- **`Site.area`(한국/남극)와 `Site.region`(해역·만 이름)은 다른 칸이다.** 이름이
  비슷해서 반드시 헷갈린다 (024)
- **`Locality.kind`(시추코어/노두)와 `Locality.collect_kind`(채취 방식)도 다른
  칸이다.** 앞은 두 갈래이고 뒤는 사람이 적는 자유 문자열(`gravity core`)이다 —
  예전 `Core.kind` 가 뒤엣것이었다 (063)
- **소속(지점·시료)은 `Slide` 에 없다.** `slide.locality`·`slide.depth_cm`·
  `slide.sample_kind` 는 시료를 거쳐 가는 **지름길 property** 이고, 질의에는
  `sample__locality__site` 로 조인한다. 소속이 없는 관찰에서 터지지 않게 한 겹
  막아 두었다 (063)
- **자동값이 사람이 채운 것을 덮으면 안 된다.** `update_or_create` 의 `defaults`
  에 `None` 을 실으면 다시 반입할 때 사람이 넣은 소속이 지워진다 — 폴더에서 못
  읽은 것은 **아무것도 안 쓰는** 것이 맞다 (`sample_fields`). `obs_label` 은
  그렇게 해 놓고 `core` 는 안 그랬다가 당했다 (063)
- **`Slide.sample` 이 `SET_NULL` 이다.** 시료를 지우면 예외가 안 나고 관찰이
  조용히 소속을 잃는다 — 막는 자리는 `manage_data.deletable` 하나뿐이고,
  `check_db.py` 7번이 그것을 센다 (063)

**화면 — 아무 일도 안 하고 "됐다" 고 말하는 것 (063)**

- **저장이 성공으로 보이는데 아무것도 안 바뀌는 갈래를 남기지 말 것.** 속성
  편집이 지역 코드가 비면 지점을 찾는 갈래를 통째로 건너뛰면서 "저장했습니다"
  를 냈다 — 사람이 같은 일을 몇 번이고 다시 한다
- **남의 행에 붙기만 할 때 폼의 빈 칸을 저장에 쓰지 말 것.** 그 칸들은 소속이
  없던 시점에 그려져 비어 있다 — 그대로 쓰면 채워 둔 좌표·해역이 지워지고
  그 행을 쓰는 관찰 전부가 함께 당한다
- **브라우저 기본 `[hidden] { display: none }` 은 특이도 (0,0,1) 이다.** 요소에
  클래스 규칙이 걸려 있으면 진다 — `.tools` 에 이어 `.tile` 에서 두 번째다
- **폼에 같은 `name` 이 둘이면 Django 는 뒤엣것을 집는다** — 지점이 "모르는 층"
  이 되어 조용히 안 만들어졌다
- **`<script>` 가 어느 블록 안에 들어갔는지 볼 것.** `{% block title %}` 안에
  들어가 `<title>` 속에서 한 번도 안 돌았다
- **개발 서버가 템플릿을 캐시한다** (`--noreload`). 고쳤는데 안 바뀌면 그것부터

**성능 — 같은 실수를 세 번 했다**

- **개체 하나 재려고 이미지 전체를 훑지 말 것.** `shape_metrics` 26배(030),
  `largest_body` 3배(033). 개체는 이미지의 0.3% 다. **`bbox` 는 부르는 쪽에서
  받는다** — 안 받고 `np.nonzero` 로 찾으면 아끼려던 비용을 그대로 쓴다
  (두 번 다 그렇게 만들었다가 되돌렸다)
- **개수를 세려고 자료를 물질화하지 말 것.** 목록 화면이 1.32 → 0.31초, 계측 표가
  1.35 → 0.48초. 셋 다 "원하는 것보다 훨씬 많이 가져와서 파이썬에서 버리는" 모양
  이었다 (022)
- **값이 안 바뀐 것을 확인하는 편이 빠르게 만드는 것보다 어렵다.** `judge` 가 그
  값으로 판정하므로 달라지면 검출·문턱 이력과 어긋난다. `cv2.fitEllipse` 는 좌표를
  옮기기만 해도 1e-7 이 달라지고 그것이 1 픽셀로 증폭된다 (030)
- **유일 제약을 옮기면 그것을 짚어 쓰던 질의도 함께 옮긴다** (058). 055 가 교정의
  열쇠를 `(viewpoint, mask_key)` → `(image, mask_key)` 로 옮겼는데 목록 집계는
  `viewpoint` 로 상관하고 있었다 — **코드는 한 글자도 안 바뀌었는데 밟고 서 있던
  인덱스가 없어져** 첫 페이지가 0.5 → 2.0초가 됐다. 053 의 반대편이다
- **성능을 잰 파일에 인덱스를 만들어 놓고 잊지 말 것** (058). 시험용 인덱스가
  든 사본을 다시 복사해 쓰다가 "파일 배치 탓" 으로 30분을 갔다. `VACUUM` 이
  파일을 절반으로 줄이고도 0 ms 도 못 줄인 것이 오히려 답을 좁혀 줬다

**GPU·의존성**

- **`except` 블록 안에서 `empty_cache()` 는 아무것도 못 비운다.** 역추적이 실패한
  호출의 지역변수(= 그 큰 GPU 텐서)를 붙들고 있다. 재시도는 `except` **밖으로**
  뺀다 (043)
- **`retina_masks=False` 는 값을 바꾼다.** 메모리는 1/4 이지만 모든 개체의 경계가
  달라진다 (마스크를 자른 뒤 확대 vs 확대한 뒤 자름)
- **8 GB 카드에 안 들어가는 프레임이 1,318장 중 1장 있다.** 검출 110개짜리 프레임이
  카드의 91% 를 쓴다. `Run.status='partial'` 로 표시된다 — **코드 문제가 아니라
  장비 문제다**
- **스키마를 조이는 마이그레이션은 뷰어와 파이프라인 판을 함께 올린다.** 판을
  갈라 둔 것(026)이 이번엔 반대로 문다 — 칼럼을 걷었는데 옛 파이프라인 이미지가
  거기에 INSERT 하면 폴러가 선다. **조인 사본에 파이프라인 컨테이너를 붙여
  먼저 돌려 볼 것** (055 에서 그렇게 잡았다)
- **함께 올려야 하는 축이 스키마 말고 하나 더 있다 — 값의 모양을 정하는 규칙이다**
  (057). 슬러그·경로·이름 규칙은 **파이프라인이 만들고 뷰어가 쓴다.** 규칙을
  고치고 파이프라인 이미지를 안 올리면 옛 규칙이 계속 값을 만들고, 그 값이 DB 에
  앉는 순간 **그것을 읽는 화면이 전부 죽는다** — 괄호가 든 슬러그 하나로 목록이
  `NoReverseMatch` 를 내 **뷰어 전체가 500** 이었다. 들어오는 값이 404 가 되는
  것과 나가는 값을 못 만드는 것은 고장의 크기가 다르다. **`/healthz` 는 링크를
  안 만들어 이 고장을 통과시킨다**
- **폴러를 세울 때 crontab 을 고치지 않는다.** `flock /tmp/DiaRUGA-poll.lock <명령>`
  이면 그 사이 실행이 조용히 물러난다 — `poll_nas.sh` 가 이미 `flock -n` 이다
- **`dbrun.sh` 로 돌릴 스크립트는 `check_db.py` 의 머리(`DIARUGA_APP`)를 베껴
  온다.** 자기 옆의 `web/` 을 보게 짜면 컨테이너에서 `No module named
  'diarugaweb'` 로 죽는다 (055)
- **`ultralytics` 는 `--no-deps` 로 넣는다.** torch 상한(`<2.10`)이 진짜가 아니라는
  것을 실측으로 확인했고, GUI 판 `opencv-python` 이 딸려 오면 슬림 이미지에서
  `libxcb.so.1` 로 죽는다. **의존은 `requirements-yolo.txt` 에 손으로 적었다 —
  ultralytics 를 올릴 때 함께 봐야 한다** (032)

**뷰어 · 확인 방법**

- **자동 시험이 먼저다** (P08 — 3.3 의 상자). `python web/manage.py test viewer`
  로 333개가 84초에, `--exclude-tag browser` 면 262개가 2.5초에 돈다.
  **고치고 나면 이것부터.** 시험이 제 DB 를 만들고 버리므로 `dbrun.sh` 를 안
  거치고, 운영 DB 나 `/data3` 를 가리키면 스스로 선다
- **URL 을 덮는 것과 갈래를 덮는 것은 다르다** (086). `/crops/`·`/detections/`
  를 여는 시험이 셋이나 있었는데 **자료가 전부 합성본이라** 프레임 갈래를 한 번도
  안 밟았고, 그 사이 두 화면이 v0.8.0 내내 500 이었다. 화면 시험을 더할 때는
  **자료가 어느 갈래로 가는지**를 본다
- **브라우저가 있다** (045 — 3.3 의 상자). 가볍게는 Django 테스트 클라이언트로 URL 을
  때려 보고, JS 는 **렌더한** 인라인 스크립트를 뽑아 `node --check` 로 파싱한다.
  **눌러야 아는 것은 playwright 로 눌러 본다** — 자동 시험은 테스트 DB 위에
  서버를 띄우고(그래서 사본이 필요 없다), **손으로 띄울 때는 반드시 사본 DB 에**
- **CSS 로 감췄다고 믿지 말고 `getComputedStyle` 로 확인한다.** `.tools { display:
  none }` 이 한 번도 먹은 적이 없었다 — `base.html` 의 `.detview .tools`
  (`display: flex`)가 특이도에서 이긴다. **지금은 지운** `/engine/`(075) 이 도구를
  감춘 줄 알고 계속 내보이고 있었고, "검토 도구만 잠가 두었습니다" 라고 적은
  화면도 그랬다.
  **base.html 이 그 요소를 어떤 선택자로 잡고 있는지 먼저 볼 것** (051)
- **속성에 값이 들어 있는 것과 그 값이 유효한 것은 다르다.** SVG 경로에 10 KB 가
  멀쩡히 들어 있는데 화면은 백지였다(숫자 한가운데 들어간 공백 하나). 길이만 세지
  말고 **파싱까지 해 볼 것** (021)
- **템플릿을 가를 때는 `div` 짝이 아니라 렌더 결과의 중첩을 본다.** `</div>` 하나가
  남아 카드 판이 `.dsview` 바깥으로 밀려났는데 HTML·CSS 문자열은 다 맞았다 (024)
- **Django 의 `{# #}` 는 한 줄짜리만 주석이다.** `{% comment %}` 를 쓸 것.
  태그는 **JS 주석 안에서도 실행된다**
- **부모를 `extends` 한 템플릿에서 `block` 바깥에 적은 것은 렌더되지 않는다.**
  `<style>` 이 한 번도 먹은 적이 없었다 — 예외도 경고도 없는 종류의 고장이다 (020)
- **눌러도 아무 일이 없는 버튼은 "눌렀는데 안 되네" 로 읽힌다.** 빈 권역의 보기방식
  버튼도, 갈 곳 없는 안내문도 같다 — 알리기만 하고 길을 안 내면 막다른 길이다 (029)

**측정·비교**

- **두 경로를 비교할 때는 입력이 같은지 먼저 확인한다.** 같은 코드가 2.4배
  느려 보여 gunicorn·nginx·워커 수를 세 번 의심했는데, curl 본문에 손으로 적은
  문턱이 기본값과 달랐다. **계측값이 이상하면 계측 자체를 먼저 의심할 것** (022)
- **숫자를 비교할 때는 양쪽이 같은 층인지 본다.** 검토 부담을 SAM2 원시 마스크
  152개와 비교해 "19배 줄어든다" 고 적었는데, 사람이 실제로 보는 것은 judge 를
  통과한 25.1개다. **19배가 아니라 3.1배**였다 (025)
- **고친 코드가 실제로 도는지부터 확인한다.** "고쳤는데 안 빨라진다" 가 두 번
  나왔는데 컨테이너가 이미지 안의 옛 `/app` 을 돌고 있었다 (033)
- **DB 의 시각을 그냥 찍으면 UTC 다.** `USE_TZ=True` 라 `Run.started_at` 은 UTC 로
  저장되고, `f"{r.started_at:%H:%M}"` 은 그것을 그대로 낸다 — KST 로 보려면
  `django.utils.timezone.localtime()` 을 거쳐야 한다. **`git log` 는 KST 로 찍힌다.**
  둘을 나란히 놓고 "이 실행이 그 수정 전인가 후인가" 를 읽다가 **아홉 시간이
  어긋나 결론이 반대로 나왔다** — YOLO 실행을 전부 "수정 전" 으로 판단했는데
  실은 절반이 수정 후였고, 그래서 못 찾던 2.8배가 자료 안에 이미 있었다.
  화면은 멀쩡하다(템플릿이 알아서 바꾼다) — **함정은 DB 를 직접 조회하는
  진단 스크립트 쪽에만 있다.** 036 이 백업 파일 이름에서 겪은 것과 같은 종류다
- **배율은 슬라이드마다 다를 수 있다.** 검사도 문턱도 슬라이드 단위다. 한 슬라이드
  **안에서** 갈라지는 것이 사고다 (017)
- **`--scale` 을 빠뜨리지 말 것.** 절반 해상도로 검출되고 `um_per_px` 가 2배로
  기록된다. 폴러와 같은 인자를 쓸 것
- **`verify_db.py` 는 임포트하면 `ImportError` 다** (실행 전용으로 막아 뒀다)

---

## 7. 시료에 대해

남극에서 채취한 시추코어의 규조류가 주다. 폴더명이 `<지역>-<코어> <깊이>cm` 꼴이면
파이프라인이 `Site`·`Core`·`Slide.depth_cm` 으로 갈라 담는다 — *같은 코어에서
깊이에 따른 군집 변화*와 *지역별 차이*가 분석 목적이다.

```
남극   RS23-GC03      71 / 231 / 369 cm     (로스해 · 추정)
       WAP13-GC47    116 / 450 cm           region = "Bigo Bay"
       AM22-GC10B     25 / 261 cm           region = "아문젠해" — 서남극/동남극이 갈리는 가정
       GC03-C1        72 / 816 cm           Elephant Island
한국   BP09-0901                            북평분지(동해)
```

**BP09-0901 은 별개 갈래로 다룬다** (2026-08-05 결정). 한국 북평분지 시료라
남극 시추코어와 퇴적 환경도 군집도 다르다. 그래서:

- **집계·비교에서 남극 시료와 섞지 않는다.** 화면의 `[한국 | 남극 | 전체]`
  갈래가 이미 그 구분을 들고 있다 — DB 의 `Site.area` 가 `kr`/`ant` 다
- **YOLO 학습에서도 당분간 따로 간다.** 지금 모델(v1)에는 BP 가 한 장도 안
  들어가 있다(4.2절). 검토가 채워져도 **그대로 섞지 않는다** — 넣을 때는 갈래를
  나눠서 성적을 따로 본다
- **밀린 검토를 셀 때 갈래를 밝힌다.** 전체 109 중 85가 BP 라서, 갈래를 안
  밝히면 "남극 작업이 크게 밀렸다" 로 잘못 읽힌다 (남극 기준으로는 24)

`Site.region` 이 아직 비어 있다 — `/d/260803_bp09-0901/edit/` 에서 "북평분지" 를
붙이면 화면에도 지역 이름이 뜬다.

**08-03 에 한국 시료가 들어왔다.** `BP09-0901` 은 폴더명이 `<지역>-<코어> <깊이>cm`
꼴이 아니어서 코어가 안 붙은 채 들어왔고, `/d/<slug>/edit/` 에서 지역을 **새로
만들어 붙였다**. 그래서 목록에 `[한국 | 남극 | 전체]` 갈래가 생겼다 — `전체` 는
권역이 아니라 "거르지 않는다" 는 뜻이고, **지역이 안 붙은 슬라이드를 볼 수 있는
유일한 곳**이다.

**`AM22` 가 아문센해인지 Amery(동남극)인지 아직 확인 안 됐다.** 아라온의 서남극
주요 해역 셋(로스해·남극반도·아문센해) 중 나머지 하나로 **가정**한 것이다. 코어
대장에서 확인이 필요하다. `Site.lat/lon` 이 있는 곳(AM22·WAP13·GC03·BP09)은
**해역 한가운데를 찍은 대략값**이고 화면에 그렇게 표시된다. `RS23` 은 좌표가 없다.

---

## 8. 이 머신

작업 환경이 이전 서버에서 이 머신으로 바뀌었다(2026-07-31).

### 8.1 GPU 는 한 장이다

```
01:00.0  NVIDIA GA104 [GeForce RTX 3060 Ti]   VRAM 8 GB   드라이버 580.173.02
0a:00.0  AMD Cezanne [Radeon Vega]            내장, 화면용
```

이전 서버는 두 장이라 `CUDA_VISIBLE_DEVICES=0` 을 걸었지만 **여기서는 걸 이유가
없다.** VRAM 이 8 GB 뿐이라 파이프라인을 상주시키지 않는다(9.3절).

YOLO 추론은 여기서 **0.71 GiB(9%)** 다 — 폴러와 공존한다. 문제가 되는 것은 검출이
100개를 넘는 프레임의 `retina_masks` 뿐이다 (6절 "GPU·의존성").

> 이전 직후 `nvidia-smi` 가 죽고 `torch.cuda.is_available()` 이 `False` 였던 것은
> `apt upgrade` 가 드라이버를 올렸는데 실행 중인 커널에 옛 모듈이 물려 있어서였다.
> **재부팅으로 끝났다.** 같은 증상(NVML mismatch / CUDA error 804)이 다시 보이면
> 이것을 의심할 것.

### 8.2 학습은 다른 기계에서 한다

**jikhanserver (RTX 8000 48 GB × 2).** 11m·`imgsz=1280`·batch 8 이 **7 GB · 20분**
이다. 다만 두 GPU 를 ocrserver 가 쓰고 있어(chandra OCR · Qwen3-32B) 자리를 비워야
한다 — 한 번은 사용자 승인을 받고 GPU0 의 chandra 를 내렸다 되살렸다.

꾸러미는 rsync + `SHA256SUMS` 전수 검증으로 옮긴다. **학습은 반드시 꾸러미
디렉토리 안에서 실행한다** — ultralytics 가 `data.yaml` 의 `path:` 를 yaml 위치가
아니라 **실행 디렉토리** 기준으로 푼다.

### 8.3 venv 는 `~/venv/DiaRUGA` 이다

README 의 `.venv` 가 아니다. Python 3.12.3 · `torch 2.13.0+cu126` ·
`torchvision 0.28.0+cu126` · `SAM-2 1.0` · Django 5.2.16 · opencv-headless.

requirements 는 넷으로 갈라져 있다 — 호스트는 `requirements.txt`, 컨테이너가
`-web`(Django·pillow·gunicorn)과 `-pipeline`(torch·SAM2)을 나눠 쓰고,
`-yolo`(ultralytics, `--no-deps`)가 파이프라인 위에 얹힌다.

**호스트 venv 로 DB 를 만지지 않는다** — 9.2절.

---

## 9. 배포 (P03)

자세한 것은 `devlog/20260731_008_containerize.md`, 계획은 `20260731_P03_*`,
스크립트는 `20260803_019_deploy-scripts.md`.

### 9.1 어디서 무엇이 도는가

```
바깥 :80 /DiaRUGA/     ─nginx─▶ 127.0.0.1:8090 ─▶ diaruga-web-1      운영
바깥 :80 /DiaRUGATest/ ─nginx─▶ 127.0.0.1:8091 ─▶ diaruga-test-web-1 테스트 (085)
바깥 :9090 ──301──▶ /DiaRUGA/                       │
                                                    │
/srv/DiaRUGA/    db/  scripts/  bin/  compose  .env ─┤  배포
/srv/DiaRUGA/test/  db/(사본) outcrop/(사본) compose .env  테스트 인스턴스
/data3/DiaRUGA/  photos/<촬영일>/<슬라이드>/  ───────┘  사진·산출물·백업·HF 캐시·로그
                stacked/ out/ backup/ hf/ logs/ datasets/ docker/
```

> **테스트 인스턴스는 지금 내려 있다** (08-07 21:15, v0.8.2 가 운영에 올라간
> 뒤). 자리는 그대로 두었다 — `/srv/DiaRUGA/test/` 와 nginx 조각이 남아 있어
> **다음 판 미리보기는 `up -d` 한 줄**이다. 그래서 지금 `/DiaRUGATest/` 는
> **502 다**(받는 쪽이 없다). 운영과는 별개 location 이라 서로 안 건드린다.
>
> **테스트 인스턴스는 사본 DB 로 돈다** (085). 운영 `db/` 는 그 컨테이너에
> **아예 마운트하지 않았다** — 경로를 잘못 적어도 닿을 수 없게. 노두 사진도
> 사본이다(화면에서 지우면 파일이 실제로 지워지는 자리다). 함께 쓰는 것은
> `/data3/DiaRUGA` 뿐이고 사진은 읽기만 한다.
>
> **오리진이 같아 `localStorage` 를 운영과 공유한다** — 테스트에서 레이어를 끄면
> 운영 화면에서도 꺼진 채로 보인다. 두 화면을 가르는 표시는 아직 없다.
>
> ```bash
> # 다음 판을 미리 볼 때: .env 의 IMAGE_TAG 를 바꾸고, DB 사본을 새로 뜬다
> deploy/host/dbrun.sh backup_db.py --note before-testdeploy
> cp /data3/DiaRUGA/backup/manual/<새 사본>.db /srv/DiaRUGA/test/db/DiaRUGA.db
> cd /srv/DiaRUGA/test && docker compose up -d web      # 올린다
> cd /srv/DiaRUGA/test && docker compose down           # 내린다
> ```
>
> **사본을 새로 뜨는 것이 요점이다.** 놔두면 옛 자료로 새 판을 보게 되고,
> "고쳤는데 안 바뀐다" 가 거기서 나온다. `cp` 는 `backup_db.py` 가 만든
> **스냅샷 파일**에만 쓴다 — 도는 DB 를 `cp` 하면 WAL 이라 불완전한 사본이 된다.
>
> nginx 조각은 `deploy/nginx/DiaRUGATest-subpath.conf`, 설치·되돌리기는
> `/srv/DiaRUGA/test/install-nginx.sh` (root 로 한 번).

**저장소는 굽고, `/srv/DiaRUGA` 은 돌린다.** 컨테이너 안팎의 경로가 같아 명령을
그대로 옮겨 쓸 수 있다.

```bash
cd /srv/DiaRUGA && docker compose up -d web                    # 뷰어
cd /srv/DiaRUGA && docker compose run --rm pipeline <명령>      # GPU, 일회성
docker compose -f deploy/docker-compose.yml build web         # 이미지 굽기 (저장소)
/srv/DiaRUGA/bin/deploy.sh <태그>                               # pull → 스냅샷 → 교체 → 게이트
```

**지금 도는 판**: `IMAGE_TAG=v0.8.2` · `PIPELINE_TAG=v0.5.1` (08-07).
뷰어 판은 `.env` 와 `/healthz` 가 알려 준다 — 이 문서보다 그쪽이 늘 옳다.

> **이미지를 Docker Hub 로 올리는 것은 아직 사람이 한다.** `test.yml` 은 태그를
> 밀면 올린다고 적혀 있지만 **저장소에 `DOCKERHUB_USERNAME`·`DOCKERHUB_TOKEN`
> 시크릿이 없어** 그 단계가 `Username and password required` 로 실패한다.
> v0.8.0~v0.8.2 는 전부 이 머신에서 손으로 올라갔다:
>
> ```bash
> DIARUGA_TAG=v0.8.2 docker compose -f deploy/docker-compose.yml build web
> DIARUGA_TAG=v0.8.2 docker compose -f deploy/docker-compose.yml push  web
> ```
>
> 그래서 **"시험을 통과한 것만 이미지가 된다" 는 관문이 실제로는 사람 손에
> 걸려 있다.** 시크릿 둘을 넣으면 문서대로 돌아간다. 넣기 전까지 **태그마다 CI 에
> 빨간 줄이 남는 것이 정상**이다 — 시험 겹은 초록이고 굽기 겹만 빨갛다.

> **되돌릴 때: 이미지는 돌아가지만 마이그레이션은 따라 내려가지 않는다.**
> `deploy.sh <옛 판>` 은 컨테이너만 갈아 끼운다. 옛 판은 새로 생긴 칼럼
> (`counted`·`hotkey`·`short`)을 모르지만 **읽는 데는 지장이 없다** — Django 는
> 모델에 없는 칼럼을 무시한다. 스키마까지 되돌려야 하면 스냅샷을 쓴다.
>
> **마이그레이션이 걸린 판을 낼 때는 `manual/` 에도 한 장 뜬다.** `deploy.sh` 가
> 뜨는 `pre_deploy/` 는 배포마다 도는 자리라 `--keep 20` 에 밀려 언젠가
> 사라진다 — 되돌아갈 지점이 밀려나면 안 된다 (042).
>
> ```bash
> deploy/host/dbrun.sh backup_db.py --note before-v0.3.0   # 배포 전, 손으로
> ```
>
> **스크립트를 고쳤으면 배포 전에 `/srv` 로 옮긴다.** `dbsync.sh --list` 가
> 어긋난 것을 알려 준다 — 저장소는 만들고 `/srv` 가 돈다.

**두 판은 따로 논다.** 처음에는 둘 다 `IMAGE_TAG` 를 썼는데, 뷰어 판을 올리는 것이
자동으로 파이프라인을 없는 판으로 가리키게 만들어 **폴러가 4시간 반 동안 524번
실패했다**(026). 그때 로그는 "NAS 가 내려갔는가" 라고 적었다 — **짐작해서 쓴 진단이
진짜 원인을 가렸다.** 지금은 가릴 수 있는 만큼만 가르고 못 가르면 "정찰 실패" 로 둔다.

### 9.2 DB 를 만지는 것은 문 하나로만 들어간다

```bash
deploy/host/dbsync.sh check_db.py     # 저장소 → /srv/DiaRUGA/scripts (옆 모듈까지 따라간다)
deploy/host/dbrun.sh  check_db.py     # 컨테이너 안에서 돈다
deploy/host/dbsync.sh --list          # 옮겨 둔 것이 저장소와 어긋났는가
```

호스트 venv 로도 같은 DB 를 열 수 있지만 그러면 **같은 파일을 두 벌의 환경이
만진다 — 두 번 당했다.** 컨테이너의 낡은 `models.py` 가 새 칼럼을 몰라 NAS 반입이
죽었고(029), root 로 돈 컨테이너가 `DiaRUGA.db` 소유자를 바꿔 호스트 스크립트가
못 쓰게 됐다.

- **`/app` 은 이미지 것 그대로 둔다.** 저장소를 `/app` 에 덮어 물리면 편집 중인
  작업 트리로 프로덕션 DB 를 만지게 된다 — 막으려던 것을 이름만 바꿔 다시 만드는
  꼴이다. 밖에서 물리는 것은 **스크립트 파일뿐**이고 읽기 전용이다
- **딸려 오는 제약**: `/app` 은 `IMAGE_TAG` 가 가리키는 판이라 **아직 배포하지 않은
  모델 변경이 필요한 스크립트는 여기서 못 돈다.** 조용히 어긋나는 것보다 낫다
- **아직 문 밖에 있는 것**: 시간별 백업 cron 이 호스트 venv 로 `backup_db.py` 를
  부른다(9.6절). `backup_db.py` 는 Django 를 안 쓰고 sqlite3 백업 API 만 쓰는
  마지막 안전망이라 그대로 두었지만, **문이 하나라는 원칙에는 예외다** — 옮길지
  결정이 필요하다

### 9.3 데이터가 저장소 밖으로 나갔다

- 위치는 `.env` 의 `DIARUGA_DATA_ROOT` 가 알려 준다 (`.env.template` 이 견본)
- `photos/` 아래는 `<촬영일>/<슬라이드>/` 두 단계 — NAS 구조와 1:1. 평탄하게 펴면
  같은 슬라이드를 다시 촬영했을 때 이름이 부딪힌다
- **`review/`·`groups_*.json` 은 저장소에 남아 있다** — git 이 추적하는 감사 기록
- **DB 디렉토리(`db/`)만 마운트한다.** `/srv/DiaRUGA` 을 통째로 물렸더니 `.env` 의
  `SECRET_KEY` 가 컨테이너에서 읽혔다 — 잘라 냈다(016). 파일 하나만 물려도 안 된다
  (`-wal`·`-shm` 이 컨테이너 안쪽에 생겨 WAL 을 공유하지 못한다)
- **컨테이너는 `1000:1000` 으로, `TZ=Asia/Seoul` 로 돈다.** 시간대를 빠뜨리면
  컨테이너가 UTC 로 돌아 **사본 이름이 아홉 시간 어긋나고**, 정리 규칙이 이름순
  정렬이라 **가장 새 사본을 지우고 옛것을 남긴다**(036). `dbtool` 은
  `environment:` 로 앵커를 덮으므로 거기 다시 적어야 한다

### 9.4 파이프라인은 일회성으로만 돌린다

`profiles: [manual]` 로 묶어 `up` 에 딸려 뜨지 않는다. 상주 워커로 두면 PyTorch
캐싱 할당자가 최대치 VRAM 을 프로세스가 죽을 때까지 물고 있다.

컨테이너를 `--gpus all` 로 띄우는 것 자체는 VRAM 을 쓰지 않는다(77 MiB → 77 MiB).
메모리를 잡는 것은 컨테이너가 아니라 그 안에서 CUDA 컨텍스트를 만드는 프로세스다.

### 9.5 사내망이 TLS 를 가로챈다

KOPRI 망이 `download.pytorch.org` 를 자체 CA 로 다시 서명한다. 파이프라인 이미지
빌드가 `CERTIFICATE_VERIFY_FAILED` 로 죽으면 `deploy/ca/README.md` 를 볼 것.
`pip` 은 시스템 CA 저장소를 보지 않아 `PIP_CERT` 를 함께 줘야 한다.

### 9.6 nginx — 80 의 서브경로다

**컨테이너는 루프백에만 붙는다.** 바깥으로 나가는 문은 nginx 하나뿐이다.

| 파일 | 무엇 |
|---|---|
| `/etc/nginx/snippets/DiaRUGA-subpath.conf` | 실제 서빙. 원본은 `deploy/nginx/DiaRUGA-subpath.conf` |
| `/etc/nginx/sites-enabled/phyloserver` | 그 스니펫을 `include` 하는 한 줄이 여기 있다 |
| `/etc/nginx/sites-enabled/DiaRUGA` | `:9090` → 301. 원본은 `deploy/nginx/DiaRUGA.conf` |
| `*.bak-048` | 이름 변경 전의 설정. 되돌릴 때 쓴다 |

80 은 phyloserver 블록이 `server_name 172.16.116.98` 로 잡고 있어서 **같은 블록 안에
location 으로 들어갔다.** 같은 `listen`·`server_name` 으로 server 블록을 둘 두면
nginx 가 앞의 것만 쓰고 뒤를 버린다.

- **`location ^~ /DiaRUGA/` 의 `^~` 를 지우지 말 것** (048). 옛 `/diatom/` 과
  대소문자를 틀리게 친 주소를 정식 주소로 보내는 **대소문자 무시 정규식**이 같은
  파일에 있는데, **nginx 는 접두 location 보다 정규식을 먼저 본다.** `^~` 가
  없으면 그 정규식이 정식 주소까지 잡아채 **리다이렉트 무한 루프**가 된다.
  `= /DiaRUGA`(슬래시 보정)는 정확히 일치라 정규식보다 우선이어서 안전하다
- `proxy_pass http://127.0.0.1:8090/;` 의 **끝 슬래시가 접두를 뗀다.** 빼면 앱이
  `/DiaRUGA/…` 을 그대로 받아 전부 404 다
- Django 쪽은 `DIARUGA_SCRIPT_NAME=/DiaRUGA`(`.env`) → `FORCE_SCRIPT_NAME`
- **템플릿에 절대경로를 박지 말 것.** JS 는 `base.html` 이 내보내는 `window.ROOT` 를,
  파이썬은 `reverse()` 를 쓴다 (devlog 018)
- **배포 중에는 점검 안내가 나간다.** `deploy.sh` 가 세우는 깃발이 있으면 503 ·
  "시스템 점검 중입니다", **깃발 없이 502/504 면 "연결할 수 없습니다"** 다. 깃발
  없이 죽은 것을 "점검 중" 이라고 하면 거짓말이고, 사람이 그걸 알아야 조치한다.
  `error_page 503 = /…` 처럼 `=` 를 쓰면 **상태가 200 으로 바뀐다** — 쓰지 말 것
- **phyloserver 저장소의 nginx 사본이 설치본보다 뒤처져 있다.** 그쪽에서 배포하면
  `include` 한 줄이 날아간다
- 랜딩 페이지의 카드는 `/srv/paleolab/index.html` 이다. **저장소 사본이 없다**

**nginx 가 사진을 직접 서빙하지는 않는다** — `/img?p=…&w=400` 은 즉석에서 축소본을
만들고 `/crop` 은 bbox 로 잘라낸다. 병목이 아니다.

### 9.7 백업 — cron 에 붙어 있다

```cron
20 * * * *  backup_db.py --keep 24                                       → logs/backup.log
40 4 * * *  timeout 1800 sync_backup_nas.py --newest-only --prune --photos → logs/nas-sync.log
```

SQLite 온라인 백업 API 로 뜬 뒤 `journal_mode=DELETE` 를 걸어 `-wal`·`-shm` 이
따라다니지 않게 한다. 컨테이너가 서빙하는 중에 떠도 안전하다.

**디렉토리가 종류를 가른다.** 이름 규칙으로도 가를 수 있지만 정리 glob 을 한 번
잘못 쓰면 섞인다 — 디렉토리는 glob 이 애초에 안 내려간다.

```
/data3/DiaRUGA/backup/            시간별 자동 — 24시간 rolling. 여기 것만 NAS 로 간다
                     manual/     사람이 --note 로 뜬 것 — 로테이션·NAS 대상 밖
                     pre_deploy/ deploy.sh 가 판 바꾸기 직전에 (--flat, 20개)
/nfs/temp-share/DiaRUGA/backup/   일별 오프사이트 — 계단식 보관
```

- **로테이션은 꼬리말 없는 이름만 굴린다**(`DiaRUGA_????????_??????.db`). 배포 전
  스냅샷은 `_pre-deploy-v0.1.9` 라는 꼬리말이 붙어 안 걸렸고 24장까지 쌓여 있었다 —
  **`--flat` 은 "이 디렉토리는 이 종류 전용" 이라는 뜻**이고 그때는 꼬리말이 붙은
  것도 굴린다 (036)
- **NAS 는 계단식으로 보관한다** — 7일 이내 전부 · 30일까지 주 1개 · 그 뒤 달 1개.
  **개수가 아니라 나이로 판단한다**: 개수는 주기가 바뀌면 뜻이 바뀐다
- **수동 스냅샷은 NAS 로 안 간다.** 작업 중에 되돌릴 지점이지 보관물이 아니다
- **사진도 같은 일별 실행에서 간다** — `photos_YYYYMMDD.tar`, 일주일 rolling.
  **압축하지 않는다** (JPEG 이라 5% 밖에 안 준다). 지금 4.0 GB 에 40초
- **백업 사본은 검증을 통과한 뒤에 제 이름을 받는다.** 뜨는 중에는 `.part` 다.
  반쯤 쓴 파일이 `DiaRUGA_*.db` 라는 이름을 달면 정리 glob 에 걸려 **가장 새
  파일로 살아남고 멀쩡한 사본을 밀어낸다** (034)
- **실패하면 셋을 다 한다** — 정리를 건너뛰고, `.corrupt` 로 증거를 남기고, DB 옆에
  `INTEGRITY_FAIL` 깃발을 세운다. `/healthz` 가 그것을 읽어 `degraded` 를 내고
  `smoke.sh` 가 배포를 세운다
- **옛 이름 사본 `diatom_*.db` 가 아직 섞여 있다** (048, 정리 대기). 새 glob 이
  `DiaRUGA_*` 라 로테이션도 NAS 동기화도 그것들을 **못 본다** — 지워지지도, 올라
  가지도 않는다. 로컬 23개 · NAS 3개. `TODOs.md` 를 볼 것.
  여기서 배울 것: **이름을 바꾸면 이름으로 대상을 찾는 안전망도 같이 눈이 먼다.**
  실제로 이름 변경 직후 `/healthz` 가 "사본이 없다" 며 `degraded` 를 냈다

```bash
python db_sentinel.py show                   # 지금 선 깃발
python db_sentinel.py clear backup_db        # 원인을 확인한 뒤 손으로 내린다
```

### 9.8 smoke

```bash
/srv/DiaRUGA/bin/smoke.sh            # .env 의 IMAGE_TAG 를 기대값으로
/srv/DiaRUGA/bin/smoke.sh v0.6.4
```

`/healthz` 200 · `status=ok` · 판 일치 · **행 수 > 0** · nginx 경유 200 을 본다.
`deploy.sh` 가 마지막에 부르고, 따로도 돌린다. **200 만으로는 판이 갈렸는지도, 빈
DB 를 물었는지도 모른다** — 둘 다 형제 프로젝트가 실제로 당한 것이다.

08-07 v0.8.2 배포에서 통과 (슬라이드 11 · 교정 7,472 · `/srv` 스크립트 대조까지).

**그런데 슬라이드마다 여는 화면이 시야 목록 하나뿐이다** — `/crops/`·
`/detections/` 가 죽어도 통과한다. 실제로 그렇게 다섯 판을 통과했다(086).

> **화면도 본다 — 그런데 배포할 때만 돈다** (061). 5번 검사가 nginx 를 거쳐
> 목록 페이지를 치므로 057 의 고장(목록이 `NoReverseMatch` 로 죽어 **모든 화면이
> 500**)도 여기서 걸렸을 것이다. 문제는 **그때 아무도 이것을 돌리지 않았다**는
> 것이다 — 그 고장은 배포가 아니라 **자료가 들어오면서** 났다. 그래서 폴러가
> 자동 처리 뒤에 같은 검사를 하고, 안 뜨면 `db_sentinel` 로 깃발을 세운다.
>
> 061 에서 목록의 슬라이드 링크를 하나 따라가는 검사를 더했다 — **200 은 "떴다"
> 일 뿐**이고, 링크를 만들 수 있는가와 그 화면이 그려지는가는 다른 물음이다.
> `/healthz` 는 링크를 안 만들어 이 갈래를 통과시킨다(그때도 `ok` 였다).

> **`/healthz` 의 `degraded` 는 503 이 아니라 200 이다.** 503 으로 바꾸면
> `deploy.sh` 의 기동 게이트가 200 을 기다리다 **배포가 스스로 멈춘다** —
> "백업이 깨졌다" 는 신호가 배포를 못 끝내게 만드는 꼴이다. 배포를 세우는 판단은
> `smoke.sh` 가 `status != ok` 로 한다 (034)

### 9.9 NAS 자동 수집이 돈다

```
* * * * * deploy/poll_nas.sh      # 호스트 cron, 1분마다
```

새 슬라이드가 올라오면 가져와 그룹핑·합성·검출까지 스스로 돈다. 할 일이 없으면
1~2초에 끝나고 **GPU 를 건드리지 않는다**. 일이 있을 때만 로그를 남긴다
(`/data3/DiaRUGA/logs/poll.log`). 마지막 처리는 08-05 20:27
(`260805_bp09-0901_1` — 057 의 재처리).

> **긴 처리가 도는 동안에는 정찰이 안 돈다.** `flock -n` 이라 그 사이 주기가
> 조용히 물러난다 — `last_scan.json` 이 갱신되지 않는 것은 고장이 아니라 검출이
> 도는 중이라는 뜻이다. 크론 자체를 의심하기 전에 `docker ps` 와
> `journalctl -u cron` 을 볼 것 (057 에서 6분 헤맸다)
>
> **폴러를 손으로 세울 때는 `flock` 홀더의 `sleep` pid 를 죽인다.** 감싼 셸만
> 죽이면 `sleep` 이 살아남아 fd 를 계속 쥐고 있다 — 락이 안 풀린다

**복사가 끝났는지는 mtime 으로 판단하지 않는다.** `rsync -a` 가 원본 시각을
보존해서 한창 들어오는 중에도 조용해 보인다. 폴더의 (파일 수·바이트) 지문을
기억해 두고 `--stable-min`(기본 5분) 동안 안 바뀌어야 가져온다.

### 9.10 아직 안 한 것

- **폴러가 멈춘 것을 아무도 알려 주지 않는다.** 026 에서 4시간 반을 몰랐고, 031 의
  슬라이드가 `processing` 에서 매분 헛도는 것도 사람이 로그를 열어 알았다.
  **가장 싼 답은 "마지막 정찰 시각" 을 뷰어에 띄우는 것**이다 — 뷰어는 늘 열려 있고
  알림 설비가 필요 없다
- **교정 수가 줄어드는 것도 백업 로그에서만 보인다.** 027 을 알아챈 것이 그것이다.
  `check_db.py` 에 "직전 백업 대비 교정 수" 를 넣는 것을 검토한다
- **`/` 가 81% (42G 남음).** 저장소 `backup/` 을 지워 한숨 돌렸고(08-05),
  **도커 자리를 `/data3/DiaRUGA/docker` 로 옮겼다**(081). `/data3` 는 10% 라
  이미지가 쌓여도 `/` 를 밀지 않는다
- **화면 검사가 배포에만 붙어 있었다 — 061 에서 폴러에도 붙였다.** 남은 것은
  **아무도 아무것도 안 할 때**다. 자료도 판도 안 바뀌면 아무 검사도 안 돈다
- **뷰어에 인증이 없다.** 80 으로 나오면서 노출 면이 넓어졌다. 필요해지면
  `DiaRUGA-subpath.conf` 에 `auth_basic` 을 걸면 된다 — Django 를 건드릴 필요가 없다
- 표준 deploy 동사 5개 중 셋이 없다 — `rollback`·`preflight`·`seed` (016).
  `build.sh` 도 없어 **시험을 통과해야 push 하는 관문**이 없다. `DEPLOY.md` 도 없다
- **CI 가 이미지를 못 올린다** — Docker Hub 시크릿이 저장소에 없다(9.1절 상자).
  태그마다 굽기 겹이 빨갛게 남고, 올리는 것은 사람이 한다
- **`smoke.sh` 가 여는 화면이 한 장뿐이다.** 슬라이드마다 화면이 여섯인데 시야
  목록만 본다 — 086 의 두 화면이 그 사이로 빠졌다. 배포 게이트가 "떴다" 와
  "화면이 산다" 를 아직 못 가른다
- **NAS 의 `manual/` 43개(1.4 GB)를 어떻게 할지** — 정책상 수동은 오프사이트로 안
  가는데 정책을 정하기 전에 옮겨 둔 것이 남아 있다

---

## 10. 다음에 할 일

순서대로 적는다. 자세한 것은 `TODOs.md`.

> **검토는 08-06 에 전수 완료됐다** (508/508 · 교정 7,472). 예전 1번이던
> "검토 113 시야" 는 끝난 일이다. **다음 판의 학습 자료가 여기서 나온다** —
> 아래가 그것을 쓰는 순서다.

1. **고아 화면 `/orphans/`** (P02 8단계) — **엔진 교체의 전제다.** YOLO 로 갈면
   `exact` 바인딩이 거의 0 이 되고, 고아가 된 교정을 볼 화면이 없으면 사람의
   판단이 조용히 버려진다. 지금 교정 7,472건이 **전부 `exact`** 라 이 길은 한 번도
   시험대에 오른 적이 없다
2. **재현율 실측 · 원형 판정 기준 실물 확인** — **전수 검토가 끝나 이제 잴 수
   있다.** 정밀도 23.6% 는 슬라이드 3장 시절 값이라 다시 재야 한다. 재현율은
   `탈락` 레이어에서 *점선만 있는 것*(문턱이 놓침)과 *점선조차 없는 것*(분할이
   놓침)을 갈라 센다
3. **싱글턴/합성본 차이가 모델 탓인지 라벨 탓인지 가른다** (4.2절 1번).
   라벨이 흐린 상태에서는 그 위의 모든 개선을 못 읽는다
4. **AM 계열에서 YOLO 를 재 본다** — judge 의 정밀도가 AM261 1.0% · 231cm 4.7% 로
   문턱 하나가 안 맞는 슬라이드들이다. YOLO 의 이득이 가장 클 곳으로 보인다
5. **`AM22-GC10B 25cm` 그룹핑 확인** — `apply_split` 로 시야가 26 → 30 이 됐다.
   **가른 것이 맞았는지는 사람이 확인할 일**이다
6. **프레임별 검토** (P06 5b) — `Image` 정규화가 끝나 **붙일 자리는 다 만들어졌다**
   (055). 결정 둘도 받아 뒀다: 검토 완료는 시야 단위 유지, 교정은 현재 검출에만.
   남은 것은 화면이다(`export_review.py` 는 FORMAT 3 으로 이미 올렸다).
   **"같은 개체 묶기" 와 짝이다** — 시야 508 × 프레임 3~5장이면 사람이 같은
   규조각을 여러 번 본다
7. **폴러 상태를 뷰어에 띄운다** (9.10절), 화면 검사와 한 자리에서 보이게
   (061 에서 검사는 붙였다. 지금은 로그와 `/healthz` 깃발로만 드러난다)
8. **`smoke.sh` 가 화면을 더 열게 한다** (9.10절) — 086 이 그 구멍으로 나왔다

외부에 걸린 것:

- **촬영 쪽에서 ZEN 의 대물렌즈 설정을 실제 교환대와 맞춰야 한다.** 지금은 어긋나도
  경고만 찍고 40x 로 계산하고 넘어간다 — 배율이 다른 슬라이드를 `state="failed"` 로
  세워 사람에게 묻는 장치는 아직 없다
- **`AM22` 가 아문센해인지 Amery 인지** 코어 대장에서 확인 (7절)
- **`Site.lat/lon`** — 좌표가 들어오면 지도가 저절로 정확해진다
