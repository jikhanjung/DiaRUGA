# HANDOFF — 2026-08-13 현재 상태

이어서 작업할 사람(또는 다음 세션)을 위한 인수 문서. **무엇이 돌아가고 있고,
무엇이 반쯤 되어 있고, 어디에 함정이 있는지**를 적는다.

**지난 일은 여기 안 남긴다** — 그것은 `devlog/` 의 몫이다. 이 문서는 **지금**만
말하고, 근거가 필요한 자리마다 devlog 번호를 건다.

**뷰어의 이름은 `DiaRUGA` 다** (Diatom Refocusing Using Genus-level AI, 041).
표기는 이 하나뿐 — `Diaruga`·`DIARUGA` 로 쓰지 않는다. 소문자 `diaruga` 는 기술이
강제하는 자리뿐이고(Docker Hub 이미지·파이썬 패키지 `diarugaweb`·`localStorage`),
아직 `diatom` 인 것은 **생물 이름**이다(`pipeline/segment_diatoms.py`·YOLO 클래스·
NAS 의 `DiatomPhotos/`).

**브랜치** main · 도는 판 `v0.12.2` (파이프라인 `v0.5.2`) — **배포를 기다리는 판이
없다.** 08-13 아침에 `v0.11.2`(등급·자세 · `0034`)와
`v0.12.0`(번지기 · 등급·코멘트를 개체로 · `0035`·`0036`)을 차례로 내보냈고
([114](devlog/20260813_114_deploy-and-pr-flow.md)), 오후에 검토 완료·번지기 대표
고침이 들어왔다 ([116](devlog/20260813_116_done-lost-and-drawn-rep.md)).
**판을 내보내는 절차는 [docs/20260813_release-flow.md](docs/20260813_release-flow.md)
하나로 모았다** — 이미지는 **태그를 밀 때만** CI 가 굽고 올린다.

---

## 1. 한 줄 요약

파이프라인이 전부 DB 를 쓰고, 뷰어·파이프라인이 컨테이너로 돌며, NAS 에 새
슬라이드가 올라오면 1분 안에 검출까지 스스로 간다(P03). 슬라이드 **12** ·
시야 **570** · 교정 **7,924** 건.

| | 지금 |
|---|---|
| 뷰어 | **`v0.12.2`** (마이그레이션 `0036`) |
| 파이프라인 | `v0.5.2` |
| **다음 판** | **없다** — 08-13 오후에 `v0.12.2`(116)를 내보냈다. 절차는 [릴리스 흐름](docs/20260813_release-flow.md) |
| 검토 대상 묶음 | **`yolo-3차`** (`RunBatch.for_review`) — 08-13 에 옮겼다 |

**사람이 하는 일은 `yolo-3차` 검토다** — 08-13 에 검토 대상을 그리로 옮겼고
**37/570** 이다. 그 앞의 `sam2-전수` 는 2차까지 **309/570**(092 부터)이고, 1차는
08-06 에 전수 완료됐다(508/508). 2차에서 `Chaetoceros` 동정이 처음으로 대거
들어왔다(093). **다음 판의 학습 자료가 여기서 나온다.**

> **완료 수는 묶음마다 따로 센다**(073). 묶음을 갈면 그 묶음에서 다시 0부터다 —
> 아직 아무도 안 본 검출이 "검토 완료" 로 보이면 안 되기 때문이다.

**개체 층이 08-09~08-11 사흘에 생겼다** — 사람이 여러 판의 마스크를 하나로 묶고(P11),
개체마다 번호와 종명을 갖고(105), 개체와 판정이 스키마에서 갈렸다(P12). 그 위에
확인 표시(109)와 등급·자세(`v0.11.2` 대기)가 얹혀 있다. **지금 무엇이 어떻게 도는지는
2절**, **어느 판에 무엇이 나갔는지는 [CHANGELOG.md](CHANGELOG.md)**, 그때 무엇을 왜
그렇게 했는지는 [진척 보고서 11차](docs/20260811_DiaRUGA-analysis-system-progress-11.md)와
devlog 다.

> **검토 대상을 갈면 검토·크롭·계측·카탈로그가 함께 옮겨 간다**(105). 화면마다
> 다른 판을 보는 상태가 아예 안 생기게 한 것이다 — 051 이 그 상태에서 났다.
> 갈아 놓은 동안 다른 묶음에 쌓인 것은 **사라지지 않고 그 묶음에 매달려 있다**
> (교정도 완료 표시도 묶음마다 따로다 · 073).

## 2. 지금 돌아가는 것

### 뷰어 — DiaRUGA (Django 5.2, DB 기반)

**http://172.16.116.98/DiaRUGA/** — 랜딩 페이지(`/`)의 카드에서도 간다.
nginx 가 80 에서 `/DiaRUGA/` 을 떼고 `127.0.0.1:8090` 의 컨테이너로 넘긴다.
**옛 주소는 전부 301 로 여기 모인다**(`:9090` · `/diatom/` · 대소문자를 틀리게
친 `/diaruga/`). **URL 은 대소문자를 가린다** — 정식 표기는 `/DiaRUGA/` 하나다.

```
데이터셋 목록  /                  [한국 | 남극 | 전체] 권역 탭 · 표·카드·지도 전환
시야 목록      /d/<slug>/
시야 화면      /d/<slug>/g/<n>/   ← 사람이 교정하는 화면
                 ?batch=<실행>    같은 화면을 다른 엔진으로. **읽기 전용** (051)
검출 갤러리    /d/<slug>/crops/
계측 표        /d/<slug>/detections/
개체 카탈로그  /d/<slug>/catalog/  관찰 하나당 카드 — 번호·종명·유형·코멘트 (105)
문턱 조정      /thresholds/ · /d/<slug>/thresholds/
시료 속성 편집 /d/<slug>/edit/    지역·지점·시료를 한 트랜잭션으로 (탭)
시스템 설정(넷) /system-settings/          자료 — 지역·지점·시료
               /system-settings/ops/      운영 — 검토할 묶음 · 조리법 · 새 묶음
               /system-settings/dataset/  학습 자료 — 검토에서 정답을 얼마나 뽑나
               /system-settings/pipeline/ 파이프라인 — 폴러가 살아 있는가 (098)
```

**옛 `/manage/*` 는 301 로 온다** (089 에서 이름을 `system_settings` 로 끝까지
맞췄다). 링크를 적을 때는 새 주소를 쓴다.

**엔진 고르기는 시야 화면 안에 있다** (051). 사진 위 탭줄의 `◉ SAM ○ YOLO`
라디오다 — 누르면 **시야는 그대로 두고** 그림 위의 개체만 바뀐다. 이웃 시야로
넘어가도 고른 엔진을 들고 간다. 묶음을 한 표로 훑던 `/engine/` 은 075 에서 지웠다.

지도는 권역마다 투영이 다르다 — 남극 EPSG:3031, 한국 EPSG:5179. `전체` 탭에는
지도가 없다(섞을 수 없다). 좌표가 없는 지역은 **주황 마커 + 오차 원**으로 대략값
임을 드러낸다.

**목록의 "검출" 칸과 상세 화면의 검출 수는 다른 것을 센다** (037). 목록은
`ClassDef.counted` 가 켜진 분류만 더한다 — 파편을 개체로 세면 밀도가 부푼다.
상세 화면의 "검출 N → 검토 후 M" 은 검토가 얼마나 걸러 냈는가를 보는 줄이라
파편도 센다.

### 분류는 `ClassDef` 테이블이 정한다 (037~040)

| 분류 | 색 | 배지 | 키 | 센다 |
|---|---|---|---|---|
| 원형 `round` | 진한 초록 `60,220,120` | `rnd` | `q` | ○ |
| 원형 파편 `round_frag` | 옅은 초록 `140,235,170` | `rndf` | `q` (한 번 더) | ✕ |
| 봉상 `rod` | 진한 파랑 `70,140,255` | `rod` | `w` | ○ |
| 봉상 파편 `rod_frag` | 옅은 파랑 `120,195,255` | `rodf` | `w` (한 번 더) | ✕ |
| Eucampia | 분홍 `255,110,190` | `euc` | `e` | ○ |
| Chaetoceros (`CRS`) | 연두 `150,225,75` | `cha` | `r` | ○ |

**색상이 "무엇인가", 진하기가 "온전한가"** 를 말한다(039). 단축키도 같은 모양
이다(040) — `q` 를 한 번 더 누르면 파편. 속명이 긴 것은 `ClassDef.short` 로
줄이되(`Chaetoceros → CRS`) **말풍선과 분류 지정 메뉴는 전체 이름**이다.

> **분류를 더할 때 채울 것이 여덟이다** — `label`·`short`·`badge`·`color`·
> `hotkey`·`counted`·`is_taxon`·`sort_order`, 그리고 **`base.html` 의 CSS**.
> 목록은 `ClassDef` 머리말에 있다. 하나라도 비면 **예외는 안 나고 그 분류만
> 조용히 다르게 굴러간다.** `ops/check_db.py` 가 단축키·색 두 개는 잡아 준다
>
> **색은 아직 테이블과 CSS 두 곳에 있다.** 테이블에서 뿜어내는 것이 근본인데
> 검토 화면 렌더링을 통째로 건드리는 일이라 미뤘다 — 그동안은 어긋날 수 있는 값
>
> **되돌리기는 지우는 것이 아니라 `active=False` 다.** 행을 지우면 그 분류로
> 붙인 교정이 이름 없는 분류가 되어 화면에서 안 읽힌다

### DB (`DiaRUGA.db`, WAL)

**08-10 18:40 기준** — 다만 굵게 적은 넷(Slide·Viewpoint·Detection·ObjectReview)은
**08-11 19:20 에 `/healthz` 로 다시 봤다.** 정확한 값은 이 문서가 아니라
`/healthz` 나 `smoke.sh` 가 낸다.

| 테이블 | 행 |
|---|---|
| Site / Locality / Sample / **Slide** | 5 / 5 / 10 / **12** |
| Viewpoint / Frame / Stack | **570** / 1,830 / 417 |
| Image | 2,664 (프레임 1,830 · 합성본 417 · 깊이맵 417) |
| Detection / Candidate | **2,817** / 122,333 |
| ViewpointReview / ObjectReview | 521 / **7,924** |
| DiatomObject | 7,909 — 그중 묶음(멤버 2 이상)은 4 (시야 셋) |
| ThresholdSet / ClassDef / Run / RunBatch | 2 / 6 / 214 / 2 |

> **교정이 08-10 저녁보다 10 늘었다** — 그 사이의 검토와 109 의 `auto_confirmed`
> 가 섞인 것이고 **갈래는 다시 안 세었다.** 아래는 08-10 18:40 의 갈래다.
>
> **교정 7,914 의 갈래**: `sam2-전수` 7,726 · `yolo-3차` 17 · 묶음이 빈 것 171
> (사람이 그린 것 `source='manual'`). 사람이 경계를 고친 것(`geom_edited`)은 4건,
> **종명(`species`)은 아직 0건**이다.
>
> **P12(108) 뒤로 판정마다 개체가 하나씩 생긴다.** `DiatomObject` 7,909 개 중
> 6,120 개가 "오검출" 이다 — 그래서 **개체를 세는 것은 규조각을 세는 것이
> 아니다.** 집계는 여전히 `Candidate` 에서 나오고, 개체는 그 위에서 **프레임에
> 겹쳐 잡힌 것을 하나로 세게** 해 준다.
>
> **`ViewpointReview`(521)가 시야 수(570)보다 적다.** 완료 표시는 **묶음마다**
> 남고(073) 새 슬라이드 62 시야는 아직 검토 줄이 없어서다.

### 층별 상태

```
권역   한국 · 남극                       Site.area
 └ 지역   BP(북평분지) · RS23            Site
    └ 지점   BP09(노두) · GC03(시추코어)  Locality
       └ 시료   0901 · 71cm              Sample
          └ 관찰   (1) · (2)             Slide    = 폴더 하나 = 슬라이드글라스 하나
```

| 지역 | 지점 | 유형 | 시료 | 관찰 | 시야 | 검토 |
|---|---|---|---|---|---|---|
| RS23 | GC03 | 시추코어 | 71cm · 231cm · 369cm | 3+1 | 189 | **전수**(369cm(100) 제외) |
| WAP13 | GC47 | 시추코어 | 116cm · 450cm | 2 | 90 | **전수** |
| AM22 | GC10B | 시추코어 | 25cm · 261cm | 2 | 57 | **전수** |
| GC03 | C1 | 시추코어 | 72cm · 816cm | 2 | 92 | **전수** |
| **BP** | **BP09** | **노두** | **0901** | **2** | **142** | **전수** |

| 관찰(슬라이드) | 시야 | 비고 |
|---|---|---|
| RS23-GC03 71cm / 231cm / 369cm | 34 / 19 / 74 | |
| RS23-GC03 369cm (100) | 62 | **12번째 슬라이드** — 08-09 반입, 08-10 `done` |
| WAP13-GC47 116cm | 47 | 단독 시야 26개 (그룹핑 임계값 미확인) |
| WAP13-GC47 450cm | 43 | YOLO 의 `val_slide` — 학습에 안 쓴 슬라이드 |
| AM22-GC10B 25cm | 30 | 08-04 에 시야를 갈랐다 (26 → 30) |
| AM22-GC10B 261cm | 27 | 27 중 26이 단독 시야 — 그룹핑 확인 표본 부족 |
| GC03-C1 72cm / 816cm | 61 / 31 | |
| **BP09-0901** | 86 | 관찰 `0` |
| **BP09-0901 (1)** | 56 | **같은 시료의 다른 관찰**(`obs_no=1`) |

12장 모두 `state=done`. **북평분지는 별개 갈래다**(7절).

**관찰은 시료 하나를 처리 방법·회차를 달리해 본 것**이고 서로 동등하다. 대표를
두지 않고 이름표(`obs_label`)로 구분하며, 통계에 무엇을 넣을지는 `hide_in_list`·
`exclude_from_totals` 로 사람이 고른다.

| 칸 | 누가 채우나 |
|---|---|
| `obs_no` | 폴더 접미사에서 자동. 없으면 `0` |
| `obs_label` | **사람** (산처리 · 체 20µm — 10자) |
| `hide_in_list` · `exclude_from_totals` | 사람 |

- **폴더 접미사는 숫자만 받는다** — 글자를 받으면 슬러그가 뭉개져 서로 다른 관찰이
  같은 슬러그로 부딪히고 **한쪽이 다른 쪽을 조용히 덮어쓴다**
- **합계는 `exclude_from_totals` 만 읽는다.** 숨김을 읽으면 보기 토글 한 번에
  같은 자료가 다른 숫자를 낸다
- **슬러그는 URL 이다.** 이 규칙은 **파이프라인 이미지 안**에 있다 — 고쳤으면 그
  판을 올려야 한다(057)

### 시스템 설정 — 넷이다

목록 머리줄에서 들어간다. 묻는 것이 다르고 쓰는 때가 달라 갈라 두었다(083 · 098).

| 화면 | 무엇을 묻는가 | 언제 |
|---|---|---|
| 자료 | 이 관찰이 어느 지점·시료의 것인가 | 새 슬라이드가 들어올 때 |
| 운영 | 지금 무엇을 보고 있고 새 자료를 어떻게 채우는가 | 회차를 돌릴 때 |
| 학습 자료 | 검토한 것에서 정답을 얼마나 뽑을 수 있나 | 다음 회차를 준비할 때 |
| 파이프라인 | 폴러가 살아 있는가 — 마지막 정찰·끝나지 않은 슬라이드·최근 실행 | 아무 때나 |

**운영 화면이 묶음을 쥔다** — 검토 대상 고르기(P10) · 조리법 적기(079) ·
**새 묶음 만들기**(084). **묶음을 만들었다고 검출이 생기지는 않는다** — 몇 시간짜리
GPU 작업을 버튼 하나로 조용히 시작하지 않는다.

**자료 화면**에서는 지역·지점·시료를 만들고 지운다. **관찰은 만들지도 지우지도
못한다** — 폴더가 만드는 것이고 그 아래에 재생성 불가한 교정이 달려 있다. 되는
것은 **소속을 옮기고 떼는 것**뿐이다.

- **소속 없는 관찰을 잡아 주는 유일한 화면이다.** 그것은 어느 권역 탭에도 안 걸려
  목록·지도·지점 페이지 어디에도 안 나온다 — 500 도 404 도 아니라 그냥 사라진다
- **지우기에 문턱이 있다.** `Slide.sample` 이 `SET_NULL` 이라 시료를 지우면 관찰이
  조용히 소속을 잃는다 — 무엇이 몇 개 걸려 있는지 **버튼에 미리 적고**, 서버가
  다시 검사한다(화면에서 막는 것은 막는 것이 아니다)
- `ops/check_db.py` 의 **7번(층)** 이 같은 것을 센다

### 노두 현장 사진 (063)

노두 지점의 페이지에 현장 사진이 붙는다. **파일이 곧 자료다 — DB 에 행이 없다.**
NAS 공유(`N:\DiaRUGA\outcrop` = `/nfs/temp-share/DiaRUGA/outcrop`)에 이름이
**`<지점코드> (n).jpg`** 인 것이 지점과 사진을 잇는 유일한 근거다.

- 화면에서도 올린다(끌어다 놓기). **이름은 서버가 짓는다** · **세 장까지** ·
  올릴 때 긴 변 2400px·q85 로 줄인다
- **NAS 가 없어도 죽지 않는다** — 읽기가 `OSError` 를 삼키고 빈 목록을 낸다
- **지점 코드를 바꾸면 사진이 떨어진다.** 이름이 유일한 근거라 그렇다
- 쓰는 곳은 `viewer/outcrop.py` 하나다

### 파이프라인 (전부 DB)

```
scan_nas → ingest_nas → group_focus_series → focus_stack --slide → segment_diatoms --slide
```

이 흐름을 `deploy/poll_nas.sh` 가 1분마다 돌린다. **GPU 를 쓰는 작업은 한 번에
하나만 돈다** — 잠금이 `segment_diatoms` 안에 있어 폴러가 도는 중에 손으로 돌려도
기다렸다 이어 간다.

### 검출 엔진이 두 벌이다

`segment_diatoms.py --backend {sam2,yolo}`. **YOLO 는 `--keep-current` 로 넣어
기존 검출·교정을 건드리지 않고 나란히 쌓았다.**

| 묶음 | 코드 | 실행 | 검출 | `is_current` | `for_review` |
|---|---|---|---|---|---|
| `sam2-전수` | `S1` | 25 | 570 | 570 | **켜짐** ← 뷰어가 보는 것 |
| `yolo-3차` | `Y3` | 48 | 2,247 | 2,247 | 꺼짐 |

> **`is_current` 가 양쪽 다 켜져 있는 것이 정상이다.** P10 에서 뜻이 **"그 묶음
> 안에서 최신"** 으로 좁아졌다. **뷰어가 볼 것을 정하는 것은 `for_review` 다.**
> 둘을 함께 보는 것이 `Detection.objects.reviewing()` 이고 화면은 전부 그것을 지난다.
>
> **`RunBatch.code` 는 105 가 더한 칸이다** — 카탈로그 번호의 꼬리가 되어
> `…-S1`·`…-Y3` 로 **번호만 보고도 어느 검출인지** 알게 한다. **사람이 정한다** —
> 라벨에서 자동으로 뽑으면 `yolo-3차`·`yolo-4차` 가 같은 글자로 눕는다. DB 가
> 유일 제약으로 막고, 손그림 자리인 `M` 은 `CheckConstraint` 로 막는다.

폴러도 스스로 묶는다(`DETECT_BATCH`). **079 부터는 묶음의 조리법을 따른다** —
검토 중인 묶음부터, 나머지는 최근 것부터 채운다.

### 이미지·검출·교정 (P06 · 055)

```
Slide 1:N Viewpoint 1:N Image(kind = stack | frame | depth) 1:N Detection 1:N Candidate
                                    ↑
                           ObjectReview.image      UNIQUE (image, batch, mask_key)
```

**`Image` 는 검출을 돌릴 수 있는 이미지 한 장**이고 열쇠는 `path` 다(파일 하나에
행 하나 — 2,664개).

- **촬영·합성 메타는 `Frame`·`Stack` 에 그대로 있다.** `Image` 는 정체만 맡는다
- **`kind="depth"` 에는 검출이 안 붙는다** — 관행이 아니라 스키마에 적혀 있다
- **만드는 문은 `web/viewer/images.py` 하나다**
- **`Image.viewpoint` 는 `SET_NULL`, `Image.stack` 은 `CASCADE`** — 시야를 가르면
  **프레임은 살아남고 합성본·깊이맵은 죽는다.** 방향이 반대인 것이 요점이다
- 앞뒤가 맞는가는 `backfill_images.py --verify` 가 본다(아홉 항목)

### 같은 개체 묶기 (P11 · 102 · 104 · 107 · **P12/108**)

시야 화면에서 마스크 우클릭 → 판마다 근처 후보를 하나씩 고르고 **별로 대표**를
세운다. 지금 **묶음 4개 · 멤버 12개**(전부 `yolo-3차`, 시야 셋)다.

**108 에서 자리가 바뀌었다.** `ObjectLink`→`DiatomObject`, `ObjectLinkMember` 는
`ObjectReview` 로 흡수됐다 — **판정 행이 곧 멤버다.** 그래서:

- **묶기 = 개체 둘을 합친다** (그릇에 밀린 개체는 `prune_objects` 가 걷는다),
  **풀기 = 개체를 가른다** (`_split_off` 가 분류·종명을 물려준다)
- **`link.delete()` 를 쓰면 안 된다** — `CASCADE` 라 그 판들의 교정을 지운다
- 개체 수는 묶음 수가 아니다. 화면·감사 기록이 말하는 묶음은 **멤버가 둘 이상인
  개체**이고, 세는 자리마다 그렇게 거른다

- **탈락 후보도 고를 수 있고, 고르면 되살아난다** (102). 팝업이 빨간 점선으로
  내고 "탈락 후보 N개를 함께 되살립니다" 라고 미리 적는다. **미리 고르기는 탈락을
  안 집는다** — 자동으로 골라 두면 사람이 확인 안 한 판정이 서 버린다
- **분류·종명·등급·자세·코멘트는 개체에 산다** (P12 · `0035`·`0036`).
  번지게 할 코드가 없다 — 진실이 하나다.
  화면에는 여전히 알려 준다(`linked_siblings`): 다른 판의 상태는 열릴 때 받은
  것이라, 안 알리면 그 판의 다음 저장이 자기가 아는 옛 값으로 되돌린다
- **엇갈리는데 안 고르면 묶기가 409 로 거절된다** (108). 개체가 값을 하나만 들
  수 있어 107 의 "그대로" 가 성립하지 않는다 — 그릇의 값이 남의 동정을 덮는다.
  **값이 하나뿐일 때는 그릇이 물려받는다**(덮을 것이 없으므로)
- **번지는 것과 안 번지는 것의 선**: 유형·종명은 개체의 것이고,
  **삭제·되살림·기하는 판마다 하는 판단**이라 판정에 남는다 (코멘트는 `0036`
  으로 개체에 갔다)
- **소급은 안 된다.** 이미 만들어진 묶음의 어긋남은 사람이 그 팝업을 다시 열어야
  맞춰진다 — 그것을 **세는** 자리(`check_db` 불변식)가 아직 없다

> **묶음은 시야도 회차도 못 넘는다.** 멤버가 `(image, batch, mask_key)` 로 짚고
> 묶음 자체도 `batch` 하나에 매여 있다 — 같은 규조각이 `sam2-전수` 와 `yolo-3차`
> 양쪽에 잡혀 있어도 **그것이 하나라는 것을 담을 자리가 없다.** 그래서 카탈로그
> 번호와 동정이 회차마다 별개다. **스키마는 YOLO 4차가 들어온 뒤에 박는다**
> (사용자 판단). 그 층을 어떻게 그을지가
> [P12](devlog/20260810_P12_object-and-judgement.md) 다.

### 개체 카탈로그 — 번호와 동정 (105)

`/d/<슬러그>/catalog/`. 카드 하나가 개체 하나이고, 왼쪽에 세워 자른 크롭,
오른쪽에 **번호 · 종명 · 유형 · 코멘트**. 검색·거르개 칩·동정 진행률이 붙는다.
**개체 코멘트를 적는 자리는 여기 하나다** (`v0.12.0` · 112) — 검토 화면은 읽기만
한다. 시야 코멘트는 검토 화면의 다른 칸이다.

**번호는 DB 에 없다 — 층·시야·bbox·묶음 코드에서 그때그때 만든다**
(`web/viewer/catalog.py` — `naming.py`·`judge.py` 와 같은 자리).

```
<지역>-<지점>-<시료>[-<관찰>]-g<시야>[-f<프레임>]-<위치>-<묶음>

RS23-GC03-071-g03-1204_856_132_97-S1        남극 · SAM2 · 합성본
RS23-GC03-071-g03-f28-1198_861_130_99-Y3    남극 · YOLO 3차 · 프레임 28
BP-BP09-0901-g12-f27-843_1502_96_210-S1     육상 · 단독 프레임
RS23-GC03-071-g03-m1a2b3c4d-M               사람이 그린 개체
```

- **위치는 bbox 전체다.** 짧게 줄이려던 후보(x·y / 중심 / 해시 4자)가 전부
  실측에서 부딪혔다 — 이미지 한 장에 개체가 최대 106개다. **bbox 전체만
  `Candidate` 의 `(detection, mask_key)` 유일 제약이 보장한다.** 해시 5자도
  오늘 0 이지만 그것은 **재 봐서 0 인 것**이고 자료가 늘면 언젠가 부딪힌다
- **번호가 안정한 것은 재료가 안정하기 때문이다** — `mask_key` 는 사람이 경계를
  고쳐도, 지웠다 되살려도 안 바뀐다
- 통과 개체 **32,480건에 번호를 매겨 전부 서로 달랐고, 못 만든 것은 0건**이었다.
  `ops/check_db.py` **9번**이 그것을 계속 본다
- **9번은 뷰어 이미지를 올린 뒤에 돈다** — 규칙이 뷰어 코드에 있어서다(057 의 축).
  낡은 판에서는 **크게 적고 넘어간다**(죽지도, 조용히 건너뛰지도 않는다)

**종명(`species`)은 `DiatomObject` 의 칸이다** (105 에서는 `ObjectReview` 였고
**P12/108 이 개체로 옮겼다** — `label` 과 같은 자리다). 자유 입력 + 자동완성으로
갔고(종은 목록으로 못 가둔다 — `var.`·`f.`·명명자가 붙고 이름이 바뀐다), 굳으면
그때 테이블로 옮긴다. **`ops/export_review.py` 가 함께 싣는다**(형식 번호는 안
올렸다 — 적은 개체에만 키를 실으므로 종명이 없는 파일은 한 글자도 안 바뀐다).

**담기는 것은 통과분과 사람이 되살린 것이다.** 어느 이미지의 개체를 낼 것인가는
`candidate_rows` 가 이미 정하고 있어 그것을 재사용했다 — 규칙이 둘이 되면 크롭
화면과 카탈로그가 서로 다른 개체를 센다.

### 등급·자세 — 둘 다 개체에 산다 (`0034` → `0035`, `v0.12.0`)

우수한 개체를 골라 **먼저 학습시키기 위한** 두 칸이다. 카탈로그 카드에 붙었다.
**값은 아직 0건이다.**

| | 어디에 사나 | 무엇에 대한 판단인가 |
|---|---|---|
| **등급** `A`·`B`·`C` | `DiatomObject` | 개체 — 완형이 잘 드러나는가·동정키가 남았는가 |
| **자세** `valve`·`girdle`·`other` | `DiatomObject` | 개체 — 초점을 옮겨도 누운 자세는 그대로다 |

> **등급은 하루 만에 자리를 옮겼다** (`0034` 판정 → `0035` 개체 · 111).
> 처음엔 *"같은 규조각도 초점면마다 동정키가 보이고 안 보인다"* 를 근거로 판에
> 두었는데, **그 판단은 `is_rep`(대표)이 이미 하고 있었다** — 어느 판을 이 개체의
> 얼굴로 삼는가가 곧 어느 판이 잘 보이는가다. 운영에 0건이라 지금이 가장 쌌다.
>
> **그래서 판(초점면)에 대해 적을 자리는 대표 하나다.** 코멘트도 `0036` 으로
> 개체에 갔다(112). 판마다 남는 것은 `removed`·`accepted`·`geom`·`is_rep`·
> `auto_confirmed` — **판정**뿐이다.

- **파편에는 두 칸을 안 준다** — `ClassDef.counted` 가 켜진 분류만이다. 카드에서
  안 띄우고 **서버(`data.check_grade_pose`)가 다시 막는다**(화면에서 막는 것은
  막는 것이 아니다). 유형을 파편으로 바꾸면 **조용히 지우지 않고 묻는다**
- **`/review` 청소가 두 칸만 있는 행을 안 지운다** — 종명·`auto_confirmed` 와 같은
  갈래다(3.3절). 검토 화면은 이 칸들을 모른다
- **진행도의 "동정" 이 셋을 요구한다**(종명·등급·자세). 지금 **0%** 로 보이는 것은
  뜻이 바뀌었고 값이 0건이라서다. 고장이 아니다
- **`ops/export_review.py` 가 두 칸을 싣는다**(08-12). 매긴 것에만 실어 형식 번호를
  안 올렸다 — 값이 없는 파일은 한 글자도 안 바뀐다
- **`ops/check_db.py` 10번이 지킨다** — 파편에 붙어 있지 않은가 · 살아 있는 개체에만
  붙어 있는가. **매긴 개체가 0건이면 절 전체를 건너뛴다**(`-v` 로 보면 이유를
  적는다). 늘 0 인 검사는 덮은 줄 알게 하므로 두지 않는 것이다
- **`0033` 이 둘이 될 뻔했다** — 109 가 같은 번호로 먼저 들어와서, 병합
  마이그레이션 대신 **`0034` 를 `0033` 뒤에 줄 세웠다**(둘 다 칼럼을 더하기만 해서
  순서가 뜻을 안 바꾼다). 근거가 마이그레이션 파일 주석에 있다

---

## 3. 지금 조심할 것

### 3.1 교정의 원본은 DB 다 — 감사 기록은 `review/` 로 나간다

**7,924건이고 재생성 불가다.** 종명·묶음·등급·자세도 같은 무게의 자료다
(**등급·자세는 아직 내보내기에 안 실린다** — 2절 끝 · 10절 0번). 원본은
`DiaRUGA.db` 이고, `ops/export_review.py` 가 `review/<슬라이드>/g<n>.json` 으로
내보낸다 — git 에 남으므로 **`git diff` 로 "언제 무엇이 달라졌나" 가 보인다.**

```bash
python ops/export_review.py            # 저장소 review/ 로 (호스트 venv 로 돈다)
python ops/export_review.py --check    # 파일 ↔ DB 대조. 아무것도 쓰지 않는다
python ops/export_review.py --db /data3/DiaRUGA/backup/<사본>.db --out /tmp/before
diff -r /tmp/before review/            # 두 시점을 비교한다
```

> **왜 호스트에서 도는가** — Django 를 임포트하지 않고 sqlite3 로 **읽기 전용**
> 으로만 열어서 `ops/backup_db.py` 와 같은 자리다. 그래야 저장소에 바로 쓰고,
> 스키마가 바뀌는 동안에도 같은 도구로 비교하고, 백업 파일을 `--db` 로 읽는다.

**그래도 `ops/backup_db.py` 가 첫 안전망이다** — 내보내기는 교정만 담는다.

- `DiaRUGA.db` 는 gitignore 다. 실물은 `/srv/DiaRUGA/db/`
- **큰 작업 전에 반드시** `deploy/host/dbrun.sh backup_db.py --note <설명>`
- `cp DiaRUGA.db` 로 뜨지 말 것 — WAL 이라 불완전한 사본이 된다
- 사본은 `/data3/DiaRUGA/backup/`, 오프사이트는 NAS (9.5절)

### 3.2 이상하면 `ops/check_db.py` 부터

`refilter`·`segment_diatoms` 를 돌린 뒤, `pipeline/judge.py` 를 고친 뒤, 숫자가
이상할 때 돌린다(1초). 여기서 잡는 것은 **예외가 나지 않고 그냥 틀린 상태**다.

현재 **아홉 절 29개 검사 전부 OK.** 8번이 묶음(P11), **9번이 개체 카탈로그**다.

```bash
deploy/host/dbrun.sh check_db.py            # 9.2절 — DB 는 컨테이너 문으로만
```

### 3.3 `/review` POST 는 그 (이미지, 묶음)의 교정 전체를 갈아치운다

```python
ObjectReview.objects.filter(image=…, batch=…).exclude(mask_key__in=keys).delete()
```

**"뷰어는 늘 전체를 보낸다"** 는 전제다. 전제가 깨지면 나머지를 전부 지운다 —
**운영 자료를 두 번 잃었다**(017 교정 14건 · 027 교정 37건). 053 은 저장이 **남의
시야로 가고 있었고**(프레임 이름이 슬라이드끼리 겹친다), 지금은 `(slug, gid)` 로
짚고 `stem` 은 검증에만 쓴다.

지금 막아 둔 것:

- 화면은 `readOnly` 면 POST 를 **아예 안 보낸다**
- 서버는 **남의 검출 키가 섞이면 409** 로 아무것도 바꾸지 않는다
- **덧문은 "그 시야가 가진 판들" 과 견준다**(103) — 하나만 집으면 **프레임 판으로
  열린 시야 11개의 저장이 통째로 거절된다.** 그 열한 시야의 검토 기록이 비어 있는
  것은 **맞다**(서버가 받은 적이 없다). 되살릴 방법은 없고 다시 검토해야 한다
- **저장 실패는 크게, 서버의 말로** 적는다 — 성공할 때까지 안 사라지는 띠(103).
  **밀린 저장이 실패한 상태에서는 묶기를 거절한다**
- **`/review` 가 안 보내는 것은 삭제 범위에서 뺀다** — 종명이 그렇다(105).
  검토 화면은 종명을 모르므로, 그 갈래가 없으면 **"검토 완료" 한 번이 동정을
  지운다.** 안전망을 너무 넓게 잡으면 빈 교정 행이 쌓이니 **키만** 얹는다

> **읽기 전용은 저장을 막는 것으로 끝나지 않는다** (051). 저장은 잠갔는데 우클릭
> 메뉴가 살아 있어 누르면 마스크가 지워지고 분류가 바뀌었다 — 그렇게 한 시야를
> 검토하고 새로고침하면 판단이 통째로 사라진다. `readOnly` 갈래를 더할 때는
> **반응하는 자리를 전부 센다**: 키보드 · 우클릭 메뉴 · 탈락 펼침판 · 시야 코멘트 칸

**여전히 빈 키 목록 POST 는 전부 지운다** — 그것이 "교정 전체 초기화" 의 정상
경로라서다. **손으로 화면을 눌러 볼 때는 사본 DB 에 붙인다**
(`DIARUGA_DB=…/사본.db DIARUGA_SCRIPT_NAME= manage.py runserver 127.0.0.1:8099`).

### 3.4 확인하는 법 — 자동 시험이 먼저다 (P08)

```bash
python web/manage.py test viewer --exclude-tag browser   # 538개 · 6.9초
python web/manage.py test viewer                         # 664개 · 170초
```

> **08-13 에 세었다** (`v0.12.0` · 114). 그전까지 이 자리에 적혀 있던 455/561 은
> 08-10 것이었고 *"세어 보고 고칠 것"* 이 붙은 채 사흘을 갔다 — **시험은 계속
> 돌렸는데 숫자를 갱신하는 것만 빠졌다.** 브라우저 레이어는 **126개**다.

호스트 venv 로 돈다 — **시험이 자기 DB 를 만들고 끝나면 버리므로** `dbrun.sh` 를
안 거치고, 운영 DB 나 `/data3` 를 가리키면 `tests/base.py` 가 스스로 멈춘다
(DB 와 `DATA_ROOT` **둘 다** 본다 — 한쪽만 봤다가 픽스처가 `/data3` 에 사진을
썼다).

**브라우저 레이어가 진짜 크로미움으로 눌러 본다**(045). 키 입력·클릭·페이지 이동·**콘솔 오류**·
화면 캡처. 이벤트 배선 고장은 이것으로만 잡힌다.

- **커버리지를 목표로 하지 않는다.** **되살려서 잡히는 것을 보고 나서 "있다" 고
  말한다** — 실패할 수 없는 시험은 없는 것보다 나쁘다
- **URL 을 덮는 것과 갈래를 덮는 것은 다르다**(086). 자료가 전부 합성본이라
  프레임 갈래를 한 번도 안 지났고, 그 사이 두 화면이 v0.8.0 내내 500 이었다
- **픽스처가 현실에 없는 모양을 세우면 시험이 옛 규칙을 증언한다**(106).
  `geom` 의 키가 표마다 다른데(`bbox` / `bbox_xywh`) 픽스처가 한쪽만 써서,
  카탈로그가 다른 판의 그림을 옛 자리로 잘라 내는 것을 못 잡았다
- **여전히 사람이 봐야 하는 것은 "눈에 보이는 것" 하나다** — 색 대비, 로고
  크기·굵기, 좁은 칸에서 글자가 넘치는가

**GitHub Actions 가 push 마다 돈다** — 시험이 통과한 것만 뷰어 이미지가 된다.
파이프라인 이미지는 거기서 안 굽는다(러너에 GPU 가 없다). `deploy.sh` 는 사람이 부른다.

### 3.5 카탈로그가 YOLO 개체의 절반을 못 본다 (106 — 안 고쳤다)

카드는 `candidate_rows` 의 규칙을 물려받아 **합성본 검출을 기준으로** 낸다.
그 규칙은 SAM 용이었다.

```
합성본+프레임 둘 다 있는 시야 417개
  프레임 개체 16,782 중 합성본에도 있는 것 11,659
  프레임에만 있는 것              5,123   ← 카탈로그에 아예 안 나온다
  (합성본 통과 개체는             4,444)
```

**저장할 자리는 있다** — `ObjectReview` 열쇠가 `(이미지, 묶음, mask_key)` 라
프레임 이미지에 동정을 앉힐 수 있고 번호도 `-f<순번>-` 을 낸다. **화면이 안
보여줄 뿐이고, 그래서 저장될 일도 없다.**

**"프레임도 다 내자" 로 안 끝나는 이유**: 같은 규조각이 초점면마다 잡혀 카드가
2~10장씩 중복되고, **번호가 판마다 달라 서로 다른 개체가 된다** — 이름이 여러 개면
이름이 아니다. 접으려면 "이 마스크들이 한 개체다" 를 아는 것이 있어야 하고,
그것이 **묶음**이다.

> **C 안("프레임에만 있는 개체 n개가 더 있습니다")을 만들었다가 접었다** —
> 세는 방식이 결국 **기계가 같은 개체인지 판정한 것**이었고, 견줄 근거가 묶음
> 4개뿐이다. **화면에 숫자를 내려면 그 숫자를 견줄 근거가 있어야 한다.**

### 3.6 106 이 접어 둔 stash 셋은 버렸다 (08-11)

`paleoadmin` 클론에 있던 셋(그린 마스크 번지기 1단계 · 프레임 개체 수 C안 ·
묶음 표시 SVG 시안)을 **`git apply --check` 로 확인하고 버렸다.** 밑바탕이 그
사이 바뀌었다 — P12 가 `save_review` 를 다시 썼고 문서 파일 이름도 갈렸다.

- **충돌보다 안 나는 부분이 위험했다.** 서버 조각(`data.py` +128줄)은
  `ObjectReview` 가 판정과 동정을 겸하던 시절의 전제로 짰는데, 지금은 **판정 행이
  개체 없이 설 수 없다**(`data.judgement_for` 문을 안 지나면 `IntegrityError`,
  또는 대표 없는 개체). 조용히 붙는 128줄이 그 자리를 지나지 않는다
- **붙지도 않는 것을 두면 "언제든 꺼내 쓸 수 있다" 는 착각이 남는다.** 코드는
  버리고 **판단만 TODOs 로 옮겨 적었다**
- **계정마다 작업 트리가 따로다**(`jikhanjung`·`paleoadmin`·`sclee`). 한쪽의 stash
  는 다른 쪽 `git stash list` 에 안 보이고 저장소에도 안 남는다 — **미커밋 상태를
  인수 근거로 삼지 말 것.** 여기서 안 보인다고 유실된 것이 아니다

**번지기는 P12 위에서 새로 짰고 `v0.12.0` 에 들어간다** (08-12 ·
[111](devlog/20260812_111_drawn-spread-and-grade-move.md)). 버린 stash 의 판단이
그대로 맞았다 — **서버 + 화면 두 조각이라 한 커밋으로 넣었고**(반쪽이면 복제가
다른 판의 저장에서 사라진다), 새로 정할 것이었던 *"복제한 마스크가 판마다 제
개체를 갖는가"* 는 **한 개체를 나눠 갖는 쪽**으로 갔다(같은 규조각을 옮겨 그린
것이니까). 묶는 문은 `data.merge_into_object` 하나다.

### 3.7 파이프라인이 도는 중에 검토를 저장하면 DB 가 잠긴다

WAL 이라 읽기는 여럿이 되지만 **쓰기는 한 번에 하나**다. 파이프라인이 개체 수천 개를
한 트랜잭션으로 넣는 동안 사람이 검토를 저장하자 20초를 기다리다 죽었고, **프레임
229장이 통째로 날아갔다**(043). 트랜잭션을 둘로 나눠 고쳤지만, **동시 작업이
일상이 되면 SQLite 를 다시 볼 문제다.**

### 3.8 아직 안 한 이전기·일회성

- **`migrate/import_json.py` 를 아무 때나 돌리지 말 것.** 멱등이지만 `Candidate` 를
  지우고 다시 만들어서, DB 에서만 한 교정이 있는데 옛 JSON 을 넣으면 JSON 쪽으로
  되돌아간다. **지금 JSON 은 DB 보다 한참 낡았다**
- **`migrate/tighten_bbox.py` 는 만들어 뒀지만 적용하지 않았다.** `mask_key` 를
  바꾸는 작업이라 P02 8단계(재바인딩·고아 화면)와 한 번에 푸는 것이 낫다
- **`migrate/verify_db.py` 는 임포트하면 `ImportError` 다** (실행 전용으로 막아 뒀다)
- **`migrate/refix_drawn_reps.py` 는 08-13 에 돌렸다 — 다시 돌릴 일이 없다**
  (116 · `v0.12.2` 배포 뒤 개체 11개를 합성본으로 되돌렸다). 남겨 두는 것은
  같은 일이 또 나면 쓰려는 것이고, `--apply` 전에 **목록을 눈으로 볼 것** —
  사람이 `/link` 에서 고른 대표와 번지기가 옮긴 대표를 스크립트가 못 가른다

### 3.9 그룹핑 — 판정은 겹침으로만 한다

`AM22-GC10B 25cm` 이 옛 기준에 걸렸지만 **사람이 확인해 보니 정상**이었고, 틀린
것은 지표 쪽이었다(018). 지금 기준: **그룹 안 상관과 경계 상관이 겹치는 쌍이
있을 때만** 사람을 부르고, **표본이 모자라면 모른다고 말한다**(`AM22 261cm` 은
그룹 안 쌍이 1개뿐이라 "확인 필요(표본 부족)"). 이미 처리된 슬라이드는 `done` 인
채로 두었다 — **앞으로 들어오는 것부터** 새 기준이 걸린다.

---

## 4. 어디까지 왔나

### 4.1 P02 — DB 이전 (8단계)

1~7 **끝**. 남은 것은 **8. 재바인딩 + 고아 화면** — `migrate/rebind.py` 는
만들었지만 **`/orphans/` 화면이 없다.**

8단계는 **학습·엔진 교체의 전제다.** 엔진이 바뀌면 `exact` 바인딩이 거의 0 이
되고, 고아가 된 교정을 볼 화면이 없으면 사람의 판단이 조용히 버려진다. 지금
교정은 **전부 `exact`**(7,726) 라 이 길은 한 번도 시험대에 오른 적이 없다.

### 4.2 P04 — 사람의 검토로 검출기를 학습시킨다

| 단계 | 상태 |
|---|---|
| 1~3. 자료 꾸러미 (`ops/export_yolo.py`) | **끝** — 264 시야 · 개체 1,503 (023) |
| 4. 첫 학습 (11m, `imgsz=1280`) | **끝** — RTX 8000 에서 20분 (025) |
| 5. 비교 | **끝** — conf 훑기까지 (025 §8) |
| 6. 운영 반입 | **반** — 이미지에 ultralytics 는 들어갔다. **엔진은 안 갈았다** |

**자료.** 사람이 인정한 개체 1,503 · **지운 것 4,039** · 검출기가 놓쳐 되살린 것
376. 음성 4,039가 핵심이다 — SAM2 가 헷갈린 자리에 사람의 "아니다" 가 붙어 있고,
YOLO 는 **라벨 없는 자리를 배경으로 배우므로** 별도 처리 없이 효력이 있다.
꾸러미는 `/data3/DiaRUGA/datasets/yolo-v1{,-seg}` (688 MB · 재생성 가능).

**성적** — `val_slide`(WAP450, 학습에 안 쓴 슬라이드) 기준.

| | 재현율\* | 정밀도 | 시야당 |
|---|---|---|---|
| SAM2 + judge | 79.6% | 14.8% | 23.7초 |
| **YOLO seg 11m** (conf 0.033) | 79.6% | **25.2%** | **0.07초** |

\* "SAM2 가 마스크를 만든 범위 안에서" 라는 한정이 붙는다 (023 §2).

**이 모델은 남극 전용이다.** 꾸러미 264 시야에 `bp09` 는 **0개**다 — 앞으로도
당분간 일부러 뺀다(7절). 가중치는 `runs/segment/11m-v1seg-1280/weights/best.pt`
(45 MB · `PROVENANCE.md` 동봉 · `runs/` 는 gitignore).

**아직 안 한 것** (025 §9 의 순서 그대로)

1. **싱글턴/합성본 차이가 모델 탓인지 라벨 탓인지 가른다.** 정답 폴리곤도 합성본
   위에서 SAM2 가 만든 것이라 둘이 안 갈린다. **이것이 다른 것들보다 먼저다**
2. **conf 0.1 로 새 시야를 뽑아 검토에 건다** — 검토 부담이 시야당 25.1 → 8.0 개
3. 재현율 상한에 걸린 8개를 눈으로 본다
4. **AM 계열에서 재 본다** — judge 의 정밀도가 AM261 1.0%, 231cm 4.7% 다
5. 깊이 맵을 채널로 · s·l·x 비교 · 슬라이드별 분해

**엔진을 갈면 합성이 유일한 병목이 된다** (33.7초 → 10.1초, 3.3배).

---

## 5. 주요 파일

| 파일 | 무엇 |
|---|---|
| `web/viewer/models.py` | 스키마 19개 모델. 읽기 전에 파일 첫 주석부터. 층은 `Site → Locality → Sample → Slide` |
| `web/viewer/naming.py` | **폴더 이름 → 층. 규칙은 여기 하나뿐이다** — 뷰어·파이프라인·마이그레이션이 같은 것을 본다 |
| `web/viewer/catalog.py` | **개체 카탈로그 번호. 규칙은 여기 하나뿐이다** (105) |
| `web/viewer/data.py` | DB → 뷰가 쓰는 dict. **읽기 전용이라는 약속이 있다.** `geom_box()`·`_spread_link_labels` 도 여기 |
| `web/viewer/images.py` | `Image` 를 만드는 문 하나 |
| `web/viewer/manage_data.py` | 시스템 설정이 쓰는 문 — 만들고 옮기고 지운다. **지우기 문턱이 여기 있다** |
| `web/viewer/outcrop.py` | 노두 현장 사진. NAS 공유에 **파일로만** 산다 |
| `web/viewer/antarctica.py` · `korea.py` | 미리 구운 해안선. 투영식이 `tools/proj*.py` 와 **두 곳에 있다 — 함께 고쳐야 한다** |
| `ops/check_db.py` | **DB 무결성** 29개 검사. 1초. 7번 층 · 8번 묶음 · 9번 카탈로그 |
| `pipeline/judge.py` | 판정 규칙. **여기가 유일한 정의다** (torch 없이 돈다) |
| `pipeline/zen_meta.py` | ZEN XML → 배율. **대물렌즈는 40x 로 고정 계산**한다 |
| `pipeline/runlog.py` | `Run`·`RunBatch` 이력 |
| `ops/backup_db.py` | DB 사본 (WAL 안전). **Django 를 임포트하지 않는 마지막 안전망** |
| `ops/db_sentinel.py` | 무결성 깃발. `/healthz` 가 읽는다 |
| `ops/sync_backup_nas.py` | 검증된 스냅샷만 NAS 로 |
| `ops/export_review.py` | 교정·종명을 git 감사 기록으로 |
| `ops/export_yolo.py` | 검토 → YOLO 꾸러미. **판정을 다시 쓰지 않고 `viewer.data` 를 부른다** |
| `migrate/rebind.py` | 새 검출과 기존 교정을 다시 맺는다 |
| `deploy/host/dbsync.sh` · `dbrun.sh` | DB 를 만지는 스크립트가 들어가는 **유일한 문** (9.2절) |
| `deploy/poll_nas.sh` | 1분 폴러 |

**저장소는 넷으로 갈려 있다** (100). `pipeline/`·`ops/` 는 `/srv` 로 가고,
`migrate/`(이전기)·`tools/`(개발 도구)는 안 간다.

읽을 devlog — **최근 것부터**:

| 문서 | 무엇 |
|---|---|
| `20260812_112_note-to-object.md` | **코멘트를 개체로 · 적는 자리를 카탈로그 하나로.** 옮기면 사고 하나가 구조적으로 사라진다 |
| `20260812_111_drawn-spread-and-grade-move.md` | **그린 마스크가 판마다 번진다 · 등급을 개체로.** 근거는 촬영 방식이다 |
| `20260811_109_auto-confirm.md` | **검토 완료가 남는 개체에 확인 표시를 단다.** `accepted` 와 섞지 않은 이유 |
| `20260811_108_diatom-object.md` | **개체와 판정을 가른 실행.** `DiatomObject` · 묶기가 개체를 합치고 가른다 |
| `20260810_P12_object-and-judgement.md` | **개체와 판정을 가르는 안.** 그때 스키마의 해부가 여기 있다 |
| `20260810_107_link-unify.md` | 묶을 때 엇갈린 분류·종명을 고르게 하기 |
| `20260810_106_yolo-many-planes.md` | **YOLO 는 시야에 판이 여럿이다.** 카탈로그가 못 보는 절반 (접어 둔 stash 는 08-11 에 버렸다 · 3.6절) |
| `20260810_105_object-catalog.md` | **개체 카탈로그.** 번호를 파생으로 둔 이유 · 검토 저장이 동정을 지우던 것 |
| `20260810_104_link-label-spread.md` | 묶인 개체는 분류를 함께 받는다 · **라벨의 집을 어디에 둘까** |
| `20260810_103_frame-save-rejected.md` | 프레임 판의 교정이 통째로 거절되던 것 |
| `20260810_102_link-rejected.md` | 묶기 팝업의 탈락 후보 — 고르면 되살린다 |
| `20260810_101_deploy-v0.9.md` · `100_repo-layout-and-srv.md` | 배포 기록 · 저장소를 넷으로, 운영을 `/srv` 하나로 |
| `20260810_097_poller-stdin-starved.md` | 폴러 3단계가 사흘 죽어 있던 것. **"조용히 성공한 빈 결과" 와 "실패" 를 한 갈래로 접지 말 것** |
| `20260809_P11_object-linking.md` | 같은 개체 묶기의 계획·결정 |
| `20260807_086_crops-dead.md` | 화면 둘이 v0.8.0 내내 500 이었던 것. **URL 을 덮는 시험과 갈래를 덮는 시험은 다르다** |
| `20260806_063_layers-and-locality.md` | **층을 말과 맞춘 것.** 지금 구조의 뿌리 |
| `20260803_P04_yolo-training.md` · `023` · `025` | 학습 계획 · 자료 꾸러미 · 첫 판 성적 |
| `20260730_P02_db-schema.md` | DB 설계와 근거. **DB 작업 전에 읽을 것** |
| `docs/20260810_db-specification.md` · `db-erd.md` | **DB 명세와 ERD** (08-10 갱신 — 테이블 19개) |

마이그레이션은 **`0036_note_to_object`** 가 마지막이다(112 · `v0.12.0`). 그 앞은
`0035_grade_to_object`(111 · `v0.12.0`) · `0034_grade_and_pose`(110 · `v0.11.2`) ·
`0033_objectreview_auto_confirmed`(109 · `v0.11.1`) · `0032_diatom_object`
(P12 · `v0.11.0`) · `0031_catalog_species_and_batch_code`(105 · `v0.10.0`) ·
`0030_objectlink_objectlinkmember_and_more`(P11 · `v0.9.0`) 다.

**칼럼을 걷은 것은 `0032`·`0035`·`0036` 이다.** `0032` 때는 파이프라인을 `v0.5.2`
로 함께 올렸다(옛 이미지는 교정 있는 시야를 지울 때 `FOREIGN KEY constraint failed`
로 죽는다 · 108). **`0035`·`0036` 은 뷰어만 올렸다** — 사본에 `v0.5.2` 파이프라인
컨테이너를 붙여 실측했고, 걷힌 칼럼(`ObjectReview.grade`·`note`)에 파이프라인이
안 닿는 것을 확인했다(114). 나머지는 칼럼을 더하기만 하고 `db_default` 를 줬다.

> **"안 닿는다" 는 지금 코드 기준이다.** `ObjectReview` 행을 **읽는** 코드가
> `pipeline/` 에 한 줄이라도 들어오면 그날로 물린다 — 시야를 지우는 갈래가
> 통과한 것은 FK 도 시그널도 없어 Django 가 **fast-delete** 로 가기 때문이지
> 스키마가 안전해서가 아니다. **다음에 파이프라인 이미지를 구울 때 함께 올린다.**

---

## 6. 자주 빠지는 함정 → `CLAUDE.md`

**목록은 `CLAUDE.md` 의 "자주 빠지는 함정" 한 곳에 있다.** 여기에 같은 목록을
길게 한 벌 더 두었더니 **두 벌이 이미 어긋나 있었다**(항목 48 대 44) — 그래서
2026-08-11 에 여기만 있던 15개를 그쪽으로 옮기고 이 절을 접었다.

**집이 `CLAUDE.md` 인 이유**: 그 파일은 세션마다 저절로 읽히고 이 문서는 사람이
열어야 한다. **전부 실제로 한 번씩 당한 것들이다.**

이 문서가 맡는 것은 **지금 조심할 것**(3절) — 지금 이 자료·이 판에서만 성립하는
것들이다. 늘 성립하는 규칙은 `CLAUDE.md` 로 간다.

## 7. 시료에 대해

```
남극   RS23-GC03      71 / 231 / 369 cm     (로스해 · 추정)
       WAP13-GC47    116 / 450 cm           region = "Bigo Bay"
       AM22-GC10B     25 / 261 cm           region = "아문젠해" — 가정
       GC03-C1        72 / 816 cm           Elephant Island
한국   BP09-0901                            북평분지(동해) · 노두
```

*같은 지점에서 깊이에 따른 군집 변화*와 *지역별 차이*가 분석 목적이다.

**BP09-0901 은 별개 갈래로 다룬다** (2026-08-05 결정). 한국 북평분지 시료라 남극
시추코어와 퇴적 환경도 군집도 다르다.

- **집계·비교에서 남극 시료와 섞지 않는다** — 화면의 `[한국 | 남극 | 전체]` 갈래가
  그 구분을 들고 있다(`Site.area` 가 `kr`/`ant`)
- **YOLO 학습에서도 당분간 따로 간다.** 검토가 채워져도 **그대로 섞지 않는다**
- **밀린 검토를 셀 때 갈래를 밝힌다** — 안 밝히면 "남극 작업이 크게 밀렸다" 로
  잘못 읽힌다

**`AM22` 가 아문센해인지 Amery(동남극)인지 아직 확인 안 됐다** — 코어 대장에서
확인이 필요하다. `Site.lat/lon` 이 있는 곳은 **해역 한가운데를 찍은 대략값**이고
화면에 그렇게 표시된다. `RS23` 은 좌표가 없다.

---

## 8. 이 머신

### 8.1 GPU 는 한 장이다

```
01:00.0  NVIDIA GA104 [GeForce RTX 3060 Ti]   VRAM 8 GB   드라이버 580.173.02
0a:00.0  AMD Cezanne [Radeon Vega]            내장, 화면용
```

VRAM 이 8 GB 뿐이라 파이프라인을 상주시키지 않는다(9.4절). YOLO 추론은 **0.71 GiB
(9%)** 라 폴러와 공존한다.

> `nvidia-smi` 가 죽고 `torch.cuda.is_available()` 이 `False` 면 **재부팅**을
> 의심할 것 — `apt upgrade` 가 드라이버를 올렸는데 실행 중인 커널에 옛 모듈이
> 물려 있던 것이다 (NVML mismatch / CUDA error 804).

### 8.2 학습은 다른 기계에서 한다

**jikhanserver (RTX 8000 48 GB × 2).** 11m·`imgsz=1280`·batch 8 이 **7 GB · 20분**.
두 GPU 를 ocrserver 가 쓰고 있어 자리를 비워야 한다(사용자 승인을 받고 내린다).

꾸러미는 rsync + `SHA256SUMS` 전수 검증으로 옮긴다. **학습은 반드시 꾸러미
디렉토리 안에서 실행한다** — ultralytics 가 `data.yaml` 의 `path:` 를 **실행
디렉토리** 기준으로 푼다.

### 8.3 venv 는 `~/venv/DiaRUGA` 이다

저장소 안의 `.venv` 가 아니다(예전 문서가 그렇게 적고 있었다). Python 3.12.3 ·
`torch 2.13.0+cu126` · `SAM-2 1.0` ·
Django 5.2.16 · opencv-headless. requirements 는 넷으로 갈라져 있다.

**호스트 venv 로 DB 를 만지지 않는다** — 9.2절.

---

## 9. 배포 (P03)

### 9.1 어디서 무엇이 도는가

```
바깥 :80 /DiaRUGA/     ─nginx─▶ 127.0.0.1:8090 ─▶ diaruga-web-1      운영
바깥 :80 /DiaRUGATest/ ─nginx─▶ 127.0.0.1:8091 ─▶ diaruga-test-web-1 테스트 (085)
바깥 :9090 ──301──▶ /DiaRUGA/
/srv/DiaRUGA/    db/  scripts/  bin/  www/  test/  compose  .env    배포
/data3/DiaRUGA/  photos/<촬영일>/<슬라이드>/                         사진·산출물
                stacked/ out/ backup/ hf/ logs/ datasets/ docker/
```

**지금 도는 판**: `IMAGE_TAG=v0.12.0` · `PIPELINE_TAG=v0.5.2`. **테스트 인스턴스는
`v0.11.2` 다**(08-12 에 올린 뒤 안 따라왔다). 판은 `.env` 와 `/healthz` 가 알려
준다 — **이 문서보다 그쪽이 늘 옳다.** 명령은 `CLAUDE.md` 의 "뷰어·배포", 배포 순서와 되돌리기는
[운영 문서](docs/20260805_operations-docker-and-backup.md) 2.6·2.7 이다.

> **`v0.9.1` 부터 운영은 `/srv/DiaRUGA` 하나로 완결된다** (100 · 사용자 방침
> "운영 서버에 저장소가 없을 수도 있다"). cron 세 줄도, 폴러도, 파이프라인
> 컨테이너도 저장소를 안 본다.
>
> **파이프라인 스크립트도 `/srv` 것이 돈다** — 이미지 안의 `/app` 사본이 아니다.
> 그래서 **스크립트 한 줄을 고치는 데 7.2 GB 를 다시 굽지 않는다.** Django 코드만
> 이미지 것을 쓴다(`DIARUGA_APP=/app`).
>
> **`APP` 은 `sys.path` 뒤에 붙인다** — 앞에 넣으면 이미지 안의 옛
> `judge.py`·`zen_meta.py` 가 `/srv` 의 새것을 가린다(100 덧 2). 스크립트를 더할
> 때 이 줄을 베껴 쓸 것.

**테스트 인스턴스** (085) — 판을 내보내기 전에 눌러 보는 자리. **사본 DB 로 돈다**
(운영 `db/` 는 그 컨테이너에 **아예 마운트하지 않았다**). 노두 사진도 사본이다.
`deploy/host/testdeploy.sh <태그>` 하나로 간다.

- **1번(안전 검사)이 그 스크립트가 있는 이유의 절반이다** — 운영 DB 를 가리키는가 ·
  NAS 원본 노두 사진을 가리키는가 · 서브경로가 `/DiaRUGA` 인가 · compose 가 운영
  `db/` 를 마운트하는가. 넷 다 **실제로 서는 것을 확인했다**
- **DB 사본은 기본으로 갈아 끼운다** — 테스트에서 사본이 낡는 것은 기본 고장이다
- **오리진이 같아 `localStorage` 를 운영과 공유한다** (레이어 토글이 넘어간다)

> **이미지를 CI 가 올리는지는 아직 확인 안 됐다**(9.9절). 손으로 올릴 때는
> `DIARUGA_TAG=<판> docker compose -f deploy/docker-compose.yml build web` 뒤에
> 같은 변수로 `push web` 이다.

### 9.2 DB 를 만지는 것은 문 하나로만 들어간다

```bash
deploy/host/dbsync.sh check_db.py     # 저장소 → /srv/DiaRUGA/scripts
deploy/host/dbrun.sh  check_db.py     # 컨테이너 안에서 돈다
deploy/host/dbsync.sh --list          # 옮겨 둔 것이 저장소와 어긋났는가
```

호스트 venv 로도 같은 DB 를 열 수 있지만 그러면 **같은 파일을 두 벌의 환경이
만진다 — 두 번 당했다**(낡은 `models.py` 가 NAS 반입을 죽였고, root 로 돈 컨테이너가
소유자를 바꿨다). **`/srv/DiaRUGA/scripts` 는 평평하다** — 스크립트끼리의 임포트도
평평해야 돈다.

**규약의 전문과 예외(백업 cron)는
[운영 문서](docs/20260805_operations-docker-and-backup.md) 2.5·3.7 에 있다.**

### 9.3 데이터가 저장소 밖으로 나갔다

- 위치는 `.env` 의 `DIARUGA_DATA_ROOT` 가 알려 준다 (`.env.template` 이 견본)
- `photos/` 아래는 `<촬영일>/<슬라이드>/` 두 단계 — NAS 구조와 1:1
- **`review/` 는 저장소에 남아 있다** — git 이 추적하는 교정 감사 기록.
  **`groups_*.json` 은 08-10 에 지웠다** — DB 이전(P02) 전의 산물이라 슬라이드
  셋만 담고 있었고 읽는 곳이 이전기 도구뿐이었다. 필요하면
  `group_focus_series.py -o` 가 다시 만든다(git 이력에도 있다)
- **DB 디렉토리(`db/`)만 마운트한다.** `/srv/DiaRUGA` 을 통째로 물렸더니 `.env` 의
  `SECRET_KEY` 가 컨테이너에서 읽혔다

### 9.4 파이프라인은 일회성으로만 돌린다

`profiles: [manual]` 로 묶어 `up` 에 딸려 뜨지 않는다. 상주 워커로 두면 PyTorch
캐싱 할당자가 최대치 VRAM 을 프로세스가 죽을 때까지 물고 있다. 컨테이너를
`--gpus all` 로 띄우는 것 자체는 VRAM 을 쓰지 않는다.

### 9.5 백업 — cron 에 붙어 있다

```cron
20 * * * *  backup_db.py --keep 24                                       → logs/backup.log
40 4 * * *  timeout 1800 sync_backup_nas.py --newest-only --prune --photos → logs/nas-sync.log
```

```
/data3/DiaRUGA/backup/            시간별 자동 — 24시간 rolling. 여기 것만 NAS 로 간다
                     manual/     사람이 --note 로 뜬 것 — 로테이션·NAS 대상 밖
                     pre_deploy/ deploy.sh 가 판 바꾸기 직전에 (--flat, 20개)
/nfs/temp-share/DiaRUGA/backup/   일별 오프사이트 — 계단식 보관
```

**정책의 전문**(디렉토리가 종류를 가르는 이유 · 로테이션 규칙 · 계단식 보관 ·
`.part` 로 뜨는 절차 · **복구 절차** · 호스트 venv 예외)은
[운영 문서](docs/20260805_operations-docker-and-backup.md) 3절에 있다.

```bash
python ops/db_sentinel.py show                   # 지금 선 깃발
python ops/db_sentinel.py clear backup_db        # 원인을 확인한 뒤 손으로 내린다
```

### 9.6 nginx — 80 의 서브경로다

**컨테이너는 루프백에만 붙는다.** 바깥으로 나가는 문은 nginx 하나뿐이다.

| 파일 | 무엇 |
|---|---|
| `/etc/nginx/snippets/DiaRUGA-subpath.conf` | 실제 서빙. 원본은 `deploy/nginx/` |
| `/etc/nginx/sites-enabled/phyloserver` | 그 스니펫을 `include` 하는 한 줄 |
| `/etc/nginx/sites-enabled/DiaRUGA` | `:9090` → 301 |

80 은 phyloserver 블록이 `server_name` 을 잡고 있어 **같은 블록 안에 location 으로
들어갔다** — 같은 `listen`·`server_name` 으로 server 블록을 둘 두면 nginx 가 앞의
것만 쓴다.

- **`location ^~ /DiaRUGA/` 의 `^~` 를 지우지 말 것** — **nginx 는 접두 location
  보다 정규식을 먼저 본다.** 없으면 대소문자 보정 정규식이 정식 주소까지 잡아채
  **리다이렉트 무한 루프**가 된다
- `proxy_pass http://127.0.0.1:8090/;` 의 **끝 슬래시가 접두를 뗀다**
- Django 쪽은 `DIARUGA_SCRIPT_NAME=/DiaRUGA` → `FORCE_SCRIPT_NAME`
- **배포 중에는 점검 안내가 나간다** — 깃발이 있으면 503 "시스템 점검 중입니다",
  **깃발 없이 502/504 면 "연결할 수 없습니다"**. `error_page 503 = /…` 처럼 `=` 를
  쓰면 **상태가 200 으로 바뀐다** — 쓰지 말 것
- **phyloserver 저장소의 nginx 사본이 설치본보다 뒤처져 있다** — 그쪽에서 배포하면
  `include` 한 줄이 날아간다
- 랜딩 페이지의 카드는 `/srv/paleolab/index.html` 이다. **저장소 사본이 없다**

### 9.7 smoke

`/healthz` 200 · `status=ok` · 판 일치 · **행 수 > 0** · nginx 경유 200 · 슬라이드
링크를 하나 따라가 본다. **200 만으로는 판이 갈렸는지도, 빈 DB 를 물었는지도
모른다.** 폴러도 자동 처리 뒤에 같은 검사를 하고, 안 뜨면 깃발을 세운다(061).

**그런데 슬라이드마다 여는 화면이 시야 목록 하나뿐이다** — `/crops/`·`/detections/`·
`/catalog/` 가 죽어도 통과한다. 실제로 그렇게 다섯 판을 통과했다(086).

### 9.8 NAS 자동 수집이 돈다

```
* * * * * deploy/poll_nas.sh      # 호스트 cron, 1분마다
```

새 슬라이드가 올라오면 가져와 그룹핑·합성·검출까지 스스로 돈다. 할 일이 없으면
1~2초에 끝나고 **GPU 를 건드리지 않는다**(`/data3/DiaRUGA/logs/poll.log`).
마지막 처리는 08-10 09:52 — `260809_rs23-gc03_369cm_100` 의 검출(`yolo-3차`).

> **10:23 의 "정찰 실패" 는 `/srv` 재편 중 스크립트가 잠깐 없던 것이다**
> (`scan_nas.py` 가 11:17 에 자리를 잡았다). 지금은 정상이다 — 정찰이 1분마다
> `last_scan.json` 을 갱신한다.
>
> **긴 처리가 도는 동안에는 정찰이 안 돈다.** `flock -n` 이라 그 사이 주기가
> 조용히 물러난다 — `last_scan.json` 이 안 갱신되는 것은 고장이 아니라 검출이
> 도는 중이라는 뜻이다.

**복사가 끝났는지는 mtime 으로 판단하지 않는다.** `rsync -a` 가 원본 시각을
보존해서 한창 들어오는 중에도 조용해 보인다. 폴더의 (파일 수·바이트) 지문을
기억해 두고 `--stable-min`(기본 5분) 동안 안 바뀌어야 가져온다.

### 9.9 아직 안 한 것

- **폴러가 멈춘 것을 화면은 말하지만**(098 · 시스템 설정 · 파이프라인) **화면을
  안 보고 있을 때는 여전히 아무도 안 알려 준다.** 026 에서 4시간 반, 097 에서
  사흘을 몰랐다
- **교정 수가 줄어드는 것도 백업 로그에서만 보인다** — `ops/check_db.py` 에
  "직전 백업 대비 교정 수" 를 넣는 것을 검토한다
- **`smoke.sh` 가 여는 화면이 한 장뿐이다** (9.7절)
- **뷰어에 인증이 없다.** 필요해지면 `DiaRUGA-subpath.conf` 에 `auth_basic` 을
  걸면 된다 — Django 를 건드릴 필요가 없다
- 표준 deploy 동사 5개 중 셋이 없다 — `rollback`·`preflight`·`seed`. `DEPLOY.md` 도 없다
- **`/` 가 83% (38 GB 남음).** 도커 자리는 `/data3/DiaRUGA/docker` 로 옮겼고
  `/data3` 는 10% 다 — 이미지가 쌓여도 `/` 를 밀지 않는다
- **테스트 인스턴스가 `v0.11.2` 에 머물러 있다** — 운영이 `v0.12.0` 이라 두 판이
  갈렸다. 다음에 눌러 볼 일이 있으면 `testdeploy.sh v0.12.0` 부터

> **CI 가 이미지를 올리는 것은 확인됐다** (08-13 · 114). `v0.12.0` 태그에서
> `Docker Hub 로그인` 레이어가 실제로 돌았고 `honestjung/diaruga:v0.12.0` 이
> 레지스트리에 섰다 — **CI 가 올린 첫 이미지다**(`v0.8.0`~`v0.11.2` 는 전부 이
> 머신에서 손으로 올렸다). **PR 에서는 굽기만 하고 로그인 단계를 건너뛴다** —
> 굽기는 늘 검증하되 올리는 것은 태그일 때만이다. 배포 절차에서 "손으로 push"
> 를 뺄 수 있다.

---

## 10. 다음에 할 일 → `TODOs.md`

**목록과 근거는 [TODOs.md](TODOs.md) 에 있다.** 여기서는 **지금 맨 앞 셋**만 적는다
(그보다 아래는 순서가 자주 바뀌어, 두 곳에 적으면 갈린다).

1. **개체 카탈로그를 채운다** — 종명 **0건**, 등급·자세도 **0건**이다. 이제 동정은
   종명·등급·자세 셋이고, **개체 코멘트도 이 화면에서만 적는다**(`v0.12.0` · 112).
   **`check_db` 10번은 매긴 개체가 생겨야 실제로 검사를 시작한다** — 지금은 절
   전체를 건너뛴다
2. **카탈로그가 YOLO 개체의 절반을 못 본다** (106) — 프레임에만 있는 개체 5,123이
   카드에 안 나온다. 번지기가 들어갔으니 **다음은 이쪽이다**
3. **뷰어에서 도감을 부른다 (Diadiction · [P14](devlog/20260812_P14_diadiction-lookup.md))** —
   막힌 자리가 하나뿐이다: **`web` 컨테이너가 NAS 의 `Diadiction` 공유를 못 본다.**
   `/srv/DiaRUGA/docker-compose.yml` 에 마운트 한 줄을 더하고 `web` 만 다시 띄우면
   되고 **이미지는 안 구워도 된다** — `/srv` 는 `paleoadmin` 자리다.
   **자료 쪽은 준비가 끝났다**([117](devlog/20260813_117_east-antarctic-plate-index.md),
   2026-08-13): 마지막까지 색인이 없던 동남극 도판집에 색인을 만들어 **도감 셋이
   다 md 색인을 갖는다.** 마운트가 열리면 바로 `atlas.py` 로 간다

**코드는 누구나 고친다** (`paleoadmin`·`jikhanjung`·`sclee`, 2026-08-12).
아래 표는 *"이 갈래는 지금 누가 들고 있나"* 이지 **권한이 아니다** — 계정마다
작업 트리가 따로라(`CLAUDE.md` 커밋 절) 같은 자리를 둘이 동시에 만지면 부딪히니
들고 있는 사람을 적어 두는 것뿐이다.

**배포만 다르다 — `paleoadmin` 이 한다.** `/srv/DiaRUGA`·운영 DB·백업·도커·
폴러가 전부 그 계정에 있고, 배포는 그것들을 한 창에서 함께 만지는 일이다.

| 무엇 | 누가 | 언제부터 |
|---|---|---|
| **배포 전부** (이미지 굽기 · `deploy.sh` · `testdeploy.sh` · `dbsync.sh`) | **`paleoadmin`** | 2026-08-12 |
| 등급·자세 · 번지기 · 코멘트 이전 | `sclee` — **끝났다.** `v0.11.2`·`v0.12.0` 으로 나갔다 (114) | 2026-08-12 |
| 고아 화면 `/orphans/` (P02 8단계) | **보류** — YOLO 4차 뒤에 다시 정한다 ([P13](devlog/20260812_P13_orphans.md)) | 2026-08-12 |

**사람이 하는 일은 남극 2차 검토다**(309/511). 위 셋은 그 자료를 쓰는 순서다.

외부에 걸린 것: **ZEN 대물렌즈 설정을 실제 교환대와 맞추기**(지금은 어긋나도 경고만
찍고 40x 로 간다) · **`AM22` 가 아문센해인지 Amery 인지**(7절) · **`Site.lat/lon`**
(좌표가 들어오면 지도가 저절로 정확해진다).
